From bcc45e17c4105a2527b0bd9696d222748e10b601 Mon Sep 17 00:00:00 2001
From: Inbae Jeon <inbest@telechips.com>
Date: Thu, 4 Dec 2025 16:27:31 +0900
Subject: [PATCH] [upg] pcie: update pcie modules to sync with latest

Updated pcie modules to sync with latest.

TCS: TMBSP-4665
---
 .../override/tcc7500-lpd4x321-pcie.dtsi       |   9 +-
 .../dts/telechips/tcc750x/tcc750x-pcie.dtsi   |   1 +
 .../override/tcc8030-lpd4321-pcie.dtsi        |   3 +-
 .../override/tcc8031pe-lpd4321-pcie.dtsi      |   5 +-
 .../dts/telechips/tcc803x/tcc803x-pcie.dtsi   |   2 +-
 .../tcc8050_53-lpd4x322-sv0.1-pcie.dtsi       |   6 -
 .../tcc8050_53-lpd4x322-sv1.0-pcie.dtsi       |   6 -
 .../override/tcc8059-lpd4x321-pcie.dtsi       |   6 -
 .../tcc805x/override/topst-d3-g-pcie.dtsi     |   6 -
 .../dts/telechips/tcc805x/tcc805x-pcie.dtsi   |   1 +
 .../override/tcc8070-lpd4x322-pcie.dtsi       |  12 -
 .../dts/telechips/tcc807x/tcc807x-pcie.dtsi   |  24 +-
 drivers/pci/controller/dwc/telechips/Kconfig  |   4 +-
 .../pci/controller/dwc/telechips/pcie-axon.h  |  42 +-
 .../controller/dwc/telechips/pcie-dolphin.h   |  30 +-
 .../pci/controller/dwc/telechips/pcie-tcc.c   | 789 +++++-------------
 drivers/phy/telechips/Kconfig                 |   8 +-
 drivers/phy/telechips/Makefile                |   4 +-
 ...phy-sec08lpp-pcie.c => phy-sf08lpp-pcie.c} | 601 +++++++------
 ...phy-sec14lpp-pcie.c => phy-sf14lpp-pcie.c} | 243 +++---
 drivers/phy/telechips/phy-ss14lpp-x1ns-pcie.c |  79 +-
 drivers/phy/telechips/phy-ss14lpp-x2ns-pcie.c | 244 +++---
 include/soc/telechips/dw_edma.h               |  30 -
 23 files changed, 850 insertions(+), 1305 deletions(-)
 rename drivers/phy/telechips/{phy-sec08lpp-pcie.c => phy-sf08lpp-pcie.c} (59%)
 rename drivers/phy/telechips/{phy-sec14lpp-pcie.c => phy-sf14lpp-pcie.c} (60%)
 delete mode 100644 include/soc/telechips/dw_edma.h

diff --git a/arch/arm64/boot/dts/telechips/tcc750x/override/tcc7500-lpd4x321-pcie.dtsi b/arch/arm64/boot/dts/telechips/tcc750x/override/tcc7500-lpd4x321-pcie.dtsi
index 8aed7f040c00..2796545e181c 100644
--- a/arch/arm64/boot/dts/telechips/tcc750x/override/tcc7500-lpd4x321-pcie.dtsi
+++ b/arch/arm64/boot/dts/telechips/tcc750x/override/tcc7500-lpd4x321-pcie.dtsi
@@ -4,6 +4,7 @@
  */
 
 &pcie_phy {
+	telechips,enable-ext-refclk;
 	status = "okay";
 };
 
@@ -12,15 +13,11 @@ &pcie {
 	pinctrl-0 = <&pcie_rst>;
 	pinctrl-1 = <&pcie_rst_idle>;
 	pinctrl-2 = <&pcie_rst_idle>;
-	refclk_type = <0>;
+	telechips,enable-ext-refclk;
 };
 
 &pcie_epc {
-	pinctrl-names = "default", "idle", "sleep";
-	pinctrl-0 = <&pcie_rst>;
-	pinctrl-1 = <&pcie_rst_idle>;
-	pinctrl-2 = <&pcie_rst_idle>;
-	refclk_type = <1>;
+	telechips,enable-ext-refclk;
 };
 
 &pinctrl {
diff --git a/arch/arm64/boot/dts/telechips/tcc750x/tcc750x-pcie.dtsi b/arch/arm64/boot/dts/telechips/tcc750x/tcc750x-pcie.dtsi
index 866a668fa1c3..c0ede9b2f2ac 100644
--- a/arch/arm64/boot/dts/telechips/tcc750x/tcc750x-pcie.dtsi
+++ b/arch/arm64/boot/dts/telechips/tcc750x/tcc750x-pcie.dtsi
@@ -27,6 +27,7 @@ pcie: pcie@12000000 {
 		reg-names = "dbi", "link", "config";
 		interrupts = <GIC_SPI 62 IRQ_TYPE_LEVEL_HIGH>;
 		resets = <&sysckc_rst SYSBUS_PCIe>;
+		linux,pci-domain = <0>;
 		bus-range = <0x00 0xff>;
 		#address-cells = <3>;
 		#size-cells = <2>;
diff --git a/arch/arm64/boot/dts/telechips/tcc803x/override/tcc8030-lpd4321-pcie.dtsi b/arch/arm64/boot/dts/telechips/tcc803x/override/tcc8030-lpd4321-pcie.dtsi
index dc82d34947b9..6be40c94142d 100644
--- a/arch/arm64/boot/dts/telechips/tcc803x/override/tcc8030-lpd4321-pcie.dtsi
+++ b/arch/arm64/boot/dts/telechips/tcc803x/override/tcc8030-lpd4321-pcie.dtsi
@@ -4,6 +4,7 @@
  */
 
 &pcie_phy {
+	telechips,enable-ext-refclk;
 	status = "okay";
 };
 
@@ -12,7 +13,7 @@ &pcie {
 	pinctrl-0 = <&pcie_rst>;
 	pinctrl-1 = <&pcie_rst_idle>;
 	pinctrl-2 = <&pcie_rst_idle>;
-	refclk_type = <1>;
+	telechips,enable-ext-refclk;
 };
 
 &pinctrl {
diff --git a/arch/arm64/boot/dts/telechips/tcc803x/override/tcc8031pe-lpd4321-pcie.dtsi b/arch/arm64/boot/dts/telechips/tcc803x/override/tcc8031pe-lpd4321-pcie.dtsi
index 3a823f0e8123..87b0a0711bba 100644
--- a/arch/arm64/boot/dts/telechips/tcc803x/override/tcc8031pe-lpd4321-pcie.dtsi
+++ b/arch/arm64/boot/dts/telechips/tcc803x/override/tcc8031pe-lpd4321-pcie.dtsi
@@ -3,12 +3,15 @@
  * Copyright (C) 2023 Telechips Inc.
  */
 
+&pcie_phy  {
+	status = "okay";
+};
+
 &pcie {
 	pinctrl-names = "default", "idle", "sleep";
 	pinctrl-0 = <&pcie_rst>;
 	pinctrl-1 = <&pcie_rst_idle>;
 	pinctrl-2 = <&pcie_rst_idle>;
-	refclk_type = <0>;
 };
 
 &pinctrl {
diff --git a/arch/arm64/boot/dts/telechips/tcc803x/tcc803x-pcie.dtsi b/arch/arm64/boot/dts/telechips/tcc803x/tcc803x-pcie.dtsi
index 46a6f6e4a87f..432e9d24d4f5 100644
--- a/arch/arm64/boot/dts/telechips/tcc803x/tcc803x-pcie.dtsi
+++ b/arch/arm64/boot/dts/telechips/tcc803x/tcc803x-pcie.dtsi
@@ -17,7 +17,6 @@ pcie_phy: pcie_phy@118800000 {
 			<&clk_peri PERI_PCIE_PCS>, <&clk_peri PERI_PCIE_REF_EXT>,
 			<&fbus_smu FBUS_HSIO>;
 		clock-names = "pcie_aux", "pcie_apb", "pcie_pcs", "pcie_ref", "fbus_hsio";
-		pms = <0x0>;
 		status = "disabled";
 	};
 
@@ -28,6 +27,7 @@ pcie: pcie@11800000 {
 			0 0x11000000 0 0x00100000>;
 		reg-names = "dbi", "link", "config";
 		interrupts = <GIC_SPI 138 IRQ_TYPE_LEVEL_HIGH>;
+		linux,pci-domain = <0>;
 		resets = <&pmu_rst SMUBUS_PCIe>;
 		bus-range = <0x00 0xff>;
 		#address-cells = <3>;
diff --git a/arch/arm64/boot/dts/telechips/tcc805x/override/tcc8050_53-lpd4x322-sv0.1-pcie.dtsi b/arch/arm64/boot/dts/telechips/tcc805x/override/tcc8050_53-lpd4x322-sv0.1-pcie.dtsi
index ac1137c939d6..f2810ead3af9 100644
--- a/arch/arm64/boot/dts/telechips/tcc805x/override/tcc8050_53-lpd4x322-sv0.1-pcie.dtsi
+++ b/arch/arm64/boot/dts/telechips/tcc805x/override/tcc8050_53-lpd4x322-sv0.1-pcie.dtsi
@@ -12,15 +12,9 @@ &pcie {
 	pinctrl-0 = <&pcie_rst>;
 	pinctrl-1 = <&pcie_rst_idle>;
 	pinctrl-2 = <&pcie_rst_idle>;
-	refclk_type = <0>;
 };
 
 &pcie_epc {
-	pinctrl-names = "default", "idle", "sleep";
-	pinctrl-0 = <&pcie_rst>;
-	pinctrl-1 = <&pcie_rst_idle>;
-	pinctrl-2 = <&pcie_rst_idle>;
-	refclk_type = <0>;
 };
 
 &pinctrl {
diff --git a/arch/arm64/boot/dts/telechips/tcc805x/override/tcc8050_53-lpd4x322-sv1.0-pcie.dtsi b/arch/arm64/boot/dts/telechips/tcc805x/override/tcc8050_53-lpd4x322-sv1.0-pcie.dtsi
index 4d8d92ec0a72..0fc44c6ed011 100644
--- a/arch/arm64/boot/dts/telechips/tcc805x/override/tcc8050_53-lpd4x322-sv1.0-pcie.dtsi
+++ b/arch/arm64/boot/dts/telechips/tcc805x/override/tcc8050_53-lpd4x322-sv1.0-pcie.dtsi
@@ -12,15 +12,9 @@ &pcie {
 	pinctrl-0 = <&pcie_rst>;
 	pinctrl-1 = <&pcie_rst_idle>;
 	pinctrl-2 = <&pcie_rst_idle>;
-	refclk_type = <0>;
 };
 
 &pcie_epc {
-	pinctrl-names = "default", "idle", "sleep";
-	pinctrl-0 = <&pcie_rst>;
-	pinctrl-1 = <&pcie_rst_idle>;
-	pinctrl-2 = <&pcie_rst_idle>;
-	refclk_type = <0>;
 };
 
 &pinctrl {
diff --git a/arch/arm64/boot/dts/telechips/tcc805x/override/tcc8059-lpd4x321-pcie.dtsi b/arch/arm64/boot/dts/telechips/tcc805x/override/tcc8059-lpd4x321-pcie.dtsi
index d8b1d5818725..dac298186a84 100644
--- a/arch/arm64/boot/dts/telechips/tcc805x/override/tcc8059-lpd4x321-pcie.dtsi
+++ b/arch/arm64/boot/dts/telechips/tcc805x/override/tcc8059-lpd4x321-pcie.dtsi
@@ -12,15 +12,9 @@ &pcie {
 	pinctrl-0 = <&pcie_rst>;
 	pinctrl-1 = <&pcie_rst_idle>;
 	pinctrl-2 = <&pcie_rst_idle>;
-	refclk_type = <0>;
 };
 
 &pcie_epc {
-	pinctrl-names = "default", "idle", "sleep";
-	pinctrl-0 = <&pcie_rst>;
-	pinctrl-1 = <&pcie_rst_idle>;
-	pinctrl-2 = <&pcie_rst_idle>;
-	refclk_type = <0>;
 };
 
 &pinctrl {
diff --git a/arch/arm64/boot/dts/telechips/tcc805x/override/topst-d3-g-pcie.dtsi b/arch/arm64/boot/dts/telechips/tcc805x/override/topst-d3-g-pcie.dtsi
index 896fa03225e1..eaeb88e5d3da 100644
--- a/arch/arm64/boot/dts/telechips/tcc805x/override/topst-d3-g-pcie.dtsi
+++ b/arch/arm64/boot/dts/telechips/tcc805x/override/topst-d3-g-pcie.dtsi
@@ -12,16 +12,10 @@ &pcie {
 	pinctrl-0 = <&pcie_rst>;
 	pinctrl-1 = <&pcie_rst_idle>;
 	pinctrl-2 = <&pcie_rst_idle>;
-	refclk_type = <0>;
 	status = "okay";
 };
 
 &pcie_epc {
-	pinctrl-names = "default", "idle", "sleep";
-	pinctrl-0 = <&pcie_rst>;
-	pinctrl-1 = <&pcie_rst_idle>;
-	pinctrl-2 = <&pcie_rst_idle>;
-	refclk_type = <0>;
 };
 
 &pinctrl {
diff --git a/arch/arm64/boot/dts/telechips/tcc805x/tcc805x-pcie.dtsi b/arch/arm64/boot/dts/telechips/tcc805x/tcc805x-pcie.dtsi
index b30290bb7ca3..2a362fc2b90b 100644
--- a/arch/arm64/boot/dts/telechips/tcc805x/tcc805x-pcie.dtsi
+++ b/arch/arm64/boot/dts/telechips/tcc805x/tcc805x-pcie.dtsi
@@ -27,6 +27,7 @@ pcie: pcie@11000000 {
 		reg-names = "dbi", "link", "config";
 		interrupts = <GIC_SPI 138 IRQ_TYPE_LEVEL_HIGH>;
 		resets = <&pmu_rst SMUBUS_PCIe>;
+		linux,pci-domain = <0>;
 		bus-range = <0x00 0xff>;
 		#address-cells = <3>;
 		#size-cells = <2>;
diff --git a/arch/arm64/boot/dts/telechips/tcc807x/override/tcc8070-lpd4x322-pcie.dtsi b/arch/arm64/boot/dts/telechips/tcc807x/override/tcc8070-lpd4x322-pcie.dtsi
index 29752e4c6dc9..e5cade2b4b22 100644
--- a/arch/arm64/boot/dts/telechips/tcc807x/override/tcc8070-lpd4x322-pcie.dtsi
+++ b/arch/arm64/boot/dts/telechips/tcc807x/override/tcc8070-lpd4x322-pcie.dtsi
@@ -16,7 +16,6 @@ &pcie0 {
 	pinctrl-0 = <&pcie0_rst>;
 	pinctrl-1 = <&pcie0_rst_idle>;
 	pinctrl-2 = <&pcie0_rst_idle>;
-	refclk_type = <0>;
 };
 
 &pcie1 {
@@ -24,23 +23,12 @@ &pcie1 {
 	pinctrl-0 = <&pcie1_rst>;
 	pinctrl-1 = <&pcie1_rst_idle>;
 	pinctrl-2 = <&pcie1_rst_idle>;
-	refclk_type = <0>;
 };
 
 &pcie_epc0 {
-	pinctrl-names = "default", "idle", "sleep";
-	pinctrl-0 = <&pcie0_rst>;
-	pinctrl-1 = <&pcie0_rst_idle>;
-	pinctrl-2 = <&pcie0_rst_idle>;
-	refclk_type = <0>;
 };
 
 &pcie_epc1 {
-	pinctrl-names = "default", "idle", "sleep";
-	pinctrl-0 = <&pcie1_rst>;
-	pinctrl-1 = <&pcie1_rst_idle>;
-	pinctrl-2 = <&pcie1_rst_idle>;
-	refclk_type = <0>;
 };
 
 &pinctrl {
diff --git a/arch/arm64/boot/dts/telechips/tcc807x/tcc807x-pcie.dtsi b/arch/arm64/boot/dts/telechips/tcc807x/tcc807x-pcie.dtsi
index 679ed38e1b13..fdcf2225b7c5 100644
--- a/arch/arm64/boot/dts/telechips/tcc807x/tcc807x-pcie.dtsi
+++ b/arch/arm64/boot/dts/telechips/tcc807x/tcc807x-pcie.dtsi
@@ -11,9 +11,8 @@ pcie0_phy: pcie_phy@11100000 {
 		compatible = "samsung,ln08lpp-pcie-phy";
 		reg = <0 0x11100000 0 0x10000
 			0 0x11110000 0 0x10000
-			0 0x11130000 0 0x10000
 			0 0x11140000 0 0x10000>;
-		reg-names = "phy", "clk", "pcs", "pma";
+		reg-names = "phy", "clk", "pma";
 		#phy-cells = <0>;
 		clocks = <&clk_peri PERI_PCIE0_PHY_CR_CLK>, <&fbus_pcie FBUS_PCIe0>;
 		clock-names = "pcie_phy", "pcie_fbus";
@@ -24,9 +23,8 @@ pcie1_phy: pcie_phy@11500000 {
 		compatible = "samsung,ln08lpp-pcie-phy";
 		reg = <0 0x11500000 0 0x10000
 			0 0x11510000 0 0x10000
-			0 0x11530000 0 0x10000
 			0 0x11540000 0 0x10000>;
-		reg-names = "phy", "clk", "pcs", "pma";
+		reg-names = "phy", "clk", "pma";
 		#phy-cells = <0>;
 		clocks = <&clk_peri PERI_PCIE1_PHY_CR_CLK>, <&fbus_pcie FBUS_PCIe1>;
 		clock-names = "pcie_phy", "pcie_fbus";
@@ -39,8 +37,9 @@ pcie0: pcie@11000000 {
 			0 0x11120000 0 0x10000
 			5 0x00000000 0 0x01000000>;
 		reg-names = "dbi", "link", "config";
-		interrupts = <GIC_SPI 335 IRQ_TYPE_LEVEL_HIGH>;
+		interrupts = <GIC_SPI 335 IRQ_TYPE_LEVEL_HIGH 0>;
 		resets = <&pmu_rst SMUBUS_PCIe>;
+		linux,pci-domain = <0>;
 		bus-range = <0x00 0xff>;
 		#address-cells = <3>;
 		#size-cells = <2>;
@@ -49,7 +48,7 @@ pcie0: pcie@11000000 {
 			0x82000000 0 0x10000000 5 0x10000000 0 0x30000000>;
 		#interrupt-cells = <1>;
 		interrupt-map-mask = <0 0 0 0>;
-		interrupt-map = <0 0 0 0 &gic 0 0 GIC_SPI 335 IRQ_TYPE_LEVEL_HIGH>;
+		interrupt-map = <0 0 0 0 &gic 0 0 GIC_SPI 335 IRQ_TYPE_LEVEL_HIGH 0>;
 		num-lanes = <1>;
 		num-viewport = <4>;
 		max-link-speed = <4>;
@@ -64,8 +63,9 @@ pcie1: pcie@11400000 {
 			0 0x11520000 0 0x10000
 			5 0x80000000 0 0x01000000>;
 		reg-names = "dbi", "link", "config";
-		interrupts = <GIC_SPI 338 IRQ_TYPE_LEVEL_HIGH>;
+		interrupts = <GIC_SPI 338 IRQ_TYPE_LEVEL_HIGH 0>;
 		resets = <&pmu_rst SMUBUS_PCIe>;
+		linux,pci-domain = <1>;
 		bus-range = <0x00 0xff>;
 		#address-cells = <3>;
 		#size-cells = <2>;
@@ -74,7 +74,7 @@ pcie1: pcie@11400000 {
 			0x82000000 0 0x10000000 5 0x90000000 0 0x30000000>;
 		#interrupt-cells = <1>;
 		interrupt-map-mask = <0 0 0 0>;
-		interrupt-map = <0 0 0 0 &gic 0 0 GIC_SPI 338 IRQ_TYPE_LEVEL_HIGH>;
+		interrupt-map = <0 0 0 0 &gic 0 0 GIC_SPI 338 IRQ_TYPE_LEVEL_HIGH 0>;
 		num-lanes = <1>;
 		num-viewport = <4>;
 		max-link-speed = <4>;
@@ -89,8 +89,8 @@ pcie_epc0: pcie_epc@11000000 {
 			0 0x11120000 0 0x10000
 			5 0x00000000 0 0x01000000>;
 		reg-names = "dbi", "link", "addr_space";
-		interrupts = <GIC_SPI 335 IRQ_TYPE_LEVEL_HIGH>,
-			<GIC_SPI 336 IRQ_TYPE_LEVEL_HIGH>;
+		interrupts = <GIC_SPI 335 IRQ_TYPE_LEVEL_HIGH 0>,
+			<GIC_SPI 336 IRQ_TYPE_LEVEL_HIGH 0>;
 		resets = <&pmu_rst SMUBUS_PCIe>;
 		max-link-speed = <4>;
 		num-lanes = <1>;
@@ -107,8 +107,8 @@ pcie_epc1: pcie_epc@11400000 {
 			0 0x11520000 0 0x10000
 			5 0x80000000 0 0x01000000>;
 		reg-names = "dbi", "link", "addr_space";
-		interrupts = <GIC_SPI 338 IRQ_TYPE_LEVEL_HIGH>,
-			<GIC_SPI 339 IRQ_TYPE_LEVEL_HIGH>;
+		interrupts = <GIC_SPI 338 IRQ_TYPE_LEVEL_HIGH 0>,
+			<GIC_SPI 339 IRQ_TYPE_LEVEL_HIGH 0>;
 		resets = <&pmu_rst SMUBUS_PCIe>;
 		max-link-speed = <4>;
 		num-lanes = <1>;
diff --git a/drivers/pci/controller/dwc/telechips/Kconfig b/drivers/pci/controller/dwc/telechips/Kconfig
index a03b7db7570b..801fd147d3a4 100644
--- a/drivers/pci/controller/dwc/telechips/Kconfig
+++ b/drivers/pci/controller/dwc/telechips/Kconfig
@@ -8,8 +8,8 @@
 config PCIE_TELECHIPS
 	tristate "Telechips PCIe controller"
 	depends on ARCH_TELECHIPS
-	select PHY_SEC14LPP_PCIE if ARCH_TCC803X
-	select PHY_SEC08LPP_PCIE if ARCH_TCC807X
+	select PHY_SF14LPP_PCIE if ARCH_TCC803X
+	select PHY_SF08LPP_PCIE if ARCH_TCC807X
 	select PHY_SS14LPP_X1NS_PCIE if (ARCH_TCC805X || ARCH_TCC750X)
 	select PHY_SS14LPP_X2NS_PCIE if ARCH_TCN100X
 	help
diff --git a/drivers/pci/controller/dwc/telechips/pcie-axon.h b/drivers/pci/controller/dwc/telechips/pcie-axon.h
index e465d8ed11b3..2fa7fb0edef3 100644
--- a/drivers/pci/controller/dwc/telechips/pcie-axon.h
+++ b/drivers/pci/controller/dwc/telechips/pcie-axon.h
@@ -24,11 +24,10 @@
 #define PCIE_LINK_CFG33			(0x084U)
 #define PCIE_LINK_CFG43			(0x0ACU)
 #define PCIE_LINK_CFG56			(0x0E0U)
+#define PCIE_LINK_CFG85			(0x154U)
 #define PCIE_LINK_CFG88			(0x160U)
 #define PCIE_LINK_CFG89			(0x164U)
 #define PCIE_LINK_CFG91			(0x16CU)
-#define PCIE_LINK_CFG92			(0x170U)
-#define PCIE_LINK_CFG93			(0x174U)
 
 #define PCIE_VDM_CTRL		(PCIE_LINK_CFG00)
 #define PCIE_VDM_CODE		(PCIE_LINK_CFG01)
@@ -45,11 +44,10 @@
 #define PCIE_SMLH_LINK_STS		(PCIE_LINK_CFG33)
 #define PCIE_PHY_RST_CTRL		(PCIE_LINK_CFG43)
 #define PCIE_SYS_INT		(PCIE_LINK_CFG56)
+#define PCIE_PM_CTRL		(PCIE_LINK_CFG85)
 #define PCIE_DBI_INDIRECT_ADDR_MASK		(PCIE_LINK_CFG88)
 #define PCIE_APP_CTRL		(PCIE_LINK_CFG89)
 #define PCIE_LINK_INTR		(PCIE_LINK_CFG91)
-#define PCIE_EDMA_INTR_STS		(PCIE_LINK_CFG92)
-#define PCIE_EDMA_INTR_EN		(PCIE_LINK_CFG93)
 
 /*
  * Mask/shift bits in PCIe related registers
@@ -83,7 +81,9 @@
 #define PCIE_LINK_CFG_RDLH_LINK_UP_MASK		((u32)0x1U << PCIE_LINK_CFG_RDLH_LINK_UP_SHIFT)
 
 #define PCIE_LINK_CFG_SMLH_LINK_UP_SHIFT		(17U)
+#define PCIE_LINK_CFG_APPS_PM_XMT_TURNOFF_SHIFT		(1U)
 #define PCIE_LINK_CFG_SMLH_LINK_UP_MASK		((u32)0x1U << PCIE_LINK_CFG_SMLH_LINK_UP_SHIFT)
+#define PCIE_LINK_CFG_APPS_PM_XMT_TURNOFF_MASK		((u32)0x1U << PCIE_LINK_CFG_APPS_PM_XMT_TURNOFF_SHIFT)
 
 #define PCIE_LINK_CFG_LTSSM_ENABLE_SHIFT		(2U)
 #define PCIE_LINK_CFG_LTSSM_ENABLE_MASK		((u32)0x1U << PCIE_LINK_CFG_LTSSM_ENABLE_SHIFT)
@@ -91,32 +91,26 @@
 #define PCIE_LINK_CFG_DEVICE_TYPE_SHIFT		(6U)
 #define PCIE_LINK_CFG_DEVICE_TYPE_MASK		((u32)0xFU << PCIE_LINK_CFG_DEVICE_TYPE_SHIFT)
 
-#define PCIE_LINK_CFG_INDIRECT_ADDR_MASK		(0x200000U)
+#define PCIE_LINK_CFG_PM_LINKST_IN_L2_SHIFT		(12U)
+#define PCIE_LINK_CFG_PM_LINKST_IN_L2_MASK		((u32)0x1U << PCIE_LINK_CFG_PM_LINKST_IN_L2_SHIFT)
 
-#define PCIE_LINK_CFG_MSI_INT_MASK		(0x60040021U)
-#define PCIE_LINK_CFG_INTX_MASK		(0x0FF8FF82U)
-#define PCIE_LINK_CFG_INTR_EN_MASK		(0xFFFFFFFDU)
+#define PCIE_LINK_CFG_DBI_INDIRECT_ADDR_MASK		(0x200000U)
+#define PCIE_LINK_CFG_SLV_INDIRECT_ADDR_MASK		(0x3FFFFFFFU)
+
+#define PCIE_LINK_CFG_MSI_INT_MASK		(0x60050021U)
+#define PCIE_LINK_CFG_INTX_MASK		(0x988AFFDAU)
+#define PCIE_LINK_CFG_INTR_EN_MASK		(0xF88FFFF9U)
 
 #define PCIE_LINK_CFG_SYS_INT_SHIFT		(0U)
 #define PCIE_LINK_CFG_SYS_INT_MASK		((u32)0x1 << PCIE_LINK_CFG_SYS_INT_SHIFT)
 
-#define PCIE_LINK_CFG_REFCLK_EXT_EN_SHIFT		(8U)
-#define PCIE_LINK_CFG_PHY_REFCLK_SEL_SHIFT		(6U)
-#define PCIE_LINK_CFG_REFCLK_EXT_EN_MASK		((u32)0x1U << PCIE_LINK_CFG_REFCLK_EXT_EN_SHIFT)
-#define PCIE_LINK_CFG_PHY_REFCLK_SEL_MASK		((u32)0x3U << PCIE_LINK_CFG_PHY_REFCLK_SEL_SHIFT)
-
 #define PCIE_LINK_CFG_POWER_UP_RST_SHIFT		(6U)
 #define PCIE_LINK_CFG_POWER_UP_RST_MASK		((u32)0x1U << PCIE_LINK_CFG_POWER_UP_RST_SHIFT)
 
-#define PCIE_LINK_CFG_EDMA_RD_INTR_STS_SHIFT		(4U)
-#define PCIE_LINK_CFG_EDMA_WR_INTR_STS_SHIFT		(0U)
-#define PCIE_LINK_CFG_EDMA_RD_INTR_STS_MASK		((u32)0xFU << PCIE_LINK_CFG_EDMA_RD_INTR_STS_SHIFT)
-#define PCIE_LINK_CFG_EDMA_WR_INTR_STS_MASK		((u32)0xFU << PCIE_LINK_CFG_EDMA_WR_INTR_STS_SHIFT)
-#define PCIE_LINK_CFG_EDMA_INTR_STS_MASK		(PCIE_LINK_CFG_EDMA_RD_INTR_STS_MASK | PCIE_LINK_CFG_EDMA_WR_INTR_STS_MASK)
-
-#define PCIE_LINK_CFG_EDMA_RD_INTR_EN_SHIFT		(4U)
-#define PCIE_LINK_CFG_EDMA_WR_INTR_EN_SHIFT		(0U)
-#define PCIE_LINK_CFG_EDMA_RD_INTR_EN_MASK		((u32)0xFU << PCIE_LINK_CFG_EDMA_RD_INTR_EN_SHIFT)
-#define PCIE_LINK_CFG_EDMA_WR_INTR_EN_MASK		((u32)0xFU << PCIE_LINK_CFG_EDMA_WR_INTR_EN_SHIFT)
-#define PCIE_LINK_CFG_EDMA_INTR_EN_MASK		(PCIE_LINK_CFG_EDMA_RD_INTR_EN_MASK | PCIE_LINK_CFG_EDMA_WR_INTR_EN_MASK)
+/* Not used */
+#define PCIE_LINK_CFG_REFCLK_EXT_EN_SHIFT		(0U)
+#define PCIE_LINK_CFG_PHY_REFCLK_SEL_SHIFT		(0U)
+#define PCIE_LINK_CFG_REFCLK_EXT_EN_MASK		(0U)
+#define PCIE_LINK_CFG_PHY_REFCLK_SEL_MASK		(0U)
+
 #endif		/* PCIE_AXON_H */
diff --git a/drivers/pci/controller/dwc/telechips/pcie-dolphin.h b/drivers/pci/controller/dwc/telechips/pcie-dolphin.h
index 70aac3524fba..313243c65d1d 100644
--- a/drivers/pci/controller/dwc/telechips/pcie-dolphin.h
+++ b/drivers/pci/controller/dwc/telechips/pcie-dolphin.h
@@ -24,11 +24,10 @@
 #define PCIE_LINK_CFG33			(0x084U)
 #define PCIE_LINK_CFG44			(0x0B0U)
 #define PCIE_LINK_CFG57			(0x0E4U)
+#define PCIE_LINK_CFG85			(0x154U)
 #define PCIE_LINK_CFG88			(0x160U)
 #define PCIE_LINK_CFG89			(0x164U)
 #define PCIE_LINK_CFG91			(0x16CU)
-#define PCIE_LINK_CFG92			(0x170U)
-#define PCIE_LINK_CFG93			(0x174U)
 
 #define PCIE_VDM_CTRL		(PCIE_LINK_CFG00)
 #define PCIE_VDM_CODE		(PCIE_LINK_CFG01)
@@ -45,10 +44,9 @@
 #define PCIE_SMLH_LINK_STS		(PCIE_LINK_CFG33)
 #define PCIE_PHY_RST_CTRL		(PCIE_LINK_CFG44)
 #define PCIE_SYS_INT		(PCIE_LINK_CFG57)
+#define PCIE_PM_CTRL		(PCIE_LINK_CFG85)
 #define PCIE_DBI_INDIRECT_ADDR_MASK		(PCIE_LINK_CFG88)
 #define PCIE_APP_CTRL		(PCIE_LINK_CFG89)
-#define PCIE_EDMA_INTR_STS		(PCIE_LINK_CFG92)
-#define PCIE_EDMA_INTR_EN		(PCIE_LINK_CFG93)
 
 #if defined(CONFIG_ARCH_TCC803X)
 #define PCIE_LINK_INTR		(PCIE_LINK_CFG04)
@@ -88,7 +86,9 @@
 #define PCIE_LINK_CFG_RDLH_LINK_UP_MASK		((u32)0x1U << PCIE_LINK_CFG_RDLH_LINK_UP_SHIFT)
 
 #define PCIE_LINK_CFG_SMLH_LINK_UP_SHIFT		(17U)
+#define PCIE_LINK_CFG_APPS_PM_XMT_TURNOFF_SHIFT		(1U)
 #define PCIE_LINK_CFG_SMLH_LINK_UP_MASK		((u32)0x1U << PCIE_LINK_CFG_SMLH_LINK_UP_SHIFT)
+#define PCIE_LINK_CFG_APPS_PM_XMT_TURNOFF_MASK		((u32)0x1U << PCIE_LINK_CFG_APPS_PM_XMT_TURNOFF_SHIFT)
 
 #define PCIE_LINK_CFG_LTSSM_ENABLE_SHIFT		(2U)
 #define PCIE_LINK_CFG_LTSSM_ENABLE_MASK		((u32)0x1U << PCIE_LINK_CFG_LTSSM_ENABLE_SHIFT)
@@ -96,16 +96,21 @@
 #define PCIE_LINK_CFG_DEVICE_TYPE_SHIFT		(6U)
 #define PCIE_LINK_CFG_DEVICE_TYPE_MASK		((u32)0xFU << PCIE_LINK_CFG_DEVICE_TYPE_SHIFT)
 
-#define PCIE_LINK_CFG_INDIRECT_ADDR_MASK		(0x200000U)
+#define PCIE_LINK_CFG_PM_LINKST_IN_L2_SHIFT		(12U)
+#define PCIE_LINK_CFG_PM_LINKST_IN_L2_MASK		((u32)0x1U << PCIE_LINK_CFG_PM_LINKST_IN_L2_SHIFT)
+
+#define PCIE_LINK_CFG_DBI_INDIRECT_ADDR_MASK		(0x200000U)
 
 #if defined(CONFIG_ARCH_TCC803X)
+#define PCIE_LINK_CFG_SLV_INDIRECT_ADDR_MASK		(0x00FFFFFFU)
 #define PCIE_LINK_CFG_MSI_INT_MASK		(0xBF3F00A0U)
 #define PCIE_LINK_CFG_INTX_MASK		(0x4040FFDEU)
 #define PCIE_LINK_CFG_INTR_EN_MASK		(0xFFFFD080U)
 #else
+#define PCIE_LINK_CFG_SLV_INDIRECT_ADDR_MASK		(0x3FFFFFFFU)
 #define PCIE_LINK_CFG_MSI_INT_MASK		(0x60040021U)
-#define PCIE_LINK_CFG_INTX_MASK		(0x0FF8FF82U)
-#define PCIE_LINK_CFG_INTR_EN_MASK		(0xFFFFFFFDU)
+#define PCIE_LINK_CFG_INTX_MASK		(0x0FF8FF86U)
+#define PCIE_LINK_CFG_INTR_EN_MASK		(0xF00FFFFDU)
 #endif
 
 #define PCIE_LINK_CFG_SYS_INT_SHIFT		(0U)
@@ -119,15 +124,4 @@
 #define PCIE_LINK_CFG_POWER_UP_RST_SHIFT		(6U)
 #define PCIE_LINK_CFG_POWER_UP_RST_MASK		((u32)0x1U << PCIE_LINK_CFG_POWER_UP_RST_SHIFT)
 
-#define PCIE_LINK_CFG_EDMA_RD_INTR_STS_SHIFT		(4U)
-#define PCIE_LINK_CFG_EDMA_WR_INTR_STS_SHIFT		(0U)
-#define PCIE_LINK_CFG_EDMA_RD_INTR_STS_MASK		((u32)0xFU << PCIE_LINK_CFG_EDMA_RD_INTR_STS_SHIFT)
-#define PCIE_LINK_CFG_EDMA_WR_INTR_STS_MASK		((u32)0xFU << PCIE_LINK_CFG_EDMA_WR_INTR_STS_SHIFT)
-#define PCIE_LINK_CFG_EDMA_INTR_STS_MASK		(PCIE_LINK_CFG_EDMA_RD_INTR_STS_MASK | PCIE_LINK_CFG_EDMA_WR_INTR_STS_MASK)
-
-#define PCIE_LINK_CFG_EDMA_RD_INTR_EN_SHIFT		(4U)
-#define PCIE_LINK_CFG_EDMA_WR_INTR_EN_SHIFT		(0U)
-#define PCIE_LINK_CFG_EDMA_RD_INTR_EN_MASK		((u32)0xFU << PCIE_LINK_CFG_EDMA_RD_INTR_EN_SHIFT)
-#define PCIE_LINK_CFG_EDMA_WR_INTR_EN_MASK		((u32)0xFU << PCIE_LINK_CFG_EDMA_WR_INTR_EN_SHIFT)
-#define PCIE_LINK_CFG_EDMA_INTR_EN_MASK		(PCIE_LINK_CFG_EDMA_RD_INTR_EN_MASK | PCIE_LINK_CFG_EDMA_WR_INTR_EN_MASK)
 #endif		/* PCIE_DOLPHIN_H */
diff --git a/drivers/pci/controller/dwc/telechips/pcie-tcc.c b/drivers/pci/controller/dwc/telechips/pcie-tcc.c
index a1d25a1061bd..8a0984253a30 100644
--- a/drivers/pci/controller/dwc/telechips/pcie-tcc.c
+++ b/drivers/pci/controller/dwc/telechips/pcie-tcc.c
@@ -19,8 +19,8 @@
 #include <linux/phy/phy.h>
 #include <linux/debugfs.h>
 #include <linux/irqdomain.h>
+#include <linux/iopoll.h>
 #include <linux/pinctrl/consumer.h>
-#include <soc/telechips/dw_edma.h>
 
 #include "../pcie-designware.h"
 
@@ -32,17 +32,6 @@
 
 #define to_tcc_pcie(x)	dev_get_drvdata((x)->dev)
 
-#define to_phy_submode(r, m) \
-	(((u32)(r) << 4) | (u32)(m))
-#define phy_set_clk(phy, mode) \
-	phy_set_mode_ext(phy, PHY_MODE_PCIE, mode)
-
-#define to_atu_outb_reg(index, offset) \
-	(u32)(PCIE_GET_ATU_OUTB_UNR_REG_OFFSET((index)) + (u32)(offset))
-
-#define to_atu_inb_reg(index, offset) \
-	(u32)(PCIE_GET_ATU_INB_UNR_REG_OFFSET((index)) + (u32)(offset))
-
 /*
  * PCIe controller wrapper DBI configuration registers
  */
@@ -59,46 +48,7 @@
 #define DBI_PCIE_PORT_LOGIC_PIPE_LOOPBACK_CONTROL		(DBI_PCIE_PORT_LOGIC_OFFSET + 0x1B8U)
 #define DBI_PCIE_PORT_LOGIC_MISC_CONTROL1		(DBI_PCIE_PORT_LOGIC_OFFSET + 0x1BCU)
 #define DBI_PCIE_PORT_LOGIC_AMBA_ORDERING_CTRL		(DBI_PCIE_PORT_LOGIC_OFFSET + 0x1D8U)
-#define DBI_PCIE_PORT_LOGIC_GEN4_LANE_MARGINING2		(DBI_PCIE_PORT_LOGIC_OFFSET + 0x480U)
-
-#define DBI_PCIE_DMA_CAP_OFFSET		(0x380000U)
-#define DBI_PCIE_DMA_CTRL_DATA_ARB_PRIOR		(DBI_PCIE_DMA_CAP_OFFSET + 0x000U)
-#define DBI_PCIE_DMA_WRITE_ENGINE_EN		(DBI_PCIE_DMA_CAP_OFFSET + 0x00CU)
-#define DBI_PCIE_DMA_WRITE_DOORBELL		(DBI_PCIE_DMA_CAP_OFFSET + 0x010U)
-#define DBI_PCIE_DMA_WRITE_CHANNEL_ARB_WEIGHT_LOW		(DBI_PCIE_DMA_CAP_OFFSET + 0x018U)
-#define DBI_PCIE_DMA_WRITE_INT_STATUS                (DBI_PCIE_DMA_CAP_OFFSET + 0x04CU)
-#define DBI_PCIE_DMA_WRITE_INT_MASK		(DBI_PCIE_DMA_CAP_OFFSET + 0x054U)
-#define DBI_PCIE_DMA_WRITE_INT_CLEAR		(DBI_PCIE_DMA_CAP_OFFSET + 0x58U)
-#define DBI_PCIE_DMA_WRITE_DONE_IMWR_LOW		(DBI_PCIE_DMA_CAP_OFFSET + 0x060U)
-#define DBI_PCIE_DMA_WRITE_DONE_IMWR_HIGH		(DBI_PCIE_DMA_CAP_OFFSET + 0x064U)
-#define DBI_PCIE_DMA_WRITE_ABORT_IMWR_LOW		(DBI_PCIE_DMA_CAP_OFFSET + 0x068U)
-#define DBI_PCIE_DMA_WRITE_ABORT_IMWR_HIGH		(DBI_PCIE_DMA_CAP_OFFSET + 0x06CU)
-#define DBI_PCIE_DMA_WRITE_CH01_IMWR_DATA		(DBI_PCIE_DMA_CAP_OFFSET + 0x070U)
-#define DBI_PCIE_DMA_WRITE_LINKED_LIST_ERR_EN		(DBI_PCIE_DMA_CAP_OFFSET + 0x090U)
-#define DBI_PCIE_DMA_WR_CH_CONTROL1		(DBI_PCIE_DMA_CAP_OFFSET + 0x200U)
-#define DBI_PCIE_DMA_WR_TRANSFER_SIZE		(DBI_PCIE_DMA_CAP_OFFSET + 0x208U)
-#define DBI_PCIE_DMA_WR_SAR_LOW		(DBI_PCIE_DMA_CAP_OFFSET + 0x20CU)
-#define DBI_PCIE_DMA_WR_SAR_HIGH		(DBI_PCIE_DMA_CAP_OFFSET + 0x210U)
-#define DBI_PCIE_DMA_WR_DAR_LOW		(DBI_PCIE_DMA_CAP_OFFSET + 0x214U)
-#define DBI_PCIE_DMA_WR_DAR_HIGH		(DBI_PCIE_DMA_CAP_OFFSET + 0x218U)
-
-#define DBI_PCIE_DMA_READ_ENGINE_EN		(DBI_PCIE_DMA_CAP_OFFSET + 0x02CU)
-#define DBI_PCIE_DMA_READ_DOORBELL		(DBI_PCIE_DMA_CAP_OFFSET + 0x030U)
-#define DBI_PCIE_DMA_READ_CHANNEL_ARB_WEIGHT_LOW		(DBI_PCIE_DMA_CAP_OFFSET + 0x038U)
-#define DBI_PCIE_DMA_READ_INT_STATUS                  (DBI_PCIE_DMA_CAP_OFFSET + 0x0A0U)
-#define DBI_PCIE_DMA_READ_INT_MASK		(DBI_PCIE_DMA_CAP_OFFSET + 0x0A8U)
-#define DBI_PCIE_DMA_READ_INT_CLEAR		(DBI_PCIE_DMA_CAP_OFFSET + 0x0ACU)
-#define DBI_PCIE_DMA_READ_LINKED_LIST_ERR_EN		(DBI_PCIE_DMA_CAP_OFFSET + 0x0C4U)
-#define DBI_PCIE_DMA_READ_DONE_IMWR_LOW		(DBI_PCIE_DMA_CAP_OFFSET + 0x0CCU)
-#define DBI_PCIE_DMA_READ_DONE_IMWR_HIGH		(DBI_PCIE_DMA_CAP_OFFSET + 0x0D0U)
-#define DBI_PCIE_DMA_READ_ABORT_IMWR_LOW		(DBI_PCIE_DMA_CAP_OFFSET + 0x0D4U)
-#define DBI_PCIE_DMA_READ_ABORT_IMWR_HIGH		(DBI_PCIE_DMA_CAP_OFFSET + 0x0D8U)
-#define DBI_PCIE_DMA_RD_CH_CONTROL1		(DBI_PCIE_DMA_CAP_OFFSET + 0x300U)
-#define DBI_PCIE_DMA_RD_TRANSFER_SIZE		(DBI_PCIE_DMA_CAP_OFFSET + 0x308U)
-#define DBI_PCIE_DMA_RD_SAR_LOW		(DBI_PCIE_DMA_CAP_OFFSET + 0x30CU)
-#define DBI_PCIE_DMA_RD_SAR_HIGH		(DBI_PCIE_DMA_CAP_OFFSET + 0x310U)
-#define DBI_PCIE_DMA_RD_DAR_LOW		(DBI_PCIE_DMA_CAP_OFFSET + 0x314U)
-#define DBI_PCIE_DMA_RD_DAR_HIGH		(DBI_PCIE_DMA_CAP_OFFSET + 0x318U)
+#define DBI_PCIE_PORT_LOGIC_GEN4_LANE_MARGINING1		(DBI_PCIE_PORT_LOGIC_OFFSET + 0x480U)
 
 /*
  * Mask/shift bits in DBI configuration registers
@@ -139,24 +89,11 @@
 #define PCIE_DBI_GEN4_RXMARGIN_MAX_TIMING_OFFSET_MASK		((u32)0x3FU << PCIE_DBI_GEN4_RXMARGIN_MAX_TIMING_OFFSET_SHIFT)
 #define PCIE_DBI_GEN4_RXMARGIN_NUM_TIMING_STEPS_MASK		((u32)0x3FU << PCIE_DBI_GEN4_RXMARGIN_NUM_TIMING_STEPS_SHIFT)
 
-#define PCIE_DBI_DMA_ABORT_INT_STATUS_SHIFT		(16U)
-#define PCIE_DBI_DMA_DONE_INT_STATUS_SHIFT		(0U)
-#define PCIE_DBI_DMA_ABORT_INT_STATUS_MASK		((u32)0xFU << PCIE_DBI_DMA_ABORT_INT_STATUS_SHIFT)
-#define PCIE_DBI_DMA_DONE_INT_STATUS_MASK		((u32)0xFU << PCIE_DBI_DMA_DONE_INT_STATUS_SHIFT)
-
-#define PCIE_DBI_DMA_CS_SHIFT		(0x5U)
-#define PCIE_DBI_DMA_CS_RUN_MASK		(0x1U << PCIE_DBI_DMA_CS_SHIFT)
-#define PCIE_DBI_DMA_CS_HALT_MASK		(0x2U << PCIE_DBI_DMA_CS_SHIFT)
-#define PCIE_DBI_DMA_CS_STOP_MASK		(0x3U << PCIE_DBI_DMA_CS_SHIFT)
-#define PCIE_DBI_DMA_CS_MASK		(PCIE_DBI_DMA_CS_RUN_MASK |  \
-	PCIE_DBI_DMA_CS_HALT_MASK | \
-	PCIE_DBI_DMA_CS_STOP_MASK)
-
 #define PCIE_LINK_UP_TIMEOUT		(120LL)
 #define PCIE_LINK_UP_DELAY		(1U)
+#define PME_ACK_TIMEOUT			(10000)
 #define PCIE_VDM_MSG_GRANT_TIMEOUT		(1000LL)
 #define PCIE_VDM_MSG_GRANT_DELAY		(1U)
-#define PCIE_REG_BACKUP_SIZE			(0x90U)
 
 enum tcc_pcie_gen {
 	PCIE_GEN1 = 1,
@@ -180,11 +117,6 @@ enum tcc_pcie_refclk_src {
 	REFCLK_SRC_PLL,
 };
 
-enum tcc_pcie_link_state {
-	PCIE_LINK_DOWN = 0,
-	PCIE_LINK_UP,
-};
-
 struct tcc_pcie_of_data {
 	enum tcc_pcie_variants variant;
 	enum dw_pcie_device_mode mode;
@@ -208,71 +140,22 @@ enum tcc_pcie_msg_route {
 	DP_PCIE_MSG_RESERVED2,
 };
 
-struct tcc_pcie_edma {
-	atomic_t cond;
-	s32 irq;
-};
-
 struct tcc_pcie {
 	struct dw_pcie		*pci;
 	void __iomem		*link_base;
-	void __iomem		*suspend_regs;
 	struct reset_control		*reset;
-	struct phy		*phy;
-	u32		refclk_type;
+	struct phy		*pcie_phy;
 	s32		irq;
 	u32		max_link_speed;
 
+	bool enable_ext_refclk;
 	enum tcc_pcie_variants		variant;
 	enum dw_pcie_device_mode		mode;
-	struct tcc_pcie_edma		*edma;
 #ifdef CONFIG_DEBUG_FS
 	enum tcc_pcie_loopback_mode		loopback_mode;
 #endif
 };
 
-static s32 tcc_pcie_backup_reg(const struct tcc_pcie *tp)
-{
-	s32 err = 0;
-
-	if (tp != NULL) {
-		const struct dw_pcie *pci = tp->pci;
-
-		if (tp->suspend_regs != NULL) {
-			(void)memcpy(tp->suspend_regs,
-					pci->dbi_base,
-					PCIE_REG_BACKUP_SIZE);
-		} else {
-			err = -ENOMEM;
-		}
-	} else {
-		err = -EINVAL;
-	}
-
-	return err;
-}
-
-static s32 tcc_pcie_restore_reg(const struct tcc_pcie *tp)
-{
-	s32 err = 0;
-
-	if (tp != NULL) {
-		const struct dw_pcie *pci = tp->pci;
-
-		if (tp->suspend_regs != NULL) {
-			(void)memcpy(pci->dbi_base,
-					tp->suspend_regs,
-					PCIE_REG_BACKUP_SIZE);
-		} else {
-			err = -ENOMEM;
-		}
-	} else {
-		err = -EINVAL;
-	}
-
-	return err;
-}
-
 static inline u32 tcc_pcie_readl(void __iomem *base, u32 offset)
 {
 	u32 ret = 0;
@@ -327,7 +210,6 @@ static int tcc_pcie_send_vdm(void *data, u64 msg)
 
 	if (data != NULL) {
 		const struct tcc_pcie *tp = (const struct tcc_pcie *)data;
-		const struct dw_pcie *pci = tp->pci;
 		u32 val, mask;
 
 		/* Clear VEN_MSG_REQ */
@@ -372,19 +254,17 @@ static int tcc_pcie_send_vdm(void *data, u64 msg)
 
 		/* wait for VEN_MSG_GRANT */
 		err = tcc_pcie_grant_is_raised(tp);
-		if (err != 0) {
-			dev_err(pci->dev, "Failed to write vdm(%d)\n", err);
-		}
-
-		/* Clear VEN_MSG_REQ */
-		val = ((u32)0x0U << PCIE_LINK_CFG_VEN_MSG_REQ_SHIFT);
-		mask = PCIE_LINK_CFG_VEN_MSG_REQ_MASK;
-		tcc_pcie_writel(tp->link_base, PCIE_VDM_CTRL, val, mask);
+		if (err == 0) {
+			/* Clear VEN_MSG_REQ */
+			val = ((u32)0x0U << PCIE_LINK_CFG_VEN_MSG_REQ_SHIFT);
+			mask = PCIE_LINK_CFG_VEN_MSG_REQ_MASK;
+			tcc_pcie_writel(tp->link_base, PCIE_VDM_CTRL, val, mask);
 
-		/* Clear VEN_MSG_GRANT */
-		val = ((u32)0x1U << PCIE_LINK_CFG_VEN_MSG_GRANT_SHIFT);
-		mask = PCIE_LINK_CFG_VEN_MSG_GRANT_MASK;
-		tcc_pcie_writel(tp->link_base, PCIE_VDM_CTRL, val, mask);
+			/* Clear VEN_MSG_GRANT */
+			val = ((u32)0x1U << PCIE_LINK_CFG_VEN_MSG_GRANT_SHIFT);
+			mask = PCIE_LINK_CFG_VEN_MSG_GRANT_MASK;
+			tcc_pcie_writel(tp->link_base, PCIE_VDM_CTRL, val, mask);
+		}
 	} else {
 		err = -EINVAL;
 	}
@@ -514,8 +394,7 @@ static int tcc_pcie_loopback_get_mode(void *data, u64 *val)
 	s32 err = 0;
 
 	if ((data != NULL) && (val != NULL)) {
-		const struct tcc_pcie *tp =
-			(const struct tcc_pcie *)data;
+		const struct tcc_pcie *tp = (const struct tcc_pcie *)data;
 
 		*val = (u64)tp->loopback_mode;
 	} else {
@@ -682,47 +561,7 @@ static s32 tcc_pcie_reset_control(const struct tcc_pcie *tp)
 	return err;
 }
 
-static s32 tcc_pcie_disable_irq(const struct tcc_pcie *tp)
-{
-	s32 err = 0;
-
-	if (tp != NULL) {
-		const struct dw_pcie *pci = tp->pci;
-		const struct pcie_port *pp = &pci->pp;
-		s32 irq;
-
-		irq = (tp->mode == DW_PCIE_RC_TYPE) ? pp->irq : tp->irq;
-		if (irq >= 0) {
-			disable_irq((u32)irq);
-		}
-	} else {
-		err = -EINVAL;
-	}
-
-	return err;
-}
-
-static s32 tcc_pcie_enable_irq(const struct tcc_pcie *tp)
-{
-	s32 err = 0;
-
-	if (tp != NULL) {
-		const struct dw_pcie *pci = tp->pci;
-		const struct pcie_port *pp = &pci->pp;
-		s32 irq;
-
-		irq = (tp->mode == DW_PCIE_RC_TYPE) ? pp->irq : tp->irq;
-		if (irq > 0) {
-			enable_irq((u32)irq);
-		}
-	} else {
-		err = -EINVAL;
-	}
-
-	return err;
-}
-
-static s32 tcc_pcie_gen4_init(const struct tcc_pcie *tp)
+static s32 tcc_pcie_set_gen4_margining_5_9_5(const struct tcc_pcie *tp)
 {
 	s32 err = 0;
 
@@ -744,12 +583,12 @@ static s32 tcc_pcie_gen4_init(const struct tcc_pcie *tp)
 				PCIE_DBI_GEN4_RXMARGIN_NUM_VOLTAGE_STEPS_MASK |
 				PCIE_DBI_GEN4_RXMARGIN_MAX_TIMING_OFFSET_MASK |
 				PCIE_DBI_GEN4_RXMARGIN_NUM_TIMING_STEPS_MASK;
-			val = dw_pcie_readl_dbi(pci, DBI_PCIE_PORT_LOGIC_GEN4_LANE_MARGINING2) & ~mask;
+			val = dw_pcie_readl_dbi(pci, DBI_PCIE_PORT_LOGIC_GEN4_LANE_MARGINING1) & ~mask;
 			val |= ((u32)0x1FU << PCIE_DBI_GEN4_RXMARGIN_NUM_TIMING_STEPS_SHIFT);
 			val |= ((u32)0x32U << PCIE_DBI_GEN4_RXMARGIN_MAX_TIMING_OFFSET_SHIFT);
 			val |= ((u32)0x7FU << PCIE_DBI_GEN4_RXMARGIN_NUM_VOLTAGE_STEPS_SHIFT);
 			val |= ((u32)0x28U << PCIE_DBI_GEN4_RXMARGIN_MAX_VOLTAGE_OFFSET_SHIFT);
-			dw_pcie_writel_dbi(pci, DBI_PCIE_PORT_LOGIC_GEN4_LANE_MARGINING2, val);
+			dw_pcie_writel_dbi(pci, DBI_PCIE_PORT_LOGIC_GEN4_LANE_MARGINING1, val);
 		}
 
 		mask = PCIE_DBI_AX_MSTR_ORDR_P_EVENT_SEL_MASK;
@@ -770,11 +609,11 @@ static s32 tcc_pcie_set_dev_type(const struct tcc_pcie *tp)
 	s32 err = 0;
 
 	if (tp != NULL) {
-		u32 val;
+		u32 val, mask;
 
 		switch (tp->mode) {
 		case DW_PCIE_RC_TYPE:
-			val = ((u32)0x4U  << PCIE_LINK_CFG_DEVICE_TYPE_SHIFT);
+			val = ((u32)0x4U << PCIE_LINK_CFG_DEVICE_TYPE_SHIFT);
 			break;
 		case DW_PCIE_EP_TYPE:
 			val = 0x0U;
@@ -787,8 +626,6 @@ static s32 tcc_pcie_set_dev_type(const struct tcc_pcie *tp)
 		}
 
 		if (err == 0) {
-			u32 mask;
-
 			mask = PCIE_LINK_CFG_DEVICE_TYPE_MASK;
 			tcc_pcie_writel(tp->link_base, PCIE_APP_CTRL,
 					val, mask);
@@ -805,7 +642,7 @@ static s32 tcc_pcie_set_max_link_speed(struct dw_pcie *pci)
 	s32 err = 0;
 
 	if (pci != NULL) {
-		struct tcc_pcie *tp = (struct tcc_pcie *)to_tcc_pcie(pci);
+		struct tcc_pcie *tp = to_tcc_pcie(pci);
 		u32 val, mask;
 
 		dw_pcie_dbi_ro_wr_en(pci);
@@ -826,21 +663,21 @@ static s32 tcc_pcie_set_layer(struct dw_pcie *pci)
 	s32 err = 0;
 
 	if (pci != NULL) {
-		struct tcc_pcie *tp = (struct tcc_pcie *)to_tcc_pcie(pci);
+		const struct tcc_pcie *tp = to_tcc_pcie(pci);
 		u32 val, mask;
 
 		dw_pcie_dbi_ro_wr_en(pci);
 		if (tp->max_link_speed >= (u32)PCIE_GEN2) {
 			mask = PCIE_DBI_DIRECT_SPEED_CHANGE_MASK;
 			val = dw_pcie_readl_dbi(pci, DBI_PCIE_PORT_LOGIC_GEN2_CTRL) & ~mask;
-			val |= ((u32)0x1U << PCIE_DBI_DIRECT_SPEED_CHANGE_SHIFT);
+			val |= mask;
 			dw_pcie_writel_dbi(pci, DBI_PCIE_PORT_LOGIC_GEN2_CTRL, val);
 		}
 
 		if (tp->max_link_speed >= (u32)PCIE_GEN3) {
 			mask = PCIE_DBI_EQ_REDO_DISABLE_MASK;
 			val = dw_pcie_readl_dbi(pci, DBI_PCIE_PORT_LOGIC_GEN3_RELATED) & ~mask;
-			val |= ((u32)0x1U << PCIE_DBI_EQ_REDO_DISABLE_SHIFT);
+			val |= mask;
 			dw_pcie_writel_dbi(pci, DBI_PCIE_PORT_LOGIC_GEN3_RELATED, val);
 		}
 		dw_pcie_dbi_ro_wr_dis(pci);
@@ -856,10 +693,10 @@ static s32 tcc_pcie_init_phy(const struct tcc_pcie *tp)
 	s32 err = 0;
 
 	if (tp != NULL) {
-		err = phy_init(tp->phy);
+		err = phy_init(tp->pcie_phy);
 		if (err == 0) {
 			if (tp->variant == TCC807X) {
-				err = tcc_pcie_gen4_init(tp);
+				err = tcc_pcie_set_gen4_margining_5_9_5(tp);
 			}
 		}
 	} else {
@@ -902,8 +739,8 @@ static s32 tcc_pcie_enable_ltssm(const struct tcc_pcie *tp, bool enable)
 	if (tp != NULL) {
 		u32 val, mask;
 
-		val = enable ? ((u32)0x1U << PCIE_LINK_CFG_LTSSM_ENABLE_SHIFT) : 0x0U;
 		mask = PCIE_LINK_CFG_LTSSM_ENABLE_MASK;
+		val = enable ? mask : 0x0U;
 		tcc_pcie_writel(tp->link_base, PCIE_LTSSM_CTRL,
 				val, mask);
 	} else {
@@ -931,14 +768,14 @@ static s32 tcc_pcie_clear_cactive(const struct tcc_pcie *tp)
 	return err;
 }
 
-static s32 tcc_pcie_clear_interrupts(const struct tcc_pcie *tp)
+static s32 tcc_pcie_irq_clear(const struct tcc_pcie *tp, u32 status)
 {
 	s32 err = 0;
 
 	if (tp != NULL) {
 		u32 val, mask;
 
-		val = 0xFFFFFFFFU;
+		val = status;
 		mask = val;
 		tcc_pcie_writel(tp->link_base, PCIE_LINK_STS_CLR,
 				val, mask);
@@ -949,7 +786,7 @@ static s32 tcc_pcie_clear_interrupts(const struct tcc_pcie *tp)
 	return err;
 }
 
-static s32 tcc_pcie_enable_interrupt(const struct tcc_pcie *tp, bool enable)
+static s32 tcc_pcie_irq_enable(const struct tcc_pcie *tp, bool enable)
 {
 	s32 err = 0;
 
@@ -966,71 +803,40 @@ static s32 tcc_pcie_enable_interrupt(const struct tcc_pcie *tp, bool enable)
 	return err;
 }
 
-static s32 tcc_pcie_set_interrupt(const struct tcc_pcie *tp, bool enable)
-{
-	s32 err = 0;
-
-	if (tp != NULL) {
-		err = tcc_pcie_clear_interrupts(tp);
-		if (err == 0) {
-			err = tcc_pcie_enable_interrupt(tp, enable);
-		}
-
-		if (err == 0) {
-			if ((IS_ENABLED(CONFIG_PCI_MSI) != 0) &&
-					(tp->mode == DW_PCIE_RC_TYPE)) {
-				struct dw_pcie *pci = tp->pci;
-				struct pcie_port *pp = &pci->pp;
-
-				dw_pcie_msi_init(pp);
-			}
-		}
-	} else {
-		err = -EINVAL;
-	}
-
-	return err;
-}
-
 static void tcc_pcie_write_dbi(struct dw_pcie *pci, void __iomem *base,
 		u32 reg, size_t size, u32 val)
 {
 	if (pci != NULL) {
-		const struct tcc_pcie *tp = (const struct tcc_pcie *)to_tcc_pcie(pci);
-		u32 mask, write_val = val;
 		s32 sz;
 
-		if (tp->variant != TCC803X) {
-			mask = 0x3FFFFFFFU;
-			if ((reg == to_atu_outb_reg(0, PCIE_ATU_UNR_LOWER_BASE)) ||
-					(reg == to_atu_outb_reg(1, PCIE_ATU_UNR_LOWER_BASE)) ||
-					(reg == to_atu_outb_reg(2, PCIE_ATU_UNR_LOWER_BASE)) ||
-					(reg == to_atu_outb_reg(0, PCIE_ATU_UNR_LOWER_LIMIT)) ||
-					(reg == to_atu_outb_reg(1, PCIE_ATU_UNR_LOWER_LIMIT)) ||
-					(reg == to_atu_outb_reg(2, PCIE_ATU_UNR_LOWER_LIMIT))) {
-				write_val &= mask;
-			}
+		if (pci->iatu_unroll_enabled != 0U) {
+			u32 idx;
+
+			for (idx = 0; idx < pci->num_viewport; idx++) {
+				u32 offset = PCIE_GET_ATU_OUTB_UNR_REG_OFFSET(idx);
 
-			if ((reg == to_atu_outb_reg(0, PCIE_ATU_UNR_UPPER_BASE)) ||
-					(reg == to_atu_outb_reg(1, PCIE_ATU_UNR_UPPER_BASE)) ||
-					(reg == to_atu_outb_reg(2, PCIE_ATU_UNR_UPPER_BASE)) ||
-					(reg == to_atu_inb_reg(0, PCIE_ATU_UNR_UPPER_BASE)) ||
-					(reg == to_atu_inb_reg(1, PCIE_ATU_UNR_UPPER_BASE)) ||
-					(reg == to_atu_inb_reg(2, PCIE_ATU_UNR_UPPER_BASE))) {
-				write_val = 0x0U;
+				if ((reg == (u32)(offset + PCIE_ATU_UNR_LOWER_BASE)) ||
+						(reg == (u32)(offset + PCIE_ATU_UNR_LOWER_LIMIT))) {
+					val &= PCIE_LINK_CFG_SLV_INDIRECT_ADDR_MASK;
+				}
+
+				if (reg == (u32)(offset + PCIE_ATU_UNR_UPPER_BASE)) {
+					val = 0x0U;
+				}
 			}
 		} else {
-			mask = 0x00FFFFFFU;
-			if ((reg == (u32)PCIE_ATU_LOWER_BASE) ||
-					(reg == (u32)PCIE_ATU_LIMIT)) {
-				write_val &= mask;
+			if ((dw_pcie_readl_dbi(pci, PCIE_ATU_VIEWPORT) & PCIE_ATU_REGION_INBOUND) == 0x0U) {
+				if (base == pci->atu_base) {
+					if ((reg == (u32)PCIE_ATU_LOWER_BASE) ||
+							(reg == (u32)PCIE_ATU_LIMIT)) {
+						val &= PCIE_LINK_CFG_SLV_INDIRECT_ADDR_MASK;
+					}
+				}
 			}
 		}
 
 		if (!__builtin_add_overflow(size, 0, &sz)) {
-			if (dw_pcie_write(base + reg, sz, write_val) != 0) {
-				dev_err(pci->dev, "failed to write dbi register(0x%08x)\n", reg);
-			}
+			(void)dw_pcie_write(base + reg, sz, val);
 		}
 	}
 }
@@ -1042,13 +848,13 @@ static void tcc_pcie_set_dbi2_mode(const struct tcc_pcie *tp, bool enable)
 			u32 val, mask;
 
 			if (enable) {
-				val = PCIE_LINK_CFG_INDIRECT_ADDR_MASK;
+				val = PCIE_LINK_CFG_DBI_INDIRECT_ADDR_MASK;
 				mask = 0x0U;
 				tcc_pcie_writel(tp->link_base, PCIE_DBI_INDIRECT_ADDR_MASK,
 						val, mask);
 			} else {
 				val = 0x0U;
-				mask = PCIE_LINK_CFG_INDIRECT_ADDR_MASK;
+				mask = PCIE_LINK_CFG_DBI_INDIRECT_ADDR_MASK;
 				tcc_pcie_writel(tp->link_base, PCIE_DBI_INDIRECT_ADDR_MASK,
 						val, mask);
 			}
@@ -1060,30 +866,25 @@ static void tcc_pcie_write_dbi2(struct dw_pcie *pci, void __iomem *base, u32 reg
 		size_t size, u32 val)
 {
 	if (pci != NULL) {
-		const struct tcc_pcie *tp = (const struct tcc_pcie *)to_tcc_pcie(pci);
-		s32 sz, err;
+		const struct tcc_pcie *tp = to_tcc_pcie(pci);
+		s32 sz;
 
-		err = 0;
 		if (!__builtin_add_overflow(size, 0, &sz)) {
 			if ((tp->variant == TCC805X) || (tp->variant == TCC807X)) {
-				reg |= PCIE_LINK_CFG_INDIRECT_ADDR_MASK;
+				reg |= PCIE_LINK_CFG_DBI_INDIRECT_ADDR_MASK;
 				tcc_pcie_set_dbi2_mode(tp, true);
-				err = dw_pcie_write(base + reg, sz, val);
+				(void)dw_pcie_write(base + reg, sz, val);
 				tcc_pcie_set_dbi2_mode(tp, false);
 			} else {
-				err = dw_pcie_write(base + reg, sz, val);
+				(void)dw_pcie_write(base + reg, sz, val);
 			}
 		}
-
-		if (err != 0) {
-			dev_err(pci->dev, "failed to write dbi2 register(%d)\n", err);
-		}
 	}
 }
 
 static s32 tcc_pcie_link_up(struct dw_pcie *pci)
 {
-	s32 err = (s32)PCIE_LINK_DOWN;
+	s32 err = 0;
 
 	if (pci != NULL) {
 		const struct tcc_pcie *tp = (const struct tcc_pcie *)to_tcc_pcie(pci);
@@ -1093,7 +894,7 @@ static s32 tcc_pcie_link_up(struct dw_pcie *pci)
 		if ((val & PCIE_LINK_CFG_RDLH_LINK_UP_MASK) != 0x0U) {
 			val = tcc_pcie_readl(tp->link_base, PCIE_SMLH_LINK_STS);
 			if ((val & PCIE_LINK_CFG_SMLH_LINK_UP_MASK) != 0x0U) {
-				err = (s32)PCIE_LINK_UP;
+				err = 1;
 			}
 		}
 	}
@@ -1101,7 +902,7 @@ static s32 tcc_pcie_link_up(struct dw_pcie *pci)
 	return err;
 }
 
-static s32 tcc_pcie_check_link_up(const struct tcc_pcie *tp)
+static s32 tcc_pcie_wait_l0(const struct tcc_pcie *tp)
 {
 	s32 err = 0;
 
@@ -1111,7 +912,7 @@ static s32 tcc_pcie_check_link_up(const struct tcc_pcie *tp)
 
 		ktime_get_ts64(&start);
 		do {
-			err = tcc_pcie_link_up(pci);
+			err = dw_pcie_link_up(pci);
 			ktime_get_ts64(&end);
 			gap = timespec64_sub(end, start);
 			if ((timespec64_to_ns(&gap)/NSEC_PER_MSEC) > PCIE_LINK_UP_TIMEOUT) {
@@ -1120,7 +921,7 @@ static s32 tcc_pcie_check_link_up(const struct tcc_pcie *tp)
 			} else {
 				mdelay(PCIE_LINK_UP_DELAY);
 			}
-		} while (err != (s32)PCIE_LINK_UP);
+		} while (err == 0);
 	} else {
 		err = -EINVAL;
 	}
@@ -1133,11 +934,11 @@ static s32 tcc_pcie_establish_link(struct dw_pcie *pci)
 	s32 err = 0;
 
 	if (pci != NULL) {
-		const struct tcc_pcie *tp = (const struct tcc_pcie *)to_tcc_pcie(pci);
+		const struct tcc_pcie *tp = to_tcc_pcie(pci);
 
 		err = tcc_pcie_enable_ltssm(tp, true);
 		if (err == 0) {
-			if (tcc_pcie_check_link_up(tp) == (s32)PCIE_LINK_UP) {
+			if (tcc_pcie_wait_l0(tp) > 0) {
 				dev_err(pci->dev, "Link up\n");
 			} else {
 				dev_err(pci->dev, "timout waiting for link_up\n");
@@ -1145,7 +946,7 @@ static s32 tcc_pcie_establish_link(struct dw_pcie *pci)
 		}
 
 		if (err == 0) {
-			err = tcc_pcie_set_interrupt(tp, true);
+			err = tcc_pcie_irq_enable(tp, true);
 		}
 	} else {
 		err = -EINVAL;
@@ -1154,14 +955,12 @@ static s32 tcc_pcie_establish_link(struct dw_pcie *pci)
 	return err;
 }
 
-static void	tcc_pcie_stop_link(struct dw_pcie *pci)
+static void tcc_pcie_stop_link(struct dw_pcie *pci)
 {
 	if (pci != NULL) {
-		const struct tcc_pcie *tp = (const struct tcc_pcie *)to_tcc_pcie(pci);
+		const struct tcc_pcie *tp = to_tcc_pcie(pci);
 
-		if (tcc_pcie_enable_ltssm(tp, false) != 0) {
-			dev_err(pci->dev, "failed to disable ltssm\n");
-		}
+		(void)tcc_pcie_enable_ltssm(tp, false);
 	}
 }
 
@@ -1186,6 +985,10 @@ static s32 tcc_pcie_host_init(struct pcie_port *pp)
 		if (err == 0) {
 			err = tcc_pcie_establish_link(pci);
 		}
+
+		if (err == 0) {
+			dw_pcie_msi_init(&pci->pp);
+		}
 	} else {
 		err = -EINVAL;
 	}
@@ -1255,8 +1058,8 @@ static irqreturn_t tcc_pcie_irq_handler(s32 irq, void *arg)
 		u32 val, mask;
 
 		mask = PCIE_LINK_CFG_INTX_MASK | PCIE_LINK_CFG_MSI_INT_MASK;
-		val = tcc_pcie_readl(tp->link_base, PCIE_LINK_STS) & mask;
-		if (val != 0x0U) {
+		val = tcc_pcie_readl(tp->link_base, PCIE_LINK_STS);
+		if ((val & mask) != 0x0U) {
 			if (IS_ENABLED(CONFIG_PCI_MSI) != 0) {
 				if ((tp->mode == DW_PCIE_RC_TYPE) &&
 						((val & PCIE_LINK_CFG_MSI_INT_MASK) != 0x0U)) {
@@ -1299,7 +1102,7 @@ static s32 tcc_pcie_prepare_rc(struct tcc_pcie *tp, struct platform_device *pdev
 	return err;
 }
 
-static s32 tcc_pcie_deassert_pwrup_reset(const struct tcc_pcie *tp)
+static s32 tcc_pcie_assert_pwrup_reset(const struct tcc_pcie *tp, bool reset)
 {
 	s32 err = 0;
 
@@ -1307,9 +1110,11 @@ static s32 tcc_pcie_deassert_pwrup_reset(const struct tcc_pcie *tp)
 		if (tp->variant != TCC803X) {
 			u32 val, mask;
 
-			/* deassert power up reset */
 			mask = PCIE_LINK_CFG_POWER_UP_RST_MASK;
-			val = mask;
+			val = tcc_pcie_readl(tp->link_base, PCIE_PHY_RST_CTRL) & ~mask;
+			if (!reset) {
+				val |= mask;
+			}
 			tcc_pcie_writel(tp->link_base, PCIE_PHY_RST_CTRL, val, mask);
 		}
 	} else {
@@ -1324,23 +1129,27 @@ static s32 tcc_pcie_wake_up_phy(const struct tcc_pcie *tp)
 	s32 err = 0;
 
 	if (tp != NULL) {
-		const struct dw_pcie *pci = (const struct dw_pcie *)tp->pci;
+		struct dw_pcie *pci = tp->pci;
+
+		err = tcc_pcie_assert_pwrup_reset(tp, true);
+		if (err == 0) {
+			err = phy_power_on(tp->pcie_phy);
+		}
 
-		err = phy_power_on(tp->phy);
 		if (err == 0) {
 			err = pinctrl_pm_select_idle_state(pci->dev);
 		}
 
 		if (err == 0) {
-			err = tcc_pcie_clear_interrupts(tp);
+			err = tcc_pcie_irq_clear(tp, 0xFFFFFFFFU);
 		}
 
 		if (err == 0) {
-			err = tcc_pcie_deassert_pwrup_reset(tp);
+			err = tcc_pcie_assert_pwrup_reset(tp, false);
 		}
 
 		if (err == 0) {
-			err = phy_reset(tp->phy);
+			err = phy_reset(tp->pcie_phy);
 		}
 	} else {
 		err = -EINVAL;
@@ -1358,8 +1167,8 @@ static s32 tcc_pcie_set_clksrc(const struct tcc_pcie *tp)
 			u32 val, mask;
 
 			mask = PCIE_LINK_CFG_REFCLK_EXT_EN_MASK | PCIE_LINK_CFG_PHY_REFCLK_SEL_MASK;
-			val = (tp->refclk_type == 0x0U) ? PCIE_LINK_CFG_REFCLK_EXT_EN_MASK : 0x0U;
-			val |= ((u32)((tp->refclk_type == 0x0U) ? REFCLK_SRC_PLL : REFCLK_SRC_IO)
+			val = tp->enable_ext_refclk ? 0x0U : PCIE_LINK_CFG_REFCLK_EXT_EN_MASK;
+			val |= ((u32)(tp->enable_ext_refclk ? REFCLK_SRC_IO : REFCLK_SRC_PLL)
 					<< PCIE_LINK_CFG_PHY_REFCLK_SEL_SHIFT);
 			tcc_pcie_writel(tp->link_base, PCIE_PHY_CTRL, val, mask);
 		}
@@ -1377,11 +1186,7 @@ static s32 tcc_pcie_set_clk(const struct tcc_pcie *tp)
 	if (tp != NULL) {
 		err = tcc_pcie_set_clksrc(tp);
 		if (err == 0) {
-			s32 submode;
-
-			if (!__builtin_add_overflow(to_phy_submode(tp->refclk_type, tp->mode), 0, &submode)) {
-				err = phy_set_clk(tp->phy, submode);
-			}
+			err = phy_set_mode(tp->pcie_phy, PHY_MODE_PCIE);
 		}
 	} else {
 		err = -EINVAL;
@@ -1391,170 +1196,6 @@ static s32 tcc_pcie_set_clk(const struct tcc_pcie *tp)
 }
 
 #ifdef CONFIG_PCIE_DW_EP
-static irqreturn_t tcc_pcie_edma_irq_handler(s32 irq, void *arg)
-{
-	struct tcc_pcie *tp = (struct tcc_pcie *)arg;
-	irqreturn_t ret = (irqreturn_t)IRQ_NONE;
-
-	(void)irq;
-
-	if (tp != NULL) {
-		struct dw_pcie *pci = tp->pci;
-		u32 reg, val, status;
-
-		status = tcc_pcie_readl(tp->link_base, PCIE_EDMA_INTR_STS);
-		if((status & PCIE_LINK_CFG_EDMA_INTR_STS_MASK) != 0x0U) {
-			reg = ((status & PCIE_LINK_CFG_EDMA_WR_INTR_STS_MASK) != 0x0U) ?
-				DBI_PCIE_DMA_WRITE_INT_STATUS : DBI_PCIE_DMA_READ_INT_STATUS;
-
-			val = dw_pcie_readl_dbi(pci, reg);
-			reg = ((status & PCIE_LINK_CFG_EDMA_WR_INTR_STS_MASK) != 0x0U) ?
-				DBI_PCIE_DMA_WRITE_INT_CLEAR : DBI_PCIE_DMA_READ_INT_CLEAR;
-			if ((val & PCIE_DBI_DMA_DONE_INT_STATUS_MASK) != 0x0U) {
-				if (atomic_read(&tp->edma->cond) == 0) {
-					atomic_set(&tp->edma->cond, 1);
-				}
-			}
-			dw_pcie_writel_dbi(pci, reg, val);
-
-			ret = (irqreturn_t)IRQ_HANDLED;
-		}
-	}
-
-	return ret;
-}
-
-static s32 tcc_pcie_init_edma(struct tcc_pcie *tp, struct platform_device *pdev)
-{
-	s32 err = 0;
-
-	if ((tp != NULL) && (pdev != NULL)) {
-		if (tp->variant != TCC803X) {
-			tp->edma = devm_kzalloc(&pdev->dev,
-					sizeof(struct tcc_pcie_edma), GFP_KERNEL);
-			if (tp->edma != NULL) {
-				tp->edma->irq = platform_get_irq(pdev, 1);
-				if (tp->edma->irq > 0) {
-					err = devm_request_irq(&pdev->dev, (u32)tp->edma->irq,
-							tcc_pcie_edma_irq_handler, IRQF_SHARED,
-							"telechips-pcie-edma", tp);
-				}
-			}
-		}
-	} else {
-		err = -EINVAL;
-	}
-
-	return err;
-}
-
-static s32 dw_edma_async_transfer_program(struct dw_pcie *pci, enum edma_data_direction dir, u64 src, u64 dst, u32 size)
-{
-	s32 err = 0;
-
-	if (pci != NULL) {
-		struct tcc_pcie *tp = to_tcc_pcie(pci);
-		u32 reg, val, mask;
-
-		mask = PCIE_DBI_DMA_CS_MASK;
-		reg = (dir == EDMA_TO_PNR) ?
-			DBI_PCIE_DMA_WR_CH_CONTROL1 : DBI_PCIE_DMA_RD_CH_CONTROL1;
-		val = dw_pcie_readl_dbi(pci, reg) & mask;
-		if (val != PCIE_DBI_DMA_CS_RUN_MASK) {
-			mask = PCIE_LINK_CFG_EDMA_INTR_EN_MASK;
-			val = tcc_pcie_readl(tp->link_base, PCIE_EDMA_INTR_EN) & ~mask;
-			val |= (dir == EDMA_TO_PNR) ?
-				PCIE_LINK_CFG_EDMA_WR_INTR_EN_MASK : PCIE_LINK_CFG_EDMA_RD_INTR_EN_MASK;
-			tcc_pcie_writel(tp->link_base, PCIE_EDMA_INTR_EN, val, mask);
-
-			atomic_set(&tp->edma->cond, 0);
-			dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_CTRL_DATA_ARB_PRIOR, 0x608U);
-			if (dir == EDMA_TO_PNR) {
-				/* Write Channel0 */
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_WRITE_ENGINE_EN, 0x1U);
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_WRITE_CHANNEL_ARB_WEIGHT_LOW, 0x20C41U);
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_WRITE_INT_MASK, 0x0U);
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_WR_CH_CONTROL1, 0x04000018U);
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_WR_TRANSFER_SIZE, size);
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_WR_SAR_LOW, lower_32_bits(src));
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_WR_SAR_HIGH, upper_32_bits(src));
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_WR_DAR_LOW, lower_32_bits(dst));
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_WR_DAR_HIGH, upper_32_bits(dst));
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_WRITE_DOORBELL, 0x0U);
-			} else {
-				/* Read Channel0 */
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_READ_ENGINE_EN, 0x1U);
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_READ_CHANNEL_ARB_WEIGHT_LOW, 0x20C41U);
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_READ_INT_MASK, 0x0U);
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_RD_CH_CONTROL1, 0x04000018U);
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_RD_TRANSFER_SIZE, size);
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_RD_SAR_LOW, lower_32_bits(src));
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_RD_SAR_HIGH, upper_32_bits(src));
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_RD_DAR_LOW, lower_32_bits(dst));
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_RD_DAR_HIGH, upper_32_bits(dst));
-				dw_pcie_writel_dbi(pci, DBI_PCIE_DMA_READ_DOORBELL, 0x0U);
-			}
-		} else {
-			err = -EAGAIN;
-		}
-	} else {
-		err = -EINVAL;
-	}
-
-	return err;
-}
-
-static s32 dw_edma_async_transfer_complete(struct tcc_pcie *tp)
-{
-	s32 err = 0;
-
-	if (tp != NULL) {
-		struct timespec64 start, end, gap;
-
-		ktime_get_ts64(&start);
-		while(atomic_read(&tp->edma->cond) == 0) {
-			ktime_get_ts64(&end);
-			gap = timespec64_sub(end, start);
-			if ((timespec64_to_ns(&gap)/NSEC_PER_MSEC) > 1000LL) {
-				err = -EAGAIN;
-				break;
-			} else {
-				udelay(1);
-			}
-		}
-	} else {
-		err = -EINVAL;
-	}
-
-	return err;
-}
-
-s32 dw_edma_async_transfer(void *arg, enum edma_data_direction dir,
-		u64 src, u64 dst, u32 sz, bool pending)
-{
-	s32 err = 0;
-
-	if (arg != NULL) {
-		struct dw_pcie_ep *ep = epc_get_drvdata((struct pci_epc *)arg);
-		struct dw_pcie *pci = to_dw_pcie_from_ep(ep);
-		struct tcc_pcie *tp = to_tcc_pcie(pci);
-
-		if (valid_edma_direction(dir) != 0) {
-			err = dw_edma_async_transfer_program(pci, dir, src, dst, sz);
-			if ((err == 0) && pending) {
-				err = dw_edma_async_transfer_complete(tp);
-			}
-		} else {
-			err = -EINVAL;
-		}
-	} else {
-		err = -EINVAL;
-	}
-
-	return err;
-}
-EXPORT_SYMBOL_GPL(dw_edma_async_transfer);
-
 static s32 tcc_pcie_ep_reset_bar(struct dw_pcie *pci, s32 bar)
 {
 	s32 err = 0;
@@ -1603,7 +1244,7 @@ static const struct pci_epc_features tcc_pcie_epc_features = {
 	.align = SZ_64K,
 };
 
-static const struct pci_epc_features* tcc_pcie_get_features(struct dw_pcie_ep *ep)
+static const struct pci_epc_features *tcc_pcie_get_features(struct dw_pcie_ep *ep)
 {
 	(void)ep;
 
@@ -1989,8 +1630,8 @@ static s32 tcc_pcie_ep_raise_legacy_irq(struct dw_pcie_ep *ep, u8 func_no)
 	(void)func_no;
 
 	if (ep != NULL) {
-		struct dw_pcie *pci = to_dw_pcie_from_ep(ep);
-		const struct tcc_pcie *tp = (const struct tcc_pcie *)to_tcc_pcie(pci);
+		const struct dw_pcie *pci = to_dw_pcie_from_ep(ep);
+		const struct tcc_pcie *tp = to_tcc_pcie(pci);
 		u32 val, mask;
 
 		mask = PCIE_LINK_CFG_SYS_INT_MASK;
@@ -2172,8 +1813,7 @@ static s32 tcc_pcie_prepare_ep(struct tcc_pcie *tp, struct platform_device *pdev
 	if ((tp != NULL) &&
 			(pdev != NULL) &&
 			(ep != NULL)) {
-		const struct resource *res =
-			(const struct resource *)platform_get_resource_byname(pdev, IORESOURCE_MEM, "addr_space");
+		const struct resource *res = platform_get_resource_byname(pdev, IORESOURCE_MEM, "addr_space");
 
 		if (res != NULL) {
 			ep->phys_base = res->start;
@@ -2227,7 +1867,7 @@ static s32 tcc_pcie_add_pcie_ep(struct tcc_pcie *tp, struct platform_device *pde
 		}
 
 		if (err == 0) {
-			err = tcc_pcie_init_edma(tp, pdev);
+			dw_pcie_ep_init_notify(ep);
 		}
 	} else {
 		err = -EINVAL;
@@ -2237,7 +1877,7 @@ static s32 tcc_pcie_add_pcie_ep(struct tcc_pcie *tp, struct platform_device *pde
 }
 #endif
 
-static s32 tcc_pcie_add_pcie_port(struct tcc_pcie *tp, struct platform_device *pdev)
+static s32 tcc_pcie_add_pcie_rp(struct tcc_pcie *tp, struct platform_device *pdev)
 {
 	s32 err = 0;
 
@@ -2282,7 +1922,7 @@ static int tcc_pcie_add_port(struct tcc_pcie *tp,
 		if (err == 0) {
 			switch (tp->mode) {
 			case DW_PCIE_RC_TYPE:
-				err = tcc_pcie_add_pcie_port(tp, pdev);
+				err = tcc_pcie_add_pcie_rp(tp, pdev);
 				break;
 			case DW_PCIE_EP_TYPE:
 #ifdef CONFIG_PCIE_DW_EP
@@ -2438,23 +2078,14 @@ static s32 tcc_pcie_get_of_data(const struct platform_device *pdev,
 	s32 err = 0;
 
 	if ((pdev != NULL) && (tp != NULL)) {
-		const struct of_device_id *match = (const struct of_device_id *)of_match_device(
-				of_match_ptr(tcc_pcie_of_match),
-				&pdev->dev);
-
-		if (match != NULL) {
-			const struct tcc_pcie_of_data *data =
-				(const struct tcc_pcie_of_data *)match->data;
+		const struct tcc_pcie_of_data *data = of_device_get_match_data(&pdev->dev);
 
+		if (data != NULL) {
 			tp->mode = data->mode;
 			tp->variant = data->variant;
 		} else {
 			err = -ENODEV;
 		}
-
-		if (err != 0) {
-			dev_err(&pdev->dev, "failed to get of_data\n");
-		}
 	} else {
 		err = -EINVAL;
 	}
@@ -2468,15 +2099,10 @@ static s32 tcc_pcie_parse_phy(struct platform_device *pdev,
 	s32 err = 0;
 
 	if ((tp != NULL) && (pdev != NULL)) {
-		tp->phy = devm_phy_get(&pdev->dev, "pcie-phy");
-		if (IS_ERR(tp->phy)) {
-			if (tp->variant != TCN100X) {
-				dev_err(&pdev->dev, "Failed to get pcie phy. Check kernel configuration.\n");
-				err = (s32)PTR_ERR(tp->phy);
-			} else {
-				dev_warn(&pdev->dev, "Failed to get pcie phy. Check if intended\n");
-				tp->phy = NULL;
-			}
+		tp->pcie_phy = devm_phy_get(&pdev->dev, "pcie-phy");
+		if (IS_ERR(tp->pcie_phy)) {
+			dev_err(&pdev->dev, "Failed to get pcie phy. Please check kernel configuration.\n");
+			tp->pcie_phy = NULL;
 		}
 	} else {
 		err = -EINVAL;
@@ -2511,11 +2137,10 @@ static s32 tcc_pcie_parse_props(struct platform_device *pdev,
 
 	if ((tp != NULL) && (pdev != NULL)) {
 		err = of_property_read_u32(pdev->dev.of_node,
-				"refclk_type", &tp->refclk_type);
-
+				"max-link-speed", &tp->max_link_speed);
 		if (err == 0) {
-			err = of_property_read_u32(pdev->dev.of_node,
-					"max-link-speed", &tp->max_link_speed);
+			tp->enable_ext_refclk = of_property_read_bool(pdev->dev.of_node,
+					"telechips,enable-ext-refclk");
 		}
 	} else {
 		err = -EINVAL;
@@ -2560,14 +2185,6 @@ static struct tcc_pcie *tcc_pcie_alloc_drvdata(struct platform_device *pdev)
 		if (tp != NULL) {
 			tp->pci = devm_kzalloc(&pdev->dev,
 					sizeof(struct dw_pcie), GFP_KERNEL);
-			if (tp->pci != NULL) {
-				tp->suspend_regs = kzalloc(
-						PCIE_REG_BACKUP_SIZE, GFP_KERNEL);
-				if (IS_ERR_OR_NULL(tp->suspend_regs)) {
-					devm_kfree(&pdev->dev, tp->pci);
-				}
-			}
-
 			if (IS_ERR_OR_NULL(tp->pci)) {
 				devm_kfree(&pdev->dev, tp);
 			}
@@ -2612,23 +2229,60 @@ static int tcc_pcie_probe(struct platform_device *pdev)
 	return err;
 }
 
-static s32 tcc_pcie_remove(struct platform_device *pdev)
+static s32 tcc_pcie_wait_l2(const struct tcc_pcie *tp)
 {
 	s32 err = 0;
 
-	if (pdev != NULL) {
-		const struct tcc_pcie *tp =
-			(const struct tcc_pcie *)platform_get_drvdata(pdev);
-		const struct dw_pcie *pci = (const struct dw_pcie *)tp->pci;
+	if (tp != NULL) {
+		u32 val, mask;
 
-		err = tcc_pcie_disable_irq(tp);
-		if (err == 0) {
-			err = pinctrl_pm_select_idle_state(pci->dev);
+		mask = PCIE_LINK_CFG_PM_LINKST_IN_L2_MASK;
+		err = readl_poll_timeout_atomic(tp->link_base + PCIE_PM_CTRL, val,
+				val & mask, 1, PME_ACK_TIMEOUT);
+	} else {
+		err = -EINVAL;
+	}
+
+	return err;
+}
+
+static s32 tcc_pcie_send_pm_turnoff(const struct tcc_pcie *tp)
+{
+	s32 err = 0;
+
+	if (tp != NULL) {
+		u32 val, mask;
+
+		mask = PCIE_LINK_CFG_APPS_PM_XMT_TURNOFF_MASK;
+		val = tcc_pcie_readl(tp->link_base, PCIE_SMLH_LINK_STS);
+		val |= mask;
+		tcc_pcie_writel(tp->link_base, PCIE_SMLH_LINK_STS, val, mask);
+
+		if (tp->variant != TCC803X) {
+			err = tcc_pcie_wait_l2(tp);
+		} else {
+			udelay(PME_ACK_TIMEOUT);
 		}
+	} else {
+		err = -EINVAL;
+	}
 
-		if (err == 0) {
-			mdelay(1);
-			err = pinctrl_pm_select_default_state(pci->dev);
+	return err;
+}
+
+static s32 tcc_pcie_pme_turnoff(const struct tcc_pcie *tp)
+{
+	s32 err = 0;
+
+	if (tp != NULL) {
+		struct dw_pcie *pci = tp->pci;
+
+		if (dw_pcie_link_up(pci) != 0) {
+			if (tcc_pcie_send_pm_turnoff(tp) != 0) {
+				dev_info(pci->dev, "Link didn't go into L2 state...!\n");
+			}
+		} else {
+			dev_info(pci->dev, "PCIe Link is down...!\n");
 		}
 	} else {
 		err = -EINVAL;
@@ -2637,81 +2291,88 @@ static s32 tcc_pcie_remove(struct platform_device *pdev)
 	return err;
 }
 
-static int tcc_pcie_suspend(struct device *dev)
+
+static s32 tcc_pcie_set_mse(const struct tcc_pcie *tp, bool enable)
 {
 	s32 err = 0;
 
-	if (dev != NULL) {
-		const struct tcc_pcie *tp =
-			(const struct tcc_pcie *)dev_get_drvdata(dev);
+	if (tp != NULL) {
 		struct dw_pcie *pci = tp->pci;
 
 		if (tp->mode == DW_PCIE_RC_TYPE) {
 			u32 val;
 
 			val = dw_pcie_readl_dbi(pci, PCI_COMMAND);
-			val &= ~(u32)PCI_COMMAND_MEMORY;
+			if (enable) {
+				val |= (u32)PCI_COMMAND_MEMORY;
+			} else {
+				val &= ~(u32)PCI_COMMAND_MEMORY;
+			}
 			dw_pcie_writel_dbi(pci, PCI_COMMAND, val);
 		}
 	} else {
-		err = -ENODEV;
+		err = -EINVAL;
 	}
 
 	return err;
 }
 
-static int tcc_pcie_resume(struct device *dev)
+static s32 tcc_pcie_deinit_controller(const struct tcc_pcie *tp)
 {
 	s32 err = 0;
 
-	if (dev != NULL) {
-		const struct tcc_pcie *tp =
-			(const struct tcc_pcie *)dev_get_drvdata(dev);
-		struct dw_pcie *pci = tp->pci;
-
+	if (tp != NULL) {
 		if (tp->mode == DW_PCIE_RC_TYPE) {
-			u32 val;
+			struct dw_pcie *pci = tp->pci;
+			struct pcie_port *pp = &pci->pp;
 
-			val = dw_pcie_readl_dbi(pci, PCI_COMMAND);
-			val |= (u32)PCI_COMMAND_MEMORY;
-			dw_pcie_writel_dbi(pci, PCI_COMMAND, val);
+			if (pp->irq > 0) {
+				disable_irq((u32)pp->irq);
+			}
+
+			err = tcc_pcie_set_mse(tp, false);
+			if (err == 0) {
+				err = tcc_pcie_pme_turnoff(tp);
+			}
+
+			if (err == 0) {
+				err = phy_power_off(tp->pcie_phy);
+			}
+			if (err == 0) {
+				err = phy_exit(tp->pcie_phy);
+			}
 		}
 	} else {
-		err = -ENODEV;
+		err = -EINVAL;
 	}
 
 	return err;
 }
 
-static s32 tcc_pcie_reestablish_link(const struct tcc_pcie *tp)
+static s32 tcc_pcie_preinit_controller(const struct tcc_pcie *tp)
 {
 	s32 err = 0;
 
 	if (tp != NULL) {
-		struct dw_pcie *pci = tp->pci;
+		if (tp->mode == DW_PCIE_RC_TYPE) {
+			struct dw_pcie *pci = tp->pci;
+			struct pcie_port *pp = &pci->pp;
 
-		err = tcc_pcie_reset_control(tp);
-		if (err == 0) {
 			err = tcc_pcie_wake_up_phy(tp);
-		}
+			if (err == 0) {
+				err = tcc_pcie_set_clk(tp);
+			}
 
-		if (err == 0) {
-			err = tcc_pcie_set_clk(tp);
-		}
+			if (err == 0) {
+				err = tcc_pcie_set_defaults(tp);
+			}
 
-		if (err == 0) {
-			err = tcc_pcie_set_defaults(tp);
-		}
+			if (err == 0) {
+				err = tcc_pcie_host_init(pp);
+			}
 
-		if (err == 0) {
-			if (tp->mode == DW_PCIE_RC_TYPE) {
-				err = tcc_pcie_host_init(&pci->pp);
-			} else if (tp->mode == DW_PCIE_EP_TYPE) {
-#ifdef CONFIG_PCIE_DW_EP
-				err = dw_pcie_ep_init(&pci->ep);
-#endif
-			} else {
-				err = -ENODEV;
+			if ((err == 0) && (pp->irq > 0)) {
+				enable_irq((u32)pp->irq);
 			}
 		}
 	} else {
@@ -2721,21 +2382,33 @@ static s32 tcc_pcie_reestablish_link(const struct tcc_pcie *tp)
 	return err;
 }
 
-static s32 tcc_pcie_resume_early(struct device *dev)
+static void tcc_pcie_shutdown(struct platform_device *pdev)
+{
+	if (pdev != NULL) {
+		const struct tcc_pcie *tp = platform_get_drvdata(pdev);
+		const struct dw_pcie *pci = tp->pci;
+		s32 err = 0;
+
+		err = tcc_pcie_deinit_controller(tp);
+		if (err == 0) {
+			err = pinctrl_pm_select_idle_state(pci->dev);
+		}
+	}
+}
+
+static int tcc_pcie_suspend(struct device *dev)
 {
 	s32 err = 0;
 
 	if (dev != NULL) {
-		const struct tcc_pcie *tp =
-			(const struct tcc_pcie *)dev_get_drvdata(dev);
+		const struct tcc_pcie *tp = dev_get_drvdata(dev);
 
-		err = tcc_pcie_reestablish_link(tp);
+		err = tcc_pcie_deinit_controller(tp);
 		if (err == 0) {
-			err = tcc_pcie_restore_reg(tp);
+			err = tcc_pcie_clear_cactive(tp);
 		}
-
 		if (err == 0) {
-			err = tcc_pcie_enable_irq(tp);
+			err = pinctrl_pm_select_sleep_state(dev);
 		}
 	} else {
 		err = -ENODEV;
@@ -2744,28 +2417,20 @@ static s32 tcc_pcie_resume_early(struct device *dev)
 	return err;
 }
 
-static s32 tcc_pcie_suspend_late(struct device *dev)
+static int tcc_pcie_resume(struct device *dev)
 {
 	s32 err = 0;
 
 	if (dev != NULL) {
-		const struct tcc_pcie *tp =
-			(const struct tcc_pcie *)dev_get_drvdata(dev);
-
-		err = tcc_pcie_disable_irq(tp);
-		if (err == 0) {
-			err = tcc_pcie_backup_reg(tp);
-		}
+		const struct tcc_pcie *tp = dev_get_drvdata(dev);
 
+		err = tcc_pcie_reset_control(tp);
 		if (err == 0) {
-			err = phy_power_off(tp->phy);
+			err = tcc_pcie_preinit_controller(tp);
 		}
 
 		if (err == 0) {
-			err = tcc_pcie_clear_cactive(tp);
-		}
-		if (err == 0) {
-			err = pinctrl_pm_select_sleep_state(dev);
+			err = tcc_pcie_set_mse(tp, true);
 		}
 	} else {
 		err = -ENODEV;
@@ -2776,18 +2441,16 @@ static s32 tcc_pcie_suspend_late(struct device *dev)
 
 static const struct dev_pm_ops tcc_pcie_pm_ops = {
 	SET_SYSTEM_SLEEP_PM_OPS(tcc_pcie_suspend, tcc_pcie_resume)
-	SET_LATE_SYSTEM_SLEEP_PM_OPS(tcc_pcie_suspend_late,
-			tcc_pcie_resume_early)
 };
 
 static struct platform_driver tcc_pcie_driver = {
 	.probe = tcc_pcie_probe,
-	.remove = tcc_pcie_remove,
 	.driver = {
 		.name = "telechips-pcie",
 		.pm = &tcc_pcie_pm_ops,
 		.of_match_table = tcc_pcie_of_match,
 	},
+	.shutdown = tcc_pcie_shutdown,
 };
 module_platform_driver(tcc_pcie_driver);
 
diff --git a/drivers/phy/telechips/Kconfig b/drivers/phy/telechips/Kconfig
index 194dc6ff076e..9e9aae82ae89 100644
--- a/drivers/phy/telechips/Kconfig
+++ b/drivers/phy/telechips/Kconfig
@@ -3,15 +3,15 @@
 # Copyright (C) 2023 Telechips Inc.
 #
 
-config PHY_SEC08LPP_PCIE
-	tristate "Telechips Dolphin5 PCIe PHY Driver"
+config PHY_SF08LPP_PCIE
+	tristate "Telechips Samsung Foundry PCIe Gen4 PHY Driver"
 	depends on ARCH_TCC807X || COMPILE_TEST
 	select GENERIC_PHY
 	help
 	  Enable this to support the PCIe PHY on Dolphin5 SoCs.
 
-config PHY_SEC14LPP_PCIE
-	tristate "Telechips Dolphin3s PCIe PHY Driver"
+config PHY_SF14LPP_PCIE
+	tristate "Telechips Samsung Foundry PCIe Gen2 PHY Driver"
 	depends on ARCH_TCC803X
 	select GENERIC_PHY
 	help
diff --git a/drivers/phy/telechips/Makefile b/drivers/phy/telechips/Makefile
index 9b777b0ef59b..78f122e54e72 100644
--- a/drivers/phy/telechips/Makefile
+++ b/drivers/phy/telechips/Makefile
@@ -3,7 +3,7 @@
 # Copyright (C) 2023 Telechips Inc.
 #
 
-obj-$(CONFIG_PHY_SEC08LPP_PCIE)		+= phy-sec08lpp-pcie.o
-obj-$(CONFIG_PHY_SEC14LPP_PCIE)		+= phy-sec14lpp-pcie.o
+obj-$(CONFIG_PHY_SF08LPP_PCIE)		+= phy-sf08lpp-pcie.o
+obj-$(CONFIG_PHY_SF14LPP_PCIE)		+= phy-sf14lpp-pcie.o
 obj-$(CONFIG_PHY_SS14LPP_X1NS_PCIE)		+= phy-ss14lpp-x1ns-pcie.o
 obj-$(CONFIG_PHY_SS14LPP_X2NS_PCIE)		+= phy-ss14lpp-x2ns-pcie.o
diff --git a/drivers/phy/telechips/phy-sec08lpp-pcie.c b/drivers/phy/telechips/phy-sf08lpp-pcie.c
similarity index 59%
rename from drivers/phy/telechips/phy-sec08lpp-pcie.c
rename to drivers/phy/telechips/phy-sf08lpp-pcie.c
index f81aa3282360..341745f39b66 100644
--- a/drivers/phy/telechips/phy-sec08lpp-pcie.c
+++ b/drivers/phy/telechips/phy-sf08lpp-pcie.c
@@ -40,11 +40,6 @@
 #define PCIE_PMA_TRSV_REG5F1		(0x17C4U)
 #define PCIE_PMA_TRSV_REG5FD		(0x17F4U)
 
-/*
- * PCIe controller wrapper pcs configuration registers
- */
-#define PCIE_PCS_OUT_VEC_4		(0x0154U)
-
 /*
  * PCIe controller wrapper phy configuration registers
  */
@@ -64,9 +59,6 @@
 /*
  * Mask/shift bits in PCIe related registers
  */
-#define PCIE_CLK_CFG_RESERVED_CON_SHIFT		(13U)
-#define PCIE_CLK_CFG_RESERVED_CON_MASK		((u32)0xFFFFU << PCIE_CLK_CFG_RESERVED_CON_SHIFT)
-
 #define PCIE_CLK_CFG_BGR_EN_SHIFT		(30U)
 #define PCIE_CLK_CFG_BGR_EN_MASK		((u32)0x1U << PCIE_CLK_CFG_BGR_EN_SHIFT)
 
@@ -103,6 +95,9 @@
 #define PCIE_CLK_CFG_ERIO_USE_LOCK_DONE_MASK		((u32)0x1U << PCIE_CLK_CFG_ERIO_USE_LOCK_DONE_SHIFT)
 #define PCIE_CLK_CFG_ERIO_ISO_ENB_MASK		((u32)0x1U << PCIE_CLK_CFG_ERIO_ISO_ENB_SHIFT)
 
+#define PCIE_CLK_CFG_RESERVED_CON_SHIFT(signal)		((u32)(signal) + 13U)
+#define PCIE_CLK_CFG_RESERVED_CON_MASK(signal)		((u32)0x1U << PCIE_CLK_CFG_RESERVED_CON_SHIFT((signal)))
+
 #define PCIE_PHY_REG_PCS_RSTN_SHIFT		(3U)
 #define PCIE_PHY_REG_PMA_PORT0_RSTN_SHIFT		(2U)
 #define PCIE_PHY_REG_PMA_CMN_RSTN_SHIFT		(1U)
@@ -123,9 +118,6 @@
 #define PCIE_PMA_ANA_LCPLL_SDM_DENOMINATOR_IC1234_G4_SHIFT		(0U)
 #define PCIE_PMA_ANA_LCPLL_SDM_DENOMINATOR_IC1234_G4_MASK		((u32)0xFFU << PCIE_PMA_ANA_LCPLL_SDM_DENOMINATOR_IC1234_G4_SHIFT)
 
-#define PCIE_PMA_IGNORE_ADAP_DONE_SHIFT		(0U)
-#define PCIE_PMA_IGNORE_ADAP_DONE_MASK		((u32)0x1U << PCIE_PMA_IGNORE_ADAP_DONE_SHIFT)
-
 #define PCIE_PMA_ANA_LCPLL_AFC_VCO_CNT_RUN_NUM_SHIFT		(3U)
 #define PCIE_PMA_ANA_LCPLL_AFC_VCO_CNT_RUN_NUM_MASK		((u32)0x1FU << PCIE_PMA_ANA_LCPLL_AFC_VCO_CNT_RUN_NUM_SHIFT)
 
@@ -189,158 +181,28 @@
 #define PCIE_PMA_LN0_OV_S_PI_OFFSET_QCLK_SHIFT		(5U)
 #define PCIE_PMA_LN0_OV_S_PI_OFFSET_QCLK_MASK		((u32)0x1U << PCIE_PMA_LN0_OV_S_PI_OFFSET_QCLK_SHIFT)
 
-#define PCIE_PCS_PMA_CONTROL_VECTOR_MASK		(0xFFFFFU)
-
 #define PCIE_CLK_CFG_PMS_PRESET		(0x201C8001U)
 #define PCIE_FBUS_CLK_RATE		(333333333UL)
 #define PCIE_PHY_CLK_RATE		(100000000UL)
 
-struct ber_preset {
+struct rx_preset {
 	s32 phase;
 	u32 offset;
 	u32 value;
 };
 
-static struct ber_preset ber_preset_table[] = {
-	/* Preset 0 */
-	{ 0, 0x5F8U, 0x00U, },
-	{ 0, 0x600U, 0x23U, },
-	{ 0, 0x604U, 0x44U, },
-	{ 0, 0x608U, 0x61U, },
-	{ 0, 0x60CU, 0x55U, },
-	{ 0, 0x610U, 0x14U, },
-	{ 0, 0x614U, 0x23U, },
-	{ 0, 0x618U, 0x1AU, },
-	{ 0, 0x61CU, 0x04U, },
-	{ 0, 0x5F8U, 0x04U, },
-	{ 0, 0x5F8U, 0x00U, },
-	/* Preset 1 */
-	{ 1, 0x5F8U, 0x08U, },
-	{ 1, 0x604U, 0x42U, },
-	{ 1, 0x5F8U, 0x0CU, },
-	{ 1, 0x5F8U, 0x08U, },
-	/* Preset 2 */
-	{ 2, 0x5F8U, 0x10U, },
-	{ 2, 0x604U, 0x40U, },
-	{ 2, 0x5F8U, 0x14U, },
-	{ 2, 0x5F8U, 0x10U, },
-	/* Preset 3 */
-	{ 3, 0x5F8U, 0x18U, },
-	{ 3, 0x604U, 0x45U, },
-	{ 3, 0x5F8U, 0x1CU, },
-	{ 3, 0x5F8U, 0x18U, },
-	/* Preset 4 */
-	{ 4, 0x5F8U, 0x20U, },
-	{ 4, 0x604U, 0x46U, },
-	{ 4, 0x5F8U, 0x24U, },
-	{ 4, 0x5F8U, 0x20U, },
-	/* Preset 5 */
-	{ 5, 0x5F8U, 0x28U, },
-	{ 5, 0x604U, 0x48U, },
-	{ 5, 0x5F8U, 0x2CU, },
-	{ 5, 0x5F8U, 0x28U, },
-	/* Preset 6 */
-	{ 6, 0x5F8U, 0x30U, },
-	{ 6, 0x604U, 0x4AU, },
-	{ 6, 0x5F8U, 0x34U, },
-	{ 6, 0x5F8U, 0x30U, },
-	/* Preset 7 */
-	{ 7, 0x5F8U, 0x38U, },
-	{ 7, 0x604U, 0x4CU, },
-	{ 7, 0x5F8U, 0x3CU, },
-	{ 7, 0x5F8U, 0x38U, },
-	/* Preset 8 */
-	{ 8, 0x5F8U, 0x40U, },
-	{ 8, 0x600U, 0x20U, },
-	{ 8, 0x604U, 0x20U, },
-	{ 8, 0x608U, 0x01U, },
-	{ 8, 0x5F8U, 0x44U, },
-	{ 8, 0x5F8U, 0x40U, },
-	/* Preset 9 */
-	{ 9, 0x5F8U, 0x48U, },
-	{ 9, 0x600U, 0x20U, },
-	{ 9, 0x604U, 0x21U, },
-	{ 9, 0x608U, 0x01U, },
-	{ 9, 0x5F8U, 0x4CU, },
-	{ 9, 0x5F8U, 0x48U, },
-	/* Preset A */
-	{ 10, 0x5F8U, 0x50U, },
-	{ 10, 0x600U, 0x26U, },
-	{ 10, 0x604U, 0x80U, },
-	{ 10, 0x608U, 0x41U, },
-	{ 10, 0x60CU, 0xAFU, },
-	{ 10, 0x610U, 0x26U, },
-	{ 10, 0x614U, 0x34U, },
-	{ 10, 0x618U, 0x24U, },
-	{ 10, 0x61CU, 0x06U, },
-	{ 10, 0x5F8U, 0x54U, },
-	{ 10, 0x5F8U, 0x50U, },
-	/* Preset B */
-	{ 11, 0x5F8U, 0x58U, },
-	{ 11, 0x604U, 0x81U, },
-	{ 11, 0x5F8U, 0x5CU, },
-	{ 11, 0x5F8U, 0x58U, },
-	/* Preset C */
-	{ 12, 0x5F8U, 0x60U, },
-	{ 12, 0x604U, 0x82U, },
-	{ 12, 0x5F8U, 0x64U, },
-	{ 12, 0x5F8U, 0x60U, },
-	/* Preset D */
-	{ 13, 0x5F8U, 0x68U, },
-	{ 13, 0x604U, 0x83U, },
-	{ 13, 0x5F8U, 0x6CU, },
-	{ 13, 0x5F8U, 0x68U, },
-	/* Preset E */
-	{ 14, 0x5F8U, 0x70U, },
-	{ 14, 0x604U, 0x84U, },
-	{ 14, 0x5F8U, 0x74U, },
-	{ 14, 0x5F8U, 0x70U, },
-	/* Preset F */
-	{ 15, 0x5F8U, 0x78U, },
-	{ 15, 0x600U, 0x26U, },
-	{ 15, 0x604U, 0x85U, },
-	{ 15, 0x608U, 0x80U, },
-	{ 15, 0x60CU, 0x7FU, },
-	{ 15, 0x610U, 0x2DU, },
-	{ 15, 0x614U, 0x34U, },
-	{ 15, 0x618U, 0x24U, },
-	{ 15, 0x61CU, 0x05U, },
-	{ 15, 0x5F8U, 0x7CU, },
-	{ 15, 0x5F8U, 0x78U, },
-	/* Preset 10 */
-	{ 16, 0x5F8U, 0x80U, },
-	{ 16, 0x604U, 0x86U, },
-	{ 16, 0x5F8U, 0x84U, },
-	{ 16, 0x5F8U, 0x80U, },
-	/* Preset 11 */
-	{ 17, 0x5F8U, 0x88U, },
-	{ 17, 0x604U, 0x87U, },
-	{ 17, 0x5F8U, 0x8CU, },
-	{ 17, 0x5F8U, 0x88U, },
-	/* Preset 12 */
-	{ 18, 0x5F8U, 0x90U, },
-	{ 18, 0x604U, 0x88U, },
-	{ 18, 0x5F8U, 0x94U, },
-	{ 18, 0x5F8U, 0x90U, },
-	/* Preset 13 */
-	{ 19, 0x5F8U, 0x98U, },
-	{ 19, 0x604U, 0x89U, },
-	{ 19, 0x5F8U, 0x9CU, },
-	{ 19, 0x5F8U, 0x98U, },
-};
-
-struct sec08lpp_pcie_phy {
+struct sf08lpp_pcie_phy {
 	struct device *dev;
 	struct clk *phy_clk;
 	struct clk *fbus_clk;
 	void __iomem *phy_base;
 	void __iomem *clk_base;
 	void __iomem *pma_base;
-	void __iomem *pcs_base;
-	u32 mode;
+	bool enable_ext_refclk;
+	bool enable;
 };
 
-static inline u32 sec08lpp_pcie_phy_readl(const void __iomem *base, u32 offset)
+static inline u32 sf08lpp_pcie_phy_readl(const void __iomem *base, u32 offset)
 {
 	u32 ret = 0x0U;
 
@@ -351,7 +213,7 @@ static inline u32 sec08lpp_pcie_phy_readl(const void __iomem *base, u32 offset)
 	return ret;
 }
 
-static inline void sec08lpp_pcie_phy_writel(void __iomem *base, u32 offset, u32 val, u32 mask)
+static inline void sf08lpp_pcie_phy_writel(void __iomem *base, u32 offset, u32 val, u32 mask)
 {
 	if (base != NULL) {
 		iowrite32((ioread32(base + offset) & ~mask)|val,
@@ -359,33 +221,32 @@ static inline void sec08lpp_pcie_phy_writel(void __iomem *base, u32 offset, u32
 	}
 }
 
-static s32 sec08lpp_pcie_phy_reset(struct phy *phy)
+static s32 sf08lpp_pcie_phy_reset(struct phy *phy)
 {
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct sec08lpp_pcie_phy *priv =
-			(const struct sec08lpp_pcie_phy *)phy_get_drvdata(phy);
+		const struct sf08lpp_pcie_phy *priv = phy_get_drvdata(phy);
 		u32 val, mask;
 
 		/* assert tx and rx resets */
 		val = 0x0U;
 		mask = PCIE_PHY_REG_PCS_RSTN_MASK;
-		sec08lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_REG00, val, mask);
+		sf08lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_REG00, val, mask);
 
 		udelay(1);
 
 		/* deassert tx, rx and global reset */
 		mask = PCIE_PHY_REG_PCS_RSTN_MASK | PCIE_PHY_REG_PMA_INIT_RSTN_MASK;
 		val = mask;
-		sec08lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_REG00, val, mask);
+		sf08lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_REG00, val, mask);
 
 		udelay(1);
 
 		/* deassert port and common blk resets */
 		mask = PCIE_PHY_REG_PMA_CMN_RSTN_MASK | PCIE_PHY_REG_PMA_PORT0_RSTN_MASK;
 		val = mask;
-		sec08lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_REG00, val, mask);
+		sf08lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_REG00, val, mask);
 	} else {
 		err = -ENODEV;
 	}
@@ -393,14 +254,16 @@ static s32 sec08lpp_pcie_phy_reset(struct phy *phy)
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_enable_clk(const struct sec08lpp_pcie_phy *priv)
+static s32 sf08lpp_pcie_phy_enable_clk(const struct sf08lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
 	if (priv != NULL) {
-		err = clk_prepare_enable(priv->fbus_clk);
-		if (err == 0) {
-			err = clk_prepare_enable(priv->phy_clk);
+		if (priv->enable) {
+			err = clk_prepare_enable(priv->fbus_clk);
+			if (err == 0) {
+				err = clk_prepare_enable(priv->phy_clk);
+			}
 		}
 	} else {
 		err = -EINVAL;
@@ -409,13 +272,15 @@ static s32 sec08lpp_pcie_phy_enable_clk(const struct sec08lpp_pcie_phy *priv)
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_disable_clk(const struct sec08lpp_pcie_phy *priv)
+static s32 sf08lpp_pcie_phy_disable_clk(const struct sf08lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
 	if (priv != NULL) {
-		clk_disable_unprepare(priv->phy_clk);
-		clk_disable_unprepare(priv->fbus_clk);
+		if (priv->enable) {
+			clk_disable_unprepare(priv->phy_clk);
+			clk_disable_unprepare(priv->fbus_clk);
+		}
 	} else {
 		err = -EINVAL;
 	}
@@ -423,7 +288,7 @@ static s32 sec08lpp_pcie_phy_disable_clk(const struct sec08lpp_pcie_phy *priv)
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_pma_pll_lock(const struct sec08lpp_pcie_phy *priv)
+static s32 sf08lpp_pcie_phy_pma_pll_lock(const struct sf08lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
@@ -431,11 +296,11 @@ static s32 sec08lpp_pcie_phy_pma_pll_lock(const struct sec08lpp_pcie_phy *priv)
 		u32 val, mask;
 
 		mask = PCIE_CLK_CFG_PLL_LOCK_MASK;
-		val = sec08lpp_pcie_phy_readl(priv->clk_base, PCIE_CLK_CFG08) & mask;
+		val = sf08lpp_pcie_phy_readl(priv->clk_base, PCIE_CLK_CFG08) & mask;
 		if (val == mask) {
 			mask = PCIE_PMA_PLL_LOCK_MASK;
 			do {
-				val = sec08lpp_pcie_phy_readl(priv->pma_base, PCIE_PMA_CMN_REG13C);
+				val = sf08lpp_pcie_phy_readl(priv->pma_base, PCIE_PMA_CMN_REG13C);
 			} while ((val & mask) != mask);
 		}
 	} else {
@@ -445,7 +310,7 @@ static s32 sec08lpp_pcie_phy_pma_pll_lock(const struct sec08lpp_pcie_phy *priv)
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_disable_clkout(const struct sec08lpp_pcie_phy *priv)
+static s32 sf08lpp_pcie_phy_disable_clkout(const struct sf08lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
@@ -454,7 +319,7 @@ static s32 sec08lpp_pcie_phy_disable_clkout(const struct sec08lpp_pcie_phy *priv
 
 		val = 0x0U;
 		mask = PCIE_CLK_CFG_RESETB_MASK;
-		sec08lpp_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG00, val, mask);
+		sf08lpp_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG00, val, mask);
 	} else {
 		err = -EINVAL;
 	}
@@ -462,24 +327,30 @@ static s32 sec08lpp_pcie_phy_disable_clkout(const struct sec08lpp_pcie_phy *priv
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_set_clk(const struct sec08lpp_pcie_phy *priv, u32 mode)
+static s32 sf08lpp_pcie_phy_set_clk(const struct sf08lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
 	if (priv != NULL) {
-		err = sec08lpp_pcie_phy_enable_clk(priv);
+		err = sf08lpp_pcie_phy_enable_clk(priv);
 		if (err == 0) {
 			u32 val, mask;
 
-			if ((mode & 0xF0U) == 0x0U) {
-				/* RESERVED_CON b[20] */
-				mask = (u32)BIT(20);
-				val = 0x0U;
-				sec08lpp_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG07, val, mask);
+			if (priv->enable_ext_refclk) {
+				mask = PCIE_PHY_REG_LPLL_REF_CLK_SEL_MASK |
+					PCIE_PHY_REG_PORT0_REF_CLK_EN_MASK;
+				val = sf08lpp_pcie_phy_readl(priv->phy_base, PCIE_PHY_REG01) & ~mask;
+				val |= ((u32)0x1U << PCIE_PHY_REG_PORT0_REF_CLK_EN_SHIFT);
+				val |= ((u32)0x1U << PCIE_PHY_REG_LPLL_REF_CLK_SEL_SHIFT);
+				sf08lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_REG01, val, mask);
+			} else {
+				mask = PCIE_CLK_CFG_RESERVED_CON_MASK(7);
+				val = sf08lpp_pcie_phy_readl(priv->clk_base, PCIE_CLK_CFG07) & ~mask;
+				sf08lpp_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG07, val, mask);
 
 				mask = PCIE_CLK_CFG_RESETB_MASK;
 				val = 0x0U;
-				sec08lpp_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG00, val, mask);
+				sf08lpp_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG00, val, mask);
 				udelay(10);
 
 				/*
@@ -488,16 +359,16 @@ static s32 sec08lpp_pcie_phy_set_clk(const struct sec08lpp_pcie_phy *priv, u32 m
 
 				mask = PCIE_CLK_CFG_BGR_EN_MASK;
 				val = mask;
-				sec08lpp_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG06, val, mask);
+				sf08lpp_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG06, val, mask);
 				udelay(100);
 
 				mask = PCIE_CLK_CFG_RESETB_MASK;
 				val = mask;
-				sec08lpp_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG00, val, mask);
+				sf08lpp_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG00, val, mask);
 
 				mask = PCIE_CLK_CFG_PLL_LOCK_MASK;
 				do {
-					val = sec08lpp_pcie_phy_readl(priv->clk_base, PCIE_CLK_CFG08);
+					val = sf08lpp_pcie_phy_readl(priv->clk_base, PCIE_CLK_CFG08);
 				} while ((val & mask) != mask);
 
 				mask = PCIE_CLK_CFG_ERIO_ISO_ENB_MASK |
@@ -505,14 +376,7 @@ static s32 sec08lpp_pcie_phy_set_clk(const struct sec08lpp_pcie_phy *priv, u32 m
 					PCIE_CLK_CFG_ERIO_USE_LOCK_DONE_MASK |
 					PCIE_CLK_CFG_ERIO_EN_MASK;
 				val = mask;
-				sec08lpp_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG04, val, mask);
-			} else {
-				mask = PCIE_PHY_REG_LPLL_REF_CLK_SEL_MASK |
-					PCIE_PHY_REG_PORT0_REF_CLK_EN_MASK;
-				val = sec08lpp_pcie_phy_readl(priv->phy_base, PCIE_PHY_REG01) & ~mask;
-				val |= ((u32)0x1U << PCIE_PHY_REG_PORT0_REF_CLK_EN_SHIFT);
-				val |= (((mode & 0xF0U) >> 4U)  << PCIE_PHY_REG_LPLL_REF_CLK_SEL_SHIFT);
-				sec08lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_REG01, val, mask);
+				sf08lpp_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG04, val, mask);
 			}
 		}
 	} else {
@@ -522,30 +386,22 @@ static s32 sec08lpp_pcie_phy_set_clk(const struct sec08lpp_pcie_phy *priv, u32 m
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_set_mode(struct phy *phy, enum phy_mode mode, s32 submode)
+static s32 sf08lpp_pcie_phy_set_mode(struct phy *phy, enum phy_mode mode, s32 submode)
 {
 	s32 err = 0;
 
-	if (phy != NULL) {
-		struct sec08lpp_pcie_phy *priv = (struct sec08lpp_pcie_phy *)phy_get_drvdata(phy);
-
-		if ((mode != PHY_MODE_PCIE) || (submode < 0)) {
-			err = -EINVAL;
-		} else {
-			u8 clk_mode;
+	(void)submode;
 
-			if (!__builtin_add_overflow(submode, 0, &clk_mode)) {
-				err = sec08lpp_pcie_phy_set_clk((const struct sec08lpp_pcie_phy *)priv, (u32)clk_mode);
-				if (err == 0) {
-					err = sec08lpp_pcie_phy_pma_pll_lock((const struct sec08lpp_pcie_phy *)priv);
-				}
+	if (phy != NULL) {
+		const struct sf08lpp_pcie_phy *priv = phy_get_drvdata(phy);
 
-				if (err == 0) {
-					priv->mode = (u32)clk_mode;
-				}
-			} else {
-				err = -EINVAL;
+		if (mode == PHY_MODE_PCIE) {
+			err = sf08lpp_pcie_phy_set_clk(priv);
+			if (err == 0) {
+				err = sf08lpp_pcie_phy_pma_pll_lock(priv);
 			}
+		} else {
+			err = -EINVAL;
 		}
 	} else {
 		err = -ENODEV;
@@ -554,7 +410,7 @@ static s32 sec08lpp_pcie_phy_set_mode(struct phy *phy, enum phy_mode mode, s32 s
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_pwr_down(const struct sec08lpp_pcie_phy *priv, bool enable)
+static s32 sf08lpp_pcie_phy_pwr_down(const struct sf08lpp_pcie_phy *priv, bool enable)
 {
 	s32 err = 0;
 
@@ -563,7 +419,7 @@ static s32 sec08lpp_pcie_phy_pwr_down(const struct sec08lpp_pcie_phy *priv, bool
 
 		mask = PCIE_PHY_REG_PMA_POWER_OFF_MASK;
 		val = enable ? mask : 0x0U;
-		sec08lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_REG04, val, mask);
+		sf08lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_REG04, val, mask);
 	} else {
 		err = -EINVAL;
 	}
@@ -571,15 +427,17 @@ static s32 sec08lpp_pcie_phy_pwr_down(const struct sec08lpp_pcie_phy *priv, bool
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_power_on(struct phy *phy)
+static s32 sf08lpp_pcie_phy_power_on(struct phy *phy)
 {
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct sec08lpp_pcie_phy *priv =
-			(const struct sec08lpp_pcie_phy *)phy_get_drvdata(phy);
+		struct sf08lpp_pcie_phy *priv = phy_get_drvdata(phy);
 
-		err = sec08lpp_pcie_phy_pwr_down(priv, false);
+		err = sf08lpp_pcie_phy_pwr_down((const struct sf08lpp_pcie_phy *)priv, false);
+		if (err == 0) {
+			priv->enable = true;
+		}
 	} else {
 		err = -ENODEV;
 	}
@@ -587,15 +445,17 @@ static s32 sec08lpp_pcie_phy_power_on(struct phy *phy)
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_power_off(struct phy *phy)
+static s32 sf08lpp_pcie_phy_power_off(struct phy *phy)
 {
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct sec08lpp_pcie_phy *priv =
-			(const struct sec08lpp_pcie_phy *)phy_get_drvdata(phy);
+		struct sf08lpp_pcie_phy *priv = phy_get_drvdata(phy);
 
-		err = sec08lpp_pcie_phy_pwr_down(priv, true);
+		err = sf08lpp_pcie_phy_pwr_down((const struct sf08lpp_pcie_phy *)priv, true);
+		if (err == 0) {
+			priv->enable = false;
+		}
 	} else {
 		err = -ENODEV;
 	}
@@ -603,7 +463,7 @@ static s32 sec08lpp_pcie_phy_power_off(struct phy *phy)
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_revert_to_default(const struct sec08lpp_pcie_phy *priv)
+static s32 sf08lpp_pcie_phy_revert_to_default(const struct sf08lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
@@ -612,15 +472,15 @@ static s32 sec08lpp_pcie_phy_revert_to_default(const struct sec08lpp_pcie_phy *p
 
 		mask = PCIE_PMA_LANE_SEQ_AES_EN_MASK;
 		val = 0x0U;
-		sec08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG17B, val, mask);
+		sf08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG17B, val, mask);
 
 		mask = PCIE_PMA_LN0_OV_S_ANA_TX_DRV_IDRV_EN_MASK;
 		val = mask;
-		sec08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_TRSV_REG40B, val, mask);
+		sf08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_TRSV_REG40B, val, mask);
 
 		mask = PCIE_PMA_LN0_OV_S_PI_OFFSET_QCLK_MASK;
 		val = 0x0U;
-		sec08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_TRSV_REG5FD, val, mask);
+		sf08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_TRSV_REG5FD, val, mask);
 	} else {
 		err = -EINVAL;
 	}
@@ -628,41 +488,168 @@ static s32 sec08lpp_pcie_phy_revert_to_default(const struct sec08lpp_pcie_phy *p
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_sfr_init(const struct sec08lpp_pcie_phy *priv)
+static s32 sf08lpp_pcie_phy_sfr_init(const struct sf08lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
 	if (priv != NULL) {
 		u32 val, mask, idx;
+		struct rx_preset rx_preset_table[] = {
+			/* Preset 0 */
+			{ 0, 0x5F8U, 0x00U, },
+			{ 0, 0x600U, 0x23U, },
+			{ 0, 0x604U, 0x44U, },
+			{ 0, 0x608U, 0x61U, },
+			{ 0, 0x60CU, 0x55U, },
+			{ 0, 0x610U, 0x14U, },
+			{ 0, 0x614U, 0x23U, },
+			{ 0, 0x618U, 0x1AU, },
+			{ 0, 0x61CU, 0x04U, },
+			{ 0, 0x5F8U, 0x04U, },
+			{ 0, 0x5F8U, 0x00U, },
+			/* Preset 1 */
+			{ 1, 0x5F8U, 0x08U, },
+			{ 1, 0x604U, 0x42U, },
+			{ 1, 0x5F8U, 0x0CU, },
+			{ 1, 0x5F8U, 0x08U, },
+			/* Preset 2 */
+			{ 2, 0x5F8U, 0x10U, },
+			{ 2, 0x604U, 0x40U, },
+			{ 2, 0x5F8U, 0x14U, },
+			{ 2, 0x5F8U, 0x10U, },
+			/* Preset 3 */
+			{ 3, 0x5F8U, 0x18U, },
+			{ 3, 0x604U, 0x45U, },
+			{ 3, 0x5F8U, 0x1CU, },
+			{ 3, 0x5F8U, 0x18U, },
+			/* Preset 4 */
+			{ 4, 0x5F8U, 0x20U, },
+			{ 4, 0x604U, 0x46U, },
+			{ 4, 0x5F8U, 0x24U, },
+			{ 4, 0x5F8U, 0x20U, },
+			/* Preset 5 */
+			{ 5, 0x5F8U, 0x28U, },
+			{ 5, 0x604U, 0x48U, },
+			{ 5, 0x5F8U, 0x2CU, },
+			{ 5, 0x5F8U, 0x28U, },
+			/* Preset 6 */
+			{ 6, 0x5F8U, 0x30U, },
+			{ 6, 0x604U, 0x4AU, },
+			{ 6, 0x5F8U, 0x34U, },
+			{ 6, 0x5F8U, 0x30U, },
+			/* Preset 7 */
+			{ 7, 0x5F8U, 0x38U, },
+			{ 7, 0x604U, 0x4CU, },
+			{ 7, 0x5F8U, 0x3CU, },
+			{ 7, 0x5F8U, 0x38U, },
+			/* Preset 8 */
+			{ 8, 0x5F8U, 0x40U, },
+			{ 8, 0x600U, 0x20U, },
+			{ 8, 0x604U, 0x20U, },
+			{ 8, 0x608U, 0x01U, },
+			{ 8, 0x5F8U, 0x44U, },
+			{ 8, 0x5F8U, 0x40U, },
+			/* Preset 9 */
+			{ 9, 0x5F8U, 0x48U, },
+			{ 9, 0x600U, 0x20U, },
+			{ 9, 0x604U, 0x21U, },
+			{ 9, 0x608U, 0x01U, },
+			{ 9, 0x5F8U, 0x4CU, },
+			{ 9, 0x5F8U, 0x48U, },
+			/* Preset A */
+			{ 10, 0x5F8U, 0x50U, },
+			{ 10, 0x600U, 0x26U, },
+			{ 10, 0x604U, 0x80U, },
+			{ 10, 0x608U, 0x41U, },
+			{ 10, 0x60CU, 0xAFU, },
+			{ 10, 0x610U, 0x26U, },
+			{ 10, 0x614U, 0x34U, },
+			{ 10, 0x618U, 0x24U, },
+			{ 10, 0x61CU, 0x06U, },
+			{ 10, 0x5F8U, 0x54U, },
+			{ 10, 0x5F8U, 0x50U, },
+			/* Preset B */
+			{ 11, 0x5F8U, 0x58U, },
+			{ 11, 0x604U, 0x81U, },
+			{ 11, 0x5F8U, 0x5CU, },
+			{ 11, 0x5F8U, 0x58U, },
+			/* Preset C */
+			{ 12, 0x5F8U, 0x60U, },
+			{ 12, 0x604U, 0x82U, },
+			{ 12, 0x5F8U, 0x64U, },
+			{ 12, 0x5F8U, 0x60U, },
+			/* Preset D */
+			{ 13, 0x5F8U, 0x68U, },
+			{ 13, 0x604U, 0x83U, },
+			{ 13, 0x5F8U, 0x6CU, },
+			{ 13, 0x5F8U, 0x68U, },
+			/* Preset E */
+			{ 14, 0x5F8U, 0x70U, },
+			{ 14, 0x604U, 0x84U, },
+			{ 14, 0x5F8U, 0x74U, },
+			{ 14, 0x5F8U, 0x70U, },
+			/* Preset F */
+			{ 15, 0x5F8U, 0x78U, },
+			{ 15, 0x600U, 0x26U, },
+			{ 15, 0x604U, 0x85U, },
+			{ 15, 0x608U, 0x80U, },
+			{ 15, 0x60CU, 0x7FU, },
+			{ 15, 0x610U, 0x2DU, },
+			{ 15, 0x614U, 0x34U, },
+			{ 15, 0x618U, 0x24U, },
+			{ 15, 0x61CU, 0x05U, },
+			{ 15, 0x5F8U, 0x7CU, },
+			{ 15, 0x5F8U, 0x78U, },
+			/* Preset 10 */
+			{ 16, 0x5F8U, 0x80U, },
+			{ 16, 0x604U, 0x86U, },
+			{ 16, 0x5F8U, 0x84U, },
+			{ 16, 0x5F8U, 0x80U, },
+			/* Preset 11 */
+			{ 17, 0x5F8U, 0x88U, },
+			{ 17, 0x604U, 0x87U, },
+			{ 17, 0x5F8U, 0x8CU, },
+			{ 17, 0x5F8U, 0x88U, },
+			/* Preset 12 */
+			{ 18, 0x5F8U, 0x90U, },
+			{ 18, 0x604U, 0x88U, },
+			{ 18, 0x5F8U, 0x94U, },
+			{ 18, 0x5F8U, 0x90U, },
+			/* Preset 13 */
+			{ 19, 0x5F8U, 0x98U, },
+			{ 19, 0x604U, 0x89U, },
+			{ 19, 0x5F8U, 0x9CU, },
+			{ 19, 0x5F8U, 0x98U, },
+		};
 
 		mask = PCIE_PMA_ANA_LCPLL_AFC_VCO_CNT_RUN_NUM_MASK;
 		val = ((u32)0x11U << PCIE_PMA_ANA_LCPLL_AFC_VCO_CNT_RUN_NUM_SHIFT);
-		sec08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG00B, val, mask);
+		sf08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG00B, val, mask);
 
 		mask = PCIE_PMA_ANA_LCPLL_AVC_MAN_CAP_BIAS_CODE_MASK;
 		val = ((u32)0x3U << PCIE_PMA_ANA_LCPLL_AVC_MAN_CAP_BIAS_CODE_SHIFT);
-		sec08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG016, val, mask);
+		sf08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG016, val, mask);
 
 		mask = PCIE_PMA_ANA_LCPLL_ANA_LPF_R_SEL_LC1234_G3_MASK;
 		val = ((u32)0x6U << PCIE_PMA_ANA_LCPLL_ANA_LPF_R_SEL_LC1234_G3_SHIFT);
-		sec08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG024, val, mask);
+		sf08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG024, val, mask);
 
 		mask = PCIE_PMA_ANA_LCPLL_ANA_LPF_R_SEL_LC1234_G4_MASK;
 		val = ((u32)0x6U << PCIE_PMA_ANA_LCPLL_ANA_LPF_R_SEL_LC1234_G4_SHIFT);
-		sec08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG025, val, mask);
+		sf08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG025, val, mask);
 
 		mask = PCIE_PMA_TG_LCPLL_FINE_LOCK_DELAY_TIME_MASK;
 		val = ((u32)0x7U << PCIE_PMA_TG_LCPLL_FINE_LOCK_DELAY_TIME_SHIFT);
-		sec08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG10A, val, mask);
+		sf08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG10A, val, mask);
 
 		mask = PCIE_PMA_RX_MARGIN_SCALE_MASK;
 		val = ((u32)0x1U << PCIE_PMA_RX_MARGIN_SCALE_SHIFT);
-		sec08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG153, val, mask);
+		sf08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG153, val, mask);
 
 		mask = PCIE_PMA_LN0_ANA_TX_RESERVED_MASK;
 		val = ((u32)0x30U << PCIE_PMA_LN0_ANA_TX_RESERVED_SHIFT);
 		for (idx = 0U; idx < 4U; idx++) {
-			sec08lpp_pcie_phy_writel(priv->pma_base,
+			sf08lpp_pcie_phy_writel(priv->pma_base,
 					PCIE_PMA_TRSV_REG424 + (idx * 0x1000U),
 					val,
 					mask);
@@ -671,7 +658,7 @@ static s32 sec08lpp_pcie_phy_sfr_init(const struct sec08lpp_pcie_phy *priv)
 		mask = PCIE_PMA_LN0_ANA_RX_SR_RESERVED_G3_MASK;
 		val = ((u32)0x2U << PCIE_PMA_LN0_ANA_RX_SR_RESERVED_G3_SHIFT);
 		for (idx = 0U; idx < 4U; idx++) {
-			sec08lpp_pcie_phy_writel(priv->pma_base,
+			sf08lpp_pcie_phy_writel(priv->pma_base,
 					PCIE_PMA_TRSV_REG47A + (idx * 0x1000U),
 					val,
 					mask);
@@ -680,7 +667,7 @@ static s32 sec08lpp_pcie_phy_sfr_init(const struct sec08lpp_pcie_phy *priv)
 		mask = PCIE_PMA_LN0_ANA_RX_SR_RESERVED_G4_MASK;
 		val = ((u32)0x4U << PCIE_PMA_LN0_ANA_RX_SR_RESERVED_G4_SHIFT);
 		for (idx = 0U; idx < 4U; idx++) {
-			sec08lpp_pcie_phy_writel(priv->pma_base,
+			sf08lpp_pcie_phy_writel(priv->pma_base,
 					PCIE_PMA_TRSV_REG47B + (idx * 0x1000U),
 					val,
 					mask);
@@ -689,7 +676,7 @@ static s32 sec08lpp_pcie_phy_sfr_init(const struct sec08lpp_pcie_phy *priv)
 		mask = PCIE_PMA_LN0_ANA_RX_CDR_CCO_VCI_AMP_I_CTRL_MASK;
 		val = ((u32)0x0U << PCIE_PMA_LN0_ANA_RX_CDR_CCO_VCI_AMP_I_CTRL_SHIFT);
 		for (idx = 0U; idx < 4U; idx++) {
-			sec08lpp_pcie_phy_writel(priv->pma_base,
+			sf08lpp_pcie_phy_writel(priv->pma_base,
 					PCIE_PMA_TRSV_REG47C + (idx * 0x1000U),
 					val,
 					mask);
@@ -698,7 +685,7 @@ static s32 sec08lpp_pcie_phy_sfr_init(const struct sec08lpp_pcie_phy *priv)
 		mask = PCIE_PMA_LN0_BER_ADAP_RX_START_CURSOR_G4_MASK;
 		val = ((u32)0xCU << PCIE_PMA_LN0_BER_ADAP_RX_START_CURSOR_G4_SHIFT);
 		for (idx = 0U; idx < 4U; idx++) {
-			sec08lpp_pcie_phy_writel(priv->pma_base,
+			sf08lpp_pcie_phy_writel(priv->pma_base,
 					PCIE_PMA_TRSV_REG4CF + (idx * 0x1000U),
 					val,
 					mask);
@@ -707,7 +694,7 @@ static s32 sec08lpp_pcie_phy_sfr_init(const struct sec08lpp_pcie_phy *priv)
 		mask = PCIE_PMA_LN0_RX_EQ_PH_0_1_TIMEOUT_G3_MASK;
 		val = ((u32)0xE5U << PCIE_PMA_LN0_RX_EQ_PH_0_1_TIMEOUT_G3_SHIFT);
 		for (idx = 0U; idx < 4U; idx++) {
-			sec08lpp_pcie_phy_writel(priv->pma_base,
+			sf08lpp_pcie_phy_writel(priv->pma_base,
 					PCIE_PMA_TRSV_REG516 + (idx * 0x1000U),
 					val,
 					mask);
@@ -716,7 +703,7 @@ static s32 sec08lpp_pcie_phy_sfr_init(const struct sec08lpp_pcie_phy *priv)
 		mask = PCIE_PMA_LN0_RX_EQ_PH_2_3_TIMEOUT_G3_MASK;
 		val = ((u32)0xB6U << PCIE_PMA_LN0_RX_EQ_PH_2_3_TIMEOUT_G3_SHIFT);
 		for (idx = 0U; idx < 4U; idx++) {
-			sec08lpp_pcie_phy_writel(priv->pma_base,
+			sf08lpp_pcie_phy_writel(priv->pma_base,
 					PCIE_PMA_TRSV_REG517 + (idx * 0x1000U),
 					val,
 					mask);
@@ -725,7 +712,7 @@ static s32 sec08lpp_pcie_phy_sfr_init(const struct sec08lpp_pcie_phy *priv)
 		mask = PCIE_PMA_LN0_RX_EQ_PH_0_1_TIMEOUT_G4_MASK;
 		val = ((u32)0xF5U << PCIE_PMA_LN0_RX_EQ_PH_0_1_TIMEOUT_G4_SHIFT);
 		for (idx = 0U; idx < 4U; idx++) {
-			sec08lpp_pcie_phy_writel(priv->pma_base,
+			sf08lpp_pcie_phy_writel(priv->pma_base,
 					PCIE_PMA_TRSV_REG518 + (idx * 0x1000U),
 					val,
 					mask);
@@ -734,7 +721,7 @@ static s32 sec08lpp_pcie_phy_sfr_init(const struct sec08lpp_pcie_phy *priv)
 		mask = PCIE_PMA_LN0_RX_EQ_PH_2_3_TIMEOUT_G4_MASK;
 		val = ((u32)0xC4U << PCIE_PMA_LN0_RX_EQ_PH_2_3_TIMEOUT_G4_SHIFT);
 		for (idx = 0U; idx < 4U; idx++) {
-			sec08lpp_pcie_phy_writel(priv->pma_base,
+			sf08lpp_pcie_phy_writel(priv->pma_base,
 					PCIE_PMA_TRSV_REG519 + (idx * 0x1000U),
 					val,
 					mask);
@@ -743,7 +730,7 @@ static s32 sec08lpp_pcie_phy_sfr_init(const struct sec08lpp_pcie_phy *priv)
 		mask = PCIE_PMA_LN0_OV_I_PMAD_RX_CTLE_RS_MF_CTRL_G4_MASK;
 		val = ((u32)0x6U << PCIE_PMA_LN0_OV_I_PMAD_RX_CTLE_RS_MF_CTRL_G4_SHIFT);
 		for (idx = 0U; idx < 4U; idx++) {
-			sec08lpp_pcie_phy_writel(priv->pma_base,
+			sf08lpp_pcie_phy_writel(priv->pma_base,
 					PCIE_PMA_TRSV_REG56D + (idx * 0x1000U),
 					val,
 					mask);
@@ -752,33 +739,21 @@ static s32 sec08lpp_pcie_phy_sfr_init(const struct sec08lpp_pcie_phy *priv)
 		mask = PCIE_PMA_LN0_ANA_RX_RESERVED__7_0_MASK;
 		val = ((u32)0x33U << PCIE_PMA_LN0_ANA_RX_RESERVED__7_0_SHIFT);
 		for (idx = 0U; idx < 4U; idx++) {
-			sec08lpp_pcie_phy_writel(priv->pma_base,
+			sf08lpp_pcie_phy_writel(priv->pma_base,
 					PCIE_PMA_TRSV_REG5F1 + (idx * 0x1000U),
 					val,
 					mask);
 		}
 
 		mask = 0xFFFFFFFFU;
-		for (idx = 0U; idx < (sizeof(ber_preset_table)/sizeof(struct ber_preset)); idx++) {
-			if (ber_preset_table[idx].offset != mask) {
-				sec08lpp_pcie_phy_writel(priv->pma_base,
-						ber_preset_table[idx].offset,
-						ber_preset_table[idx].value,
+		for (idx = 0U; idx < (sizeof(rx_preset_table)/sizeof(struct rx_preset)); idx++) {
+			if (rx_preset_table[idx].offset != mask) {
+				sf08lpp_pcie_phy_writel(priv->pma_base,
+						rx_preset_table[idx].offset,
+						rx_preset_table[idx].value,
 						mask);
 			}
 		}
-
-		val = ((u32)0x1U << PCIE_PMA_IGNORE_ADAP_DONE_SHIFT);
-		mask = PCIE_PMA_IGNORE_ADAP_DONE_MASK;
-		sec08lpp_pcie_phy_writel(priv->pma_base, PCIE_PMA_CMN_REG130, val, mask);
-
-		mask = PCIE_PCS_PMA_CONTROL_VECTOR_MASK;
-		if((priv->mode & (u32)BIT(1)) != 0x0U) {
-			val = 0x700D5U;
-		} else {
-			val = 0x700DDU;
-		}
-		sec08lpp_pcie_phy_writel(priv->pcs_base, PCIE_PCS_OUT_VEC_4, val, mask);
 	} else {
 		err = -EINVAL;
 	}
@@ -786,17 +761,16 @@ static s32 sec08lpp_pcie_phy_sfr_init(const struct sec08lpp_pcie_phy *priv)
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_init(struct phy *phy)
+static s32 sf08lpp_pcie_phy_init(struct phy *phy)
 {
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct sec08lpp_pcie_phy *priv =
-			(const struct sec08lpp_pcie_phy *)phy_get_drvdata(phy);
+		const struct sf08lpp_pcie_phy *priv = phy_get_drvdata(phy);
 
-		err = sec08lpp_pcie_phy_revert_to_default(priv);
+		err = sf08lpp_pcie_phy_revert_to_default(priv);
 		if (err == 0) {
-			err = sec08lpp_pcie_phy_sfr_init(priv);
+			err = sf08lpp_pcie_phy_sfr_init(priv);
 		}
 	} else {
 		err = -ENODEV;
@@ -805,13 +779,12 @@ static s32 sec08lpp_pcie_phy_init(struct phy *phy)
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_exit(struct phy *phy)
+static s32 sf08lpp_pcie_phy_exit(struct phy *phy)
 {
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct sec08lpp_pcie_phy *priv =
-			(const struct sec08lpp_pcie_phy *)phy_get_drvdata(phy);
+		const struct sf08lpp_pcie_phy *priv = phy_get_drvdata(phy);
 
 		/* TODO */
 		(void)priv;
@@ -822,27 +795,26 @@ static s32 sec08lpp_pcie_phy_exit(struct phy *phy)
 	return err;
 }
 
-static const struct phy_ops sec08lpp_pcie_phy_ops = {
-	.init		= sec08lpp_pcie_phy_init,
-	.exit		= sec08lpp_pcie_phy_exit,
-	.power_on		= sec08lpp_pcie_phy_power_on,
-	.power_off		= sec08lpp_pcie_phy_power_off,
-	.reset		= sec08lpp_pcie_phy_reset,
-	.set_mode		= sec08lpp_pcie_phy_set_mode,
+static const struct phy_ops sf08lpp_pcie_phy_ops = {
+	.init		= sf08lpp_pcie_phy_init,
+	.exit		= sf08lpp_pcie_phy_exit,
+	.power_on		= sf08lpp_pcie_phy_power_on,
+	.power_off		= sf08lpp_pcie_phy_power_off,
+	.reset		= sf08lpp_pcie_phy_reset,
+	.set_mode		= sf08lpp_pcie_phy_set_mode,
 	.owner		= THIS_MODULE,
 };
 
-static s32 sec08lpp_pcie_phy_suspend_late(struct device *dev)
+static s32 sf08lpp_pcie_phy_suspend_late(struct device *dev)
 {
 	s32 err = 0;
 
 	if (dev != NULL) {
-		const struct sec08lpp_pcie_phy *priv =
-			(const struct sec08lpp_pcie_phy *)dev_get_drvdata(dev);
+		const struct sf08lpp_pcie_phy *priv = dev_get_drvdata(dev);
 
-		err = sec08lpp_pcie_phy_disable_clkout(priv);
+		err = sf08lpp_pcie_phy_disable_clkout(priv);
 		if (err == 0) {
-			err = sec08lpp_pcie_phy_disable_clk(priv);
+			err = sf08lpp_pcie_phy_disable_clk(priv);
 		}
 	} else {
 		err = -ENODEV;
@@ -851,15 +823,14 @@ static s32 sec08lpp_pcie_phy_suspend_late(struct device *dev)
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_resume_early(struct device *dev)
+static s32 sf08lpp_pcie_phy_resume_early(struct device *dev)
 {
 	s32 err = 0;
 
 	if (dev != NULL) {
-		const struct sec08lpp_pcie_phy *priv =
-			(const struct sec08lpp_pcie_phy *)dev_get_drvdata(dev);
+		const struct sf08lpp_pcie_phy *priv = dev_get_drvdata(dev);
 
-		err = sec08lpp_pcie_phy_enable_clk(priv);
+		err = sf08lpp_pcie_phy_enable_clk(priv);
 	} else {
 		err = -ENODEV;
 	}
@@ -867,20 +838,20 @@ static s32 sec08lpp_pcie_phy_resume_early(struct device *dev)
 	return err;
 }
 
-static const struct dev_pm_ops sec08lpp_pcie_phy_pm_ops = {
-	SET_LATE_SYSTEM_SLEEP_PM_OPS(sec08lpp_pcie_phy_suspend_late,
-			sec08lpp_pcie_phy_resume_early)
+static const struct dev_pm_ops sf08lpp_pcie_phy_pm_ops = {
+	SET_LATE_SYSTEM_SLEEP_PM_OPS(sf08lpp_pcie_phy_suspend_late,
+			sf08lpp_pcie_phy_resume_early)
 };
 
-static const struct of_device_id sec08lpp_pcie_phy_id_table[] = {
+static const struct of_device_id sf08lpp_pcie_phy_id_table[] = {
 	{
 		.compatible = "samsung,ln08lpp-pcie-phy",
 	},
 	{},
 };
-MODULE_DEVICE_TABLE(of, sec08lpp_pcie_phy_id_table);
+MODULE_DEVICE_TABLE(of, sf08lpp_pcie_phy_id_table);
 
-static s32 sec08lpp_pcie_phy_of_parse_clk(struct platform_device *pdev, struct sec08lpp_pcie_phy *priv)
+static s32 sf08lpp_pcie_phy_of_parse_clk(struct platform_device *pdev, struct sf08lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
@@ -906,7 +877,7 @@ static s32 sec08lpp_pcie_phy_of_parse_clk(struct platform_device *pdev, struct s
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_of_parse_reg(struct platform_device *pdev, struct sec08lpp_pcie_phy *priv)
+static s32 sf08lpp_pcie_phy_of_parse_reg(struct platform_device *pdev, struct sf08lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
@@ -940,31 +911,26 @@ static s32 sec08lpp_pcie_phy_of_parse_reg(struct platform_device *pdev, struct s
 				}
 			}
 		}
-
-		if (err == 0) {
-			res = platform_get_resource_byname(pdev, IORESOURCE_MEM, "pcs");
-			if (res != NULL) {
-				priv->pcs_base = devm_ioremap(&pdev->dev, res->start, resource_size(res));
-				if (IS_ERR(priv->pcs_base)) {
-					err = (s32)PTR_ERR(priv->pcs_base);
-				}
-			}
-		}
 	} else {
 		err = -EINVAL;
 	}
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_of_parse_dt(struct platform_device *pdev, struct sec08lpp_pcie_phy *priv)
+static s32 sf08lpp_pcie_phy_parse_dt(struct platform_device *pdev, struct sf08lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
 	if ((pdev != NULL) &&
 			(priv != NULL)) {
-		err = sec08lpp_pcie_phy_of_parse_reg(pdev, priv);
+		err = sf08lpp_pcie_phy_of_parse_reg(pdev, priv);
+		if (err == 0) {
+			err = sf08lpp_pcie_phy_of_parse_clk(pdev, priv);
+		}
+
 		if (err == 0) {
-			err = sec08lpp_pcie_phy_of_parse_clk(pdev, priv);
+			priv->enable_ext_refclk = of_property_read_bool(pdev->dev.of_node,
+					"telechips,enable-ext-refclk");
 		}
 	} else {
 		err = -EINVAL;
@@ -973,19 +939,19 @@ static s32 sec08lpp_pcie_phy_of_parse_dt(struct platform_device *pdev, struct se
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_register(struct sec08lpp_pcie_phy *priv)
+static s32 sf08lpp_pcie_phy_register(struct sf08lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
 	if (priv != NULL) {
 		struct phy_provider *provider;
-		struct phy *phy;
+		struct phy *pcie_phy;
 
-		phy = devm_phy_create(priv->dev, NULL, &sec08lpp_pcie_phy_ops);
-		if (IS_ERR(phy)) {
-			err = (s32)PTR_ERR(phy);
+		pcie_phy = devm_phy_create(priv->dev, NULL, &sf08lpp_pcie_phy_ops);
+		if (IS_ERR(pcie_phy)) {
+			err = (s32)PTR_ERR(pcie_phy);
 		} else {
-			phy_set_drvdata(phy, priv);
+			phy_set_drvdata(pcie_phy, priv);
 			provider = devm_of_phy_provider_register(priv->dev,
 					of_phy_simple_xlate);
 			if (IS_ERR(provider)) {
@@ -999,19 +965,19 @@ static s32 sec08lpp_pcie_phy_register(struct sec08lpp_pcie_phy *priv)
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_probe(struct platform_device *pdev)
+static s32 sf08lpp_pcie_phy_probe(struct platform_device *pdev)
 {
-	struct sec08lpp_pcie_phy *priv;
+	struct sf08lpp_pcie_phy *priv;
 	s32 err = 0;
 
-	priv = (struct sec08lpp_pcie_phy *)devm_kzalloc(&pdev->dev,
-			sizeof(struct sec08lpp_pcie_phy), GFP_KERNEL);
+	priv = devm_kzalloc(&pdev->dev,
+			sizeof(struct sf08lpp_pcie_phy), GFP_KERNEL);
 	if (priv != NULL) {
-		err = sec08lpp_pcie_phy_of_parse_dt(pdev, priv);
+		err = sf08lpp_pcie_phy_parse_dt(pdev, priv);
 		if (err == 0) {
 			priv->dev = &pdev->dev;
 			platform_set_drvdata(pdev, priv);
-			err = sec08lpp_pcie_phy_register(priv);
+			err = sf08lpp_pcie_phy_register(priv);
 		}
 
 		if (err == 0) {
@@ -1024,16 +990,15 @@ static s32 sec08lpp_pcie_phy_probe(struct platform_device *pdev)
 	return err;
 }
 
-static s32 sec08lpp_pcie_phy_remove(struct platform_device *pdev)
+static s32 sf08lpp_pcie_phy_remove(struct platform_device *pdev)
 {
 	s32 err = 0;
 
 	if (pdev != NULL) {
-		const struct sec08lpp_pcie_phy *priv =
-			(const struct sec08lpp_pcie_phy *)platform_get_drvdata(pdev);
+		const struct sf08lpp_pcie_phy *priv = platform_get_drvdata(pdev);
 
 		pm_runtime_disable(priv->dev);
-		err = sec08lpp_pcie_phy_disable_clk(priv);
+		err = sf08lpp_pcie_phy_disable_clk(priv);
 	} else {
 		err = -ENODEV;
 	}
@@ -1041,16 +1006,16 @@ static s32 sec08lpp_pcie_phy_remove(struct platform_device *pdev)
 	return err;
 }
 
-static struct platform_driver sec08lpp_pcie_phy_driver = {
-	.probe		= sec08lpp_pcie_phy_probe,
-	.remove		= sec08lpp_pcie_phy_remove,
+static struct platform_driver sf08lpp_pcie_phy_driver = {
+	.probe		= sf08lpp_pcie_phy_probe,
+	.remove		= sf08lpp_pcie_phy_remove,
 	.driver		= {
-		.name	= "sec-ln08lpp-pcie-phy",
-		.pm	= &sec08lpp_pcie_phy_pm_ops,
-		.of_match_table = of_match_ptr(sec08lpp_pcie_phy_id_table),
+		.name	= "sf-ln08lpp-pcie-phy",
+		.pm	= &sf08lpp_pcie_phy_pm_ops,
+		.of_match_table = of_match_ptr(sf08lpp_pcie_phy_id_table),
 	},
 };
-module_platform_driver(sec08lpp_pcie_phy_driver);
+module_platform_driver(sf08lpp_pcie_phy_driver);
 
 MODULE_DESCRIPTION("Telechips Dolphin5 PCIe PHY Driver");
 MODULE_LICENSE("GPL v2");
diff --git a/drivers/phy/telechips/phy-sec14lpp-pcie.c b/drivers/phy/telechips/phy-sf14lpp-pcie.c
similarity index 60%
rename from drivers/phy/telechips/phy-sec14lpp-pcie.c
rename to drivers/phy/telechips/phy-sf14lpp-pcie.c
index ed29fee1ee2d..a30225d18c9d 100644
--- a/drivers/phy/telechips/phy-sec14lpp-pcie.c
+++ b/drivers/phy/telechips/phy-sf14lpp-pcie.c
@@ -74,7 +74,7 @@
 #define PCIE_PCS_CLK_RATE		(100000000UL)
 #define PCIE_REF_CLK_RATE		(100000000UL)
 
-struct sec14lpp_pcie_phy {
+struct sf14lpp_pcie_phy {
 	struct device *dev;
 	void __iomem *phy_base;
 	void __iomem *link_base;
@@ -83,11 +83,11 @@ struct sec14lpp_pcie_phy {
 	struct clk *apb_clk;
 	struct clk *pcs_clk;
 	struct clk *ref_clk;
-	u32 pms;
-	u32 mode;
+	bool enable_ext_refclk;
+	bool enable;
 };
 
-static inline u32 sec14lpp_pcie_phy_readl(const void __iomem *base, u32 offset)
+static inline u32 sf14lpp_pcie_phy_readl(const void __iomem *base, u32 offset)
 {
 	u32 ret = 0x0U;
 
@@ -98,7 +98,7 @@ static inline u32 sec14lpp_pcie_phy_readl(const void __iomem *base, u32 offset)
 	return ret;
 }
 
-static inline void sec14lpp_pcie_phy_writel(void __iomem *base, u32 offset, u32 val, u32 mask)
+static inline void sf14lpp_pcie_phy_writel(void __iomem *base, u32 offset, u32 val, u32 mask)
 {
 	if (base != NULL) {
 		iowrite32((ioread32(base + offset) & ~mask)|val,
@@ -106,26 +106,28 @@ static inline void sec14lpp_pcie_phy_writel(void __iomem *base, u32 offset, u32
 	}
 }
 
-static s32 sec14lpp_pcie_phy_enable_clk(const struct sec14lpp_pcie_phy *priv)
+static s32 sf14lpp_pcie_phy_enable_clk(const struct sf14lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
 	if (priv != NULL) {
-		err = clk_prepare_enable(priv->aux_clk);
-		if (err == 0) {
-			err = clk_prepare_enable(priv->apb_clk);
-		}
+		if (priv->enable) {
+			err = clk_prepare_enable(priv->aux_clk);
+			if (err == 0) {
+				err = clk_prepare_enable(priv->apb_clk);
+			}
 
-		if (err == 0) {
-			err = clk_prepare_enable(priv->pcs_clk);
-		}
+			if (err == 0) {
+				err = clk_prepare_enable(priv->pcs_clk);
+			}
 
-		if (err == 0) {
-			err = clk_prepare_enable(priv->ref_clk);
-		}
+			if (err == 0) {
+				err = clk_prepare_enable(priv->ref_clk);
+			}
 
-		if (err == 0) {
-			err = clk_prepare_enable(priv->fbus_clk);
+			if (err == 0) {
+				err = clk_prepare_enable(priv->fbus_clk);
+			}
 		}
 	} else {
 		err = -EINVAL;
@@ -134,15 +136,17 @@ static s32 sec14lpp_pcie_phy_enable_clk(const struct sec14lpp_pcie_phy *priv)
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_disable_clk(const struct sec14lpp_pcie_phy *priv)
+static s32 sf14lpp_pcie_phy_disable_clk(const struct sf14lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
 	if (priv != NULL) {
-		clk_disable_unprepare(priv->ref_clk);
-		clk_disable_unprepare(priv->pcs_clk);
-		clk_disable_unprepare(priv->apb_clk);
-		clk_disable_unprepare(priv->aux_clk);
+		if (priv->enable) {
+			clk_disable_unprepare(priv->ref_clk);
+			clk_disable_unprepare(priv->pcs_clk);
+			clk_disable_unprepare(priv->apb_clk);
+			clk_disable_unprepare(priv->aux_clk);
+		}
 	} else {
 		err = -EINVAL;
 	}
@@ -150,25 +154,16 @@ static s32 sec14lpp_pcie_phy_disable_clk(const struct sec14lpp_pcie_phy *priv)
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_set_clk(const struct sec14lpp_pcie_phy *priv, u32 mode)
+static s32 sf14lpp_pcie_phy_pwr_down(const struct sf14lpp_pcie_phy *priv, bool enable)
 {
 	s32 err = 0;
 
 	if (priv != NULL) {
-		err = sec14lpp_pcie_phy_enable_clk(priv);
-		if (err == 0) {
-			u32 val, mask;
-
-			if ((mode & 0xF0U) == 0x0U) {
-				val = (0x1U << PCIE_PHY_ANA_PLL_CLK_OUT_TO_EXT_IO_SEL_SHIFT);
-				mask = PCIE_PHY_ANA_PLL_CLK_OUT_TO_EXT_IO_SEL_MASK;
-				sec14lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_CMN_REG062, val, mask);
+		u32 val, mask;
 
-				val = (0x1U << PCIE_PHY_ANA_AUX_RX_TX_SEL_SHIFT);
-				mask = PCIE_PHY_ANA_AUX_RX_TX_SEL_MASK;
-				sec14lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_CMN_REG064, val, mask);
-			}
-		}
+		mask = PCIE_LINK_CFG_PHY_POWER_OFF_MASK;
+		val = enable ? mask : 0x0U;
+		sf14lpp_pcie_phy_writel(priv->link_base, PCIE_LINK_CFG08, val, mask);
 	} else {
 		err = -EINVAL;
 	}
@@ -176,16 +171,25 @@ static s32 sec14lpp_pcie_phy_set_clk(const struct sec14lpp_pcie_phy *priv, u32 m
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_pwr_down(const struct sec14lpp_pcie_phy *priv, bool enable)
+static s32 sf14lpp_pcie_phy_set_clk(const struct sf14lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
 	if (priv != NULL) {
-		u32 val, mask;
+		err = sf14lpp_pcie_phy_enable_clk(priv);
+		if (err == 0) {
+			u32 val, mask;
 
-		mask = PCIE_LINK_CFG_PHY_POWER_OFF_MASK;
-		val = enable ? mask : 0x0U;
-		sec14lpp_pcie_phy_writel(priv->link_base, PCIE_LINK_CFG08, val, mask);
+			if (!priv->enable_ext_refclk) {
+				val = (0x1U << PCIE_PHY_ANA_PLL_CLK_OUT_TO_EXT_IO_SEL_SHIFT);
+				mask = PCIE_PHY_ANA_PLL_CLK_OUT_TO_EXT_IO_SEL_MASK;
+				sf14lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_CMN_REG062, val, mask);
+
+				val = (0x1U << PCIE_PHY_ANA_AUX_RX_TX_SEL_SHIFT);
+				mask = PCIE_PHY_ANA_AUX_RX_TX_SEL_MASK;
+				sf14lpp_pcie_phy_writel(priv->phy_base, PCIE_PHY_CMN_REG064, val, mask);
+			}
+		}
 	} else {
 		err = -EINVAL;
 	}
@@ -193,24 +197,17 @@ static s32 sec14lpp_pcie_phy_pwr_down(const struct sec14lpp_pcie_phy *priv, bool
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_set_mode(struct phy *phy, enum phy_mode mode, s32 submode)
+static s32 sf14lpp_pcie_phy_set_mode(struct phy *phy, enum phy_mode mode, s32 submode)
 {
 	s32 err = 0;
 
+	(void)submode;
+
 	if (phy != NULL) {
-		struct sec14lpp_pcie_phy *priv = (struct sec14lpp_pcie_phy *)phy_get_drvdata(phy);
+		const struct sf14lpp_pcie_phy *priv = phy_get_drvdata(phy);
 
 		if (mode == PHY_MODE_PCIE) {
-			u8 clk_mode;
-
-			if (!__builtin_add_overflow(submode, 0, &clk_mode)) {
-				err = sec14lpp_pcie_phy_set_clk((const struct sec14lpp_pcie_phy *)priv, (u32)clk_mode);
-				if (err == 0) {
-					priv->mode = (u32)clk_mode;
-				}
-			} else {
-				err = -EINVAL;
-			}
+			err = sf14lpp_pcie_phy_set_clk(priv);
 		} else {
 			err = -EINVAL;
 		}
@@ -221,13 +218,12 @@ static s32 sec14lpp_pcie_phy_set_mode(struct phy *phy, enum phy_mode mode, s32 s
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_reset(struct phy *phy)
+static s32 sf14lpp_pcie_phy_reset(struct phy *phy)
 {
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct sec14lpp_pcie_phy *priv =
-			(const struct sec14lpp_pcie_phy *)phy_get_drvdata(phy);
+		const struct sf14lpp_pcie_phy *priv = phy_get_drvdata(phy);
 
 		/* TODO */
 		(void)priv;
@@ -238,15 +234,17 @@ static s32 sec14lpp_pcie_phy_reset(struct phy *phy)
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_power_on(struct phy *phy)
+static s32 sf14lpp_pcie_phy_power_on(struct phy *phy)
 {
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct sec14lpp_pcie_phy *priv =
-			(const struct sec14lpp_pcie_phy *)phy_get_drvdata(phy);
+		struct sf14lpp_pcie_phy *priv = phy_get_drvdata(phy);
 
-		err = sec14lpp_pcie_phy_pwr_down(priv, false);
+		err = sf14lpp_pcie_phy_pwr_down((const struct sf14lpp_pcie_phy *)priv, false);
+		if (err == 0) {
+			priv->enable = true;
+		}
 	} else {
 		err = -ENODEV;
 	}
@@ -254,15 +252,17 @@ static s32 sec14lpp_pcie_phy_power_on(struct phy *phy)
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_power_off(struct phy *phy)
+static s32 sf14lpp_pcie_phy_power_off(struct phy *phy)
 {
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct sec14lpp_pcie_phy *priv =
-			(const struct sec14lpp_pcie_phy *)phy_get_drvdata(phy);
+		struct sf14lpp_pcie_phy *priv = phy_get_drvdata(phy);
 
-		err = sec14lpp_pcie_phy_pwr_down(priv, true);
+		err = sf14lpp_pcie_phy_pwr_down((const struct sf14lpp_pcie_phy *)priv, true);
+		if (err == 0) {
+			priv->enable = false;
+		}
 	} else {
 		err = -ENODEV;
 	}
@@ -270,34 +270,33 @@ static s32 sec14lpp_pcie_phy_power_off(struct phy *phy)
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_init(struct phy *phy)
+static s32 sf14lpp_pcie_phy_init(struct phy *phy)
 {
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct sec14lpp_pcie_phy *priv =
-			(const struct sec14lpp_pcie_phy *)phy_get_drvdata(phy);
+		const struct sf14lpp_pcie_phy *priv = phy_get_drvdata(phy);
 		u32 val, mask;
 
 		/* wait for PLL lock */
 		mask = PCIE_LINK_CFG_PHY_PLL_LOCKED_MASK;
 		do {
-			val = sec14lpp_pcie_phy_readl(priv->link_base, PCIE_LINK_CFG08) & mask;
+			val = sf14lpp_pcie_phy_readl(priv->link_base, PCIE_LINK_CFG08) & mask;
 		} while (val == 0U);
 
 		/* wait for CDR lock */
 		mask = PCIE_LINK_CFG_PHY_CDR_LOCKED_MASK;
 		do {
-			val = sec14lpp_pcie_phy_readl(priv->link_base, PCIE_LINK_CFG08) & mask;
+			val = sf14lpp_pcie_phy_readl(priv->link_base, PCIE_LINK_CFG08) & mask;
 		} while (val == 0U);
 
 		mask = PCIE_LINK_CFG_AUX_SEL_MASK;
 		val = mask;
-		sec14lpp_pcie_phy_writel(priv->link_base, PCIE_LINK_CFG43, val, mask);
+		sf14lpp_pcie_phy_writel(priv->link_base, PCIE_LINK_CFG43, val, mask);
 
 		mask = PCIE_LINK_CFG_RCVRY_IDLE_STATE_MASK;
 		do {
-			val = sec14lpp_pcie_phy_readl(priv->link_base, PCIE_LINK_CFG08) & mask;
+			val = sf14lpp_pcie_phy_readl(priv->link_base, PCIE_LINK_CFG08) & mask;
 		} while (val != 0U);
 	} else {
 		err = -ENODEV;
@@ -306,13 +305,12 @@ static s32 sec14lpp_pcie_phy_init(struct phy *phy)
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_exit(struct phy *phy)
+static s32 sf14lpp_pcie_phy_exit(struct phy *phy)
 {
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct sec14lpp_pcie_phy *priv =
-			(const struct sec14lpp_pcie_phy *)phy_get_drvdata(phy);
+		const struct sf14lpp_pcie_phy *priv = phy_get_drvdata(phy);
 
 		/* TODO */
 		(void)priv;
@@ -323,25 +321,24 @@ static s32 sec14lpp_pcie_phy_exit(struct phy *phy)
 	return err;
 }
 
-static const struct phy_ops sec14lpp_pcie_phy_ops = {
-	.init		= sec14lpp_pcie_phy_init,
-	.exit		= sec14lpp_pcie_phy_exit,
-	.power_on		= sec14lpp_pcie_phy_power_on,
-	.power_off		= sec14lpp_pcie_phy_power_off,
-	.reset		= sec14lpp_pcie_phy_reset,
-	.set_mode		= sec14lpp_pcie_phy_set_mode,
+static const struct phy_ops sf14lpp_pcie_phy_ops = {
+	.init		= sf14lpp_pcie_phy_init,
+	.exit		= sf14lpp_pcie_phy_exit,
+	.power_on		= sf14lpp_pcie_phy_power_on,
+	.power_off		= sf14lpp_pcie_phy_power_off,
+	.reset		= sf14lpp_pcie_phy_reset,
+	.set_mode		= sf14lpp_pcie_phy_set_mode,
 	.owner		= THIS_MODULE,
 };
 
-static s32 sec14lpp_pcie_phy_suspend_late(struct device *dev)
+static s32 sf14lpp_pcie_phy_suspend_late(struct device *dev)
 {
 	s32 err = 0;
 
 	if (dev != NULL) {
-		const struct sec14lpp_pcie_phy *priv =
-			(const struct sec14lpp_pcie_phy *)dev_get_drvdata(dev);
+		const struct sf14lpp_pcie_phy *priv = dev_get_drvdata(dev);
 
-		err = sec14lpp_pcie_phy_disable_clk(priv);
+		err = sf14lpp_pcie_phy_disable_clk(priv);
 	} else {
 		err = -ENODEV;
 	}
@@ -349,15 +346,14 @@ static s32 sec14lpp_pcie_phy_suspend_late(struct device *dev)
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_resume_early(struct device *dev)
+static s32 sf14lpp_pcie_phy_resume_early(struct device *dev)
 {
 	s32 err = 0;
 
 	if (dev != NULL) {
-		const struct sec14lpp_pcie_phy *priv =
-			(const struct sec14lpp_pcie_phy *)dev_get_drvdata(dev);
+		const struct sf14lpp_pcie_phy *priv = dev_get_drvdata(dev);
 
-		err = sec14lpp_pcie_phy_enable_clk(priv);
+		err = sf14lpp_pcie_phy_enable_clk(priv);
 	} else {
 		err = -ENODEV;
 	}
@@ -365,23 +361,23 @@ static s32 sec14lpp_pcie_phy_resume_early(struct device *dev)
 	return err;
 }
 
-static const struct dev_pm_ops sec14lpp_pcie_phy_pm_ops = {
-	SET_LATE_SYSTEM_SLEEP_PM_OPS(sec14lpp_pcie_phy_suspend_late,
-			sec14lpp_pcie_phy_resume_early)
+static const struct dev_pm_ops sf14lpp_pcie_phy_pm_ops = {
+	SET_LATE_SYSTEM_SLEEP_PM_OPS(sf14lpp_pcie_phy_suspend_late,
+			sf14lpp_pcie_phy_resume_early)
 };
 
 
 #ifdef CONFIG_OF
-static const struct of_device_id sec14lpp_pcie_phy_id_table[] = {
+static const struct of_device_id sf14lpp_pcie_phy_id_table[] = {
 	{
 		.compatible = "samsung,14lpp-pcie-phy",
 	},
 	{},
 };
-MODULE_DEVICE_TABLE(of, sec14lpp_pcie_phy_id_table);
+MODULE_DEVICE_TABLE(of, sf14lpp_pcie_phy_id_table);
 #endif
 
-static s32 sec14lpp_pcie_phy_of_parse_clk(struct platform_device *pdev, struct sec14lpp_pcie_phy *priv)
+static s32 sf14lpp_pcie_phy_of_parse_clk(struct platform_device *pdev, struct sf14lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
@@ -424,18 +420,13 @@ static s32 sec14lpp_pcie_phy_of_parse_clk(struct platform_device *pdev, struct s
 				err = clk_set_rate(priv->ref_clk, PCIE_REF_CLK_RATE);
 			}
 		}
-
-		if (err == 0) {
-			err = of_property_read_u32(pdev->dev.of_node, "pms",
-					&priv->pms);
-		}
 	} else {
 		err = -EINVAL;
 	}
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_of_parse_reg(struct platform_device *pdev, struct sec14lpp_pcie_phy *priv)
+static s32 sf14lpp_pcie_phy_of_parse_reg(struct platform_device *pdev, struct sf14lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
@@ -469,14 +460,19 @@ static s32 sec14lpp_pcie_phy_of_parse_reg(struct platform_device *pdev, struct s
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_of_parse_dt(struct platform_device *pdev, struct sec14lpp_pcie_phy *priv)
+static s32 sf14lpp_pcie_phy_of_parse_dt(struct platform_device *pdev, struct sf14lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
 	if ((pdev != NULL) && (priv != NULL)) {
-		err = sec14lpp_pcie_phy_of_parse_reg(pdev, priv);
+		err = sf14lpp_pcie_phy_of_parse_reg(pdev, priv);
+		if (err == 0) {
+			err = sf14lpp_pcie_phy_of_parse_clk(pdev, priv);
+		}
+
 		if (err == 0) {
-			err = sec14lpp_pcie_phy_of_parse_clk(pdev, priv);
+			priv->enable_ext_refclk = of_property_read_bool(pdev->dev.of_node,
+					"telechips,enable-ext-refclk");
 		}
 	} else {
 		err = -EINVAL;
@@ -485,7 +481,7 @@ static s32 sec14lpp_pcie_phy_of_parse_dt(struct platform_device *pdev, struct se
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_register(struct sec14lpp_pcie_phy *priv)
+static s32 sf14lpp_pcie_phy_register(struct sf14lpp_pcie_phy *priv)
 {
 	s32 err = 0;
 
@@ -493,7 +489,7 @@ static s32 sec14lpp_pcie_phy_register(struct sec14lpp_pcie_phy *priv)
 		struct phy_provider *provider;
 		struct phy *phy;
 
-		phy = devm_phy_create(priv->dev, NULL, &sec14lpp_pcie_phy_ops);
+		phy = devm_phy_create(priv->dev, NULL, &sf14lpp_pcie_phy_ops);
 		if (IS_ERR(phy)) {
 			err = (s32)PTR_ERR(phy);
 		}
@@ -513,19 +509,19 @@ static s32 sec14lpp_pcie_phy_register(struct sec14lpp_pcie_phy *priv)
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_probe(struct platform_device *pdev)
+static s32 sf14lpp_pcie_phy_probe(struct platform_device *pdev)
 {
-	struct sec14lpp_pcie_phy *priv;
+	struct sf14lpp_pcie_phy *priv;
 	s32 err = 0;
 
-	priv = (struct sec14lpp_pcie_phy *)devm_kzalloc(&pdev->dev,
-			sizeof(struct sec14lpp_pcie_phy), GFP_KERNEL);
+	priv = devm_kzalloc(&pdev->dev,
+			sizeof(struct sf14lpp_pcie_phy), GFP_KERNEL);
 	if (priv != NULL) {
-		err = sec14lpp_pcie_phy_of_parse_dt(pdev, priv);
+		err = sf14lpp_pcie_phy_of_parse_dt(pdev, priv);
 		if (err == 0) {
 			priv->dev = &pdev->dev;
 			platform_set_drvdata(pdev, priv);
-			err = sec14lpp_pcie_phy_register(priv);
+			err = sf14lpp_pcie_phy_register(priv);
 		}
 
 		if (err == 0) {
@@ -538,16 +534,15 @@ static s32 sec14lpp_pcie_phy_probe(struct platform_device *pdev)
 	return err;
 }
 
-static s32 sec14lpp_pcie_phy_remove(struct platform_device *pdev)
+static s32 sf14lpp_pcie_phy_remove(struct platform_device *pdev)
 {
 	s32 err = 0;
 
 	if (pdev != NULL) {
-		const struct sec14lpp_pcie_phy *priv =
-			(const struct sec14lpp_pcie_phy *)platform_get_drvdata(pdev);
+		const struct sf14lpp_pcie_phy *priv = platform_get_drvdata(pdev);
 
 		pm_runtime_disable(priv->dev);
-		err = sec14lpp_pcie_phy_disable_clk(priv);
+		err = sf14lpp_pcie_phy_disable_clk(priv);
 	} else {
 		err = -ENODEV;
 	}
@@ -555,16 +550,16 @@ static s32 sec14lpp_pcie_phy_remove(struct platform_device *pdev)
 	return err;
 }
 
-static struct platform_driver sec14lpp_pcie_phy_driver = {
-	.probe		= sec14lpp_pcie_phy_probe,
-	.remove		= sec14lpp_pcie_phy_remove,
+static struct platform_driver sf14lpp_pcie_phy_driver = {
+	.probe		= sf14lpp_pcie_phy_probe,
+	.remove		= sf14lpp_pcie_phy_remove,
 	.driver		= {
-		.name	= "sec14lpp-pcie-phy",
-		.pm	= &sec14lpp_pcie_phy_pm_ops,
-		.of_match_table = of_match_ptr(sec14lpp_pcie_phy_id_table),
+		.name	= "sf-14lpp-pcie-phy",
+		.pm	= &sf14lpp_pcie_phy_pm_ops,
+		.of_match_table = of_match_ptr(sf14lpp_pcie_phy_id_table),
 	},
 };
-module_platform_driver(sec14lpp_pcie_phy_driver);
+module_platform_driver(sf14lpp_pcie_phy_driver);
 
 MODULE_DESCRIPTION("Telechips Dolphin3s PCIe PHY Driver");
 MODULE_LICENSE("GPL v2");
diff --git a/drivers/phy/telechips/phy-ss14lpp-x1ns-pcie.c b/drivers/phy/telechips/phy-ss14lpp-x1ns-pcie.c
index 527bd44af33f..a3536633eac5 100644
--- a/drivers/phy/telechips/phy-ss14lpp-x1ns-pcie.c
+++ b/drivers/phy/telechips/phy-ss14lpp-x1ns-pcie.c
@@ -62,7 +62,8 @@ struct ss14lpp_x1ns_pcie_phy {
 	struct clk *phy_clk;
 	struct clk *fbus_clk;
 	u32 pms;
-	u32 mode;
+	bool enable;
+	bool enable_ext_refclk;
 };
 
 static inline u32 ss14lpp_x1ns_pcie_phy_readl(const void __iomem *base, u32 offset)
@@ -89,9 +90,11 @@ static s32 ss14lpp_x1ns_pcie_phy_enable_clk(const struct ss14lpp_x1ns_pcie_phy *
 	s32 err = 0;
 
 	if (priv != NULL) {
-		err = clk_prepare_enable(priv->fbus_clk);
-		if (err == 0) {
-			err = clk_prepare_enable(priv->phy_clk);
+		if (priv->enable) {
+			err = clk_prepare_enable(priv->fbus_clk);
+			if (err == 0) {
+				err = clk_prepare_enable(priv->phy_clk);
+			}
 		}
 	} else {
 		err = -EINVAL;
@@ -105,8 +108,10 @@ static s32 ss14lpp_x1ns_pcie_phy_disable_clk(const struct ss14lpp_x1ns_pcie_phy
 	s32 err = 0;
 
 	if (priv != NULL) {
-		clk_disable_unprepare(priv->phy_clk);
-		clk_disable_unprepare(priv->fbus_clk);
+		if (priv->enable) {
+			clk_disable_unprepare(priv->phy_clk);
+			clk_disable_unprepare(priv->fbus_clk);
+		}
 	} else {
 		err = -EINVAL;
 	}
@@ -131,7 +136,7 @@ static s32 ss14lpp_x1ns_pcie_phy_disable_clkout(const struct ss14lpp_x1ns_pcie_p
 	return err;
 }
 
-static s32 ss14lpp_x1ns_pcie_phy_set_clk(const struct ss14lpp_x1ns_pcie_phy *priv, u32 mode)
+static s32 ss14lpp_x1ns_pcie_phy_set_clk(const struct ss14lpp_x1ns_pcie_phy *priv)
 {
 	s32 err = 0;
 
@@ -142,10 +147,10 @@ static s32 ss14lpp_x1ns_pcie_phy_set_clk(const struct ss14lpp_x1ns_pcie_phy *pri
 			u32 val, mask;
 
 			mask = PCIE_PHY_REG_REF_USE_PAD_MASK;
-			val = ((mode & 0xF0U) != 0x0U) ? mask : 0x0U;
+			val = priv->enable_ext_refclk ? mask : 0x0U;
 			ss14lpp_x1ns_pcie_phy_writel(priv->phy_base, PCIE_PHY_REG08, val, mask);
 
-			if ((mode & 0xF0U) == 0x0U) {
+			if (!priv->enable_ext_refclk) {
 				val = 0x0U;
 				mask = PCIE_CLK_CFG_RESETB_MASK;
 				ss14lpp_x1ns_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG00, val, mask);
@@ -200,20 +205,13 @@ static s32 ss14lpp_x1ns_pcie_phy_set_mode(struct phy *phy, enum phy_mode mode, s
 {
 	s32 err = 0;
 
+	(void)submode;
+
 	if (phy != NULL) {
-		struct ss14lpp_x1ns_pcie_phy *priv = (struct ss14lpp_x1ns_pcie_phy *)phy_get_drvdata(phy);
+		const struct ss14lpp_x1ns_pcie_phy *priv = phy_get_drvdata(phy);
 
 		if (mode == PHY_MODE_PCIE) {
-			u8 clk_mode;
-
-			if (!__builtin_add_overflow(submode, 0, &clk_mode)) {
-				err = ss14lpp_x1ns_pcie_phy_set_clk((const struct ss14lpp_x1ns_pcie_phy *)priv, (u32)clk_mode);
-				if (err == 0) {
-					priv->mode = (u32)clk_mode;
-				}
-			} else {
-				err = -EINVAL;
-			}
+			err = ss14lpp_x1ns_pcie_phy_set_clk(priv);
 		} else {
 			err = -EINVAL;
 		}
@@ -229,8 +227,7 @@ static s32 ss14lpp_x1ns_pcie_phy_reset(struct phy *phy)
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct ss14lpp_x1ns_pcie_phy *priv =
-			(const struct ss14lpp_x1ns_pcie_phy *)phy_get_drvdata(phy);
+		const struct ss14lpp_x1ns_pcie_phy *priv = phy_get_drvdata(phy);
 
 		/* TODO */
 		(void)priv;
@@ -246,10 +243,12 @@ static s32 ss14lpp_x1ns_pcie_phy_power_on(struct phy *phy)
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct ss14lpp_x1ns_pcie_phy *priv =
-			(const struct ss14lpp_x1ns_pcie_phy *)phy_get_drvdata(phy);
+		struct ss14lpp_x1ns_pcie_phy *priv = phy_get_drvdata(phy);
 
-		err = ss14lpp_x1ns_pcie_phy_pwr_down(priv, false);
+		err = ss14lpp_x1ns_pcie_phy_pwr_down((const struct ss14lpp_x1ns_pcie_phy *)priv, false);
+		if (err == 0) {
+			priv->enable = true;
+		}
 	} else {
 		err = -ENODEV;
 	}
@@ -262,10 +261,12 @@ static s32 ss14lpp_x1ns_pcie_phy_power_off(struct phy *phy)
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct ss14lpp_x1ns_pcie_phy *priv =
-			(const struct ss14lpp_x1ns_pcie_phy *)phy_get_drvdata(phy);
+		struct ss14lpp_x1ns_pcie_phy *priv = phy_get_drvdata(phy);
 
-		err = ss14lpp_x1ns_pcie_phy_pwr_down(priv, true);
+		err = ss14lpp_x1ns_pcie_phy_pwr_down((const struct ss14lpp_x1ns_pcie_phy *)priv, true);
+		if (err == 0) {
+			priv->enable = false;
+		}
 	} else {
 		err = -ENODEV;
 	}
@@ -278,8 +279,7 @@ static s32 ss14lpp_x1ns_pcie_phy_init(struct phy *phy)
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct ss14lpp_x1ns_pcie_phy *priv =
-			(const struct ss14lpp_x1ns_pcie_phy *)phy_get_drvdata(phy);
+		const struct ss14lpp_x1ns_pcie_phy *priv = phy_get_drvdata(phy);
 
 		/* TODO */
 		(void)priv;
@@ -295,8 +295,7 @@ static s32 ss14lpp_x1ns_pcie_phy_exit(struct phy *phy)
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct ss14lpp_x1ns_pcie_phy *priv =
-			(const struct ss14lpp_x1ns_pcie_phy *)phy_get_drvdata(phy);
+		const struct ss14lpp_x1ns_pcie_phy *priv = phy_get_drvdata(phy);
 
 		/* TODO */
 		(void)priv;
@@ -322,8 +321,7 @@ static s32 ss14lpp_x1ns_pcie_phy_suspend_late(struct device *dev)
 	s32 err = 0;
 
 	if (dev != NULL) {
-		const struct ss14lpp_x1ns_pcie_phy *priv =
-			(const struct ss14lpp_x1ns_pcie_phy *)dev_get_drvdata(dev);
+		const struct ss14lpp_x1ns_pcie_phy *priv = dev_get_drvdata(dev);
 
 		err = ss14lpp_x1ns_pcie_phy_disable_clkout(priv);
 		if (err == 0) {
@@ -341,8 +339,7 @@ static s32 ss14lpp_x1ns_pcie_phy_resume_early(struct device *dev)
 	s32 err = 0;
 
 	if (dev != NULL) {
-		const struct ss14lpp_x1ns_pcie_phy *priv =
-			(const struct ss14lpp_x1ns_pcie_phy *)dev_get_drvdata(dev);
+		const struct ss14lpp_x1ns_pcie_phy *priv = dev_get_drvdata(dev);
 
 		err = ss14lpp_x1ns_pcie_phy_enable_clk(priv);
 	} else {
@@ -441,6 +438,11 @@ static s32 ss14lpp_x1ns_pcie_phy_of_parse_dt(struct platform_device *pdev, struc
 		if (err == 0) {
 			err = ss14lpp_x1ns_pcie_phy_of_parse_clk(pdev, priv);
 		}
+
+		if (err == 0) {
+			priv->enable_ext_refclk = of_property_read_bool(pdev->dev.of_node,
+					"telechips,enable-ext-refclk");
+		}
 	} else {
 		err = -EINVAL;
 	}
@@ -480,7 +482,7 @@ static s32 ss14lpp_x1ns_pcie_phy_probe(struct platform_device *pdev)
 	struct ss14lpp_x1ns_pcie_phy *priv;
 	s32 err = 0;
 
-	priv = (struct ss14lpp_x1ns_pcie_phy *)devm_kzalloc(&pdev->dev,
+	priv = devm_kzalloc(&pdev->dev,
 			sizeof(struct ss14lpp_x1ns_pcie_phy), GFP_KERNEL);
 	if (priv != NULL) {
 		err = ss14lpp_x1ns_pcie_phy_of_parse_dt(pdev, priv);
@@ -505,8 +507,7 @@ static s32 ss14lpp_x1ns_pcie_phy_remove(struct platform_device *pdev)
 	s32 err = 0;
 
 	if (pdev != NULL) {
-		const struct ss14lpp_x1ns_pcie_phy *priv =
-			(const struct ss14lpp_x1ns_pcie_phy *)platform_get_drvdata(pdev);
+		const struct ss14lpp_x1ns_pcie_phy *priv = platform_get_drvdata(pdev);
 
 		pm_runtime_disable(priv->dev);
 		err = ss14lpp_x1ns_pcie_phy_disable_clk(priv);
diff --git a/drivers/phy/telechips/phy-ss14lpp-x2ns-pcie.c b/drivers/phy/telechips/phy-ss14lpp-x2ns-pcie.c
index 825ade2890f7..2617936dfcff 100644
--- a/drivers/phy/telechips/phy-ss14lpp-x2ns-pcie.c
+++ b/drivers/phy/telechips/phy-ss14lpp-x2ns-pcie.c
@@ -16,12 +16,10 @@
 /*
  * PCIe controller wrapper clock configuration registers
  */
-#define PCIE_CLK_CFG00		(0x00U)
-#define PCIE_CLK_CFG01		(0x04U)
-#define PCIE_CLK_CFG02		(0x08U)
-#define PCIE_CLK_CFG04		(0x10U)
-#define PCIE_CLK_CFG06		(0x18U)
-#define PCIE_CLK_CFG07		(0x1CU)
+#define PCIE_CLK_REF_BUFF_CFG		(0x04U)
+#define PCIE_CLK_PLL_EN		(0x0CU)
+#define PCIE_CLK_PLL_DIV		(0x14U)
+#define PCIE_CLK_PLLLOCK_ST		(0x1CU)
 
 /*
  * PCIe controller wrapper phy configuration registers
@@ -42,44 +40,44 @@
 #define PCIE_PHY_REG_CFG_BIFURCATION_SHIFT		(0U)
 #define PCIE_PHY_REG_CFG_BIFURCATION_MASK		((u32)0x3U << PCIE_PHY_REG_CFG_BIFURCATION_SHIFT)
 
-#define PCIE_CLK_CFG_FBDIV_SHIFT		(8U)
-#define PCIE_CLK_CFG_DESKEW_SHIFT		(3U)
-#define PCIE_CLK_CFG_FBDIV_MASK		((u32)0xFFFU << PCIE_CLK_CFG_FBDIV_SHIFT)
-#define PCIE_CLK_CFG_DESKEW_MASK		((u32)0x1U << PCIE_CLK_CFG_DESKEW_SHIFT)
-
-#define PCIE_CLK_CFG_POSTDIVEN_SHIFT		(26U)
-#define PCIE_CLK_CFG_POSTDIV0_SHIFT		(19U)
-#define PCIE_CLK_CFG_PLLEN_SHIFT		(18U)
-#define PCIE_CLK_CFG_POSTDIVEN_MASK		((u32)0x1FU << PCIE_CLK_CFG_POSTDIVEN_SHIFT)
-#define PCIE_CLK_CFG_POSTDIV0_MASK		((u32)0x3FU << PCIE_CLK_CFG_POSTDIV0_SHIFT)
-#define PCIE_CLK_CFG_PLLEN_MASK		((u32)0x1U << PCIE_CLK_CFG_PLLEN_SHIFT)
-
-#define PCIE_CLK_CFG_PLL_STS_LOCK_SHIFT		(16U)
-#define PCIE_CLK_CFG_PLL_CLK_EN_SHIFT		(13U)
-#define PCIE_CLK_CFG_REFDIVEN_SHIFT		(8U)
-#define PCIE_CLK_CFG_REFDIV_SHIFT		(2U)
-#define PCIE_CLK_CFG_PLL_STS_LOCK_MASK		((u32)0x1U << PCIE_CLK_CFG_PLL_STS_LOCK_SHIFT)
-#define PCIE_CLK_CFG_PLL_CLK_EN_MASK		((u32)0x1U << PCIE_CLK_CFG_PLL_CLK_EN_SHIFT)
-#define PCIE_CLK_CFG_REFDIVEN_MASK		((u32)0x1U << PCIE_CLK_CFG_REFDIVEN_SHIFT)
-#define PCIE_CLK_CFG_REFDIV_MASK		((u32)0x3FU << PCIE_CLK_CFG_REFDIV_SHIFT)
-
-#define PCIE_CLK_CFG_ERIO_EN_SHIFT		(18U)
-#define PCIE_CLK_CFG_ERIO_PLL_LOCK_DONE_SHIFT		(5U)
-#define PCIE_CLK_CFG_ERIO_USE_LOCK_DONE_SHIFT		(4U)
-#define PCIE_CLK_CFG_ERIO_ISO_ENB_SHIFT		(0U)
-#define PCIE_CLK_CFG_ERIO_EN_MASK		((u32)0x1U << PCIE_CLK_CFG_ERIO_EN_SHIFT)
-#define PCIE_CLK_CFG_ERIO_PLL_LOCK_DONE_MASK		((u32)0x1U << PCIE_CLK_CFG_ERIO_PLL_LOCK_DONE_SHIFT)
-#define PCIE_CLK_CFG_ERIO_USE_LOCK_DONE_MASK		((u32)0x1U << PCIE_CLK_CFG_ERIO_USE_LOCK_DONE_SHIFT)
-#define PCIE_CLK_CFG_ERIO_ISO_ENB_MASK		((u32)0x1U << PCIE_CLK_CFG_ERIO_ISO_ENB_SHIFT)
-
-#define PCIE_CLK_CFG_PLL_LOCK_CNT_SHIFT		(0U)
-#define PCIE_CLK_CFG_PLL_LOCK_CNT_MASK		((u32)0xFU << PCIE_CLK_CFG_PLL_LOCK_CNT_SHIFT)
-
-#define PCIE_CLK_CFG_OFFSETCALCNT_SHIFT		(2U)
-#define PCIE_CLK_CFG_OFFSETCALCNT_MASK		((u32)0x7U << PCIE_CLK_CFG_OFFSETCALCNT_SHIFT)
-
-#define PCIE_FBUS_CLK_RATE		(333333333UL)
-#define PCIE_PHY_CLK_RATE		(100000000UL)
+#define PCIE_CLK_REF_BUFF_CFG_ERIO_EN_SHIFT		(18U)
+#define PCIE_CLK_REF_BUFF_CFG_ERIO_PLL_LOCK_DONE_SHIFT		(5U)
+#define PCIE_CLK_REF_BUFF_CFG_ERIO_USE_LOCK_DONE_SHIFT		(4U)
+#define PCIE_CLK_REF_BUFF_CFG_ERIO_FB_CLK_EN_SHIFT		(3U)
+#define PCIE_CLK_REF_BUFF_CFG_ERIO_ISO_ENB_SHIFT		(0U)
+#define PCIE_CLK_REF_BUFF_CFG_ERIO_EN_MASK		((u32)0x1U << PCIE_CLK_REF_BUFF_CFG_ERIO_EN_SHIFT)
+#define PCIE_CLK_REF_BUFF_CFG_ERIO_PLL_LOCK_DONE_MASK		((u32)0x1U << PCIE_CLK_REF_BUFF_CFG_ERIO_PLL_LOCK_DONE_SHIFT)
+#define PCIE_CLK_REF_BUFF_CFG_ERIO_USE_LOCK_DONE_MASK		((u32)0x1U << PCIE_CLK_REF_BUFF_CFG_ERIO_USE_LOCK_DONE_SHIFT)
+#define PCIE_CLK_REF_BUFF_CFG_ERIO_FB_CLK_EN_MASK		((u32)0x1U << PCIE_CLK_REF_BUFF_CFG_ERIO_FB_CLK_EN_SHIFT)
+#define PCIE_CLK_REF_BUFF_CFG_ERIO_ISO_ENB_MASK		((u32)0x1U << PCIE_CLK_REF_BUFF_CFG_ERIO_ISO_ENB_SHIFT)
+
+#define PCIE_CLK_PLL_EN_PLL_CLOCK_GEN_SHIFT		(5U)
+#define PCIE_CLK_PLL_EN_DSMEN_SHIFT		(4U)
+#define PCIE_CLK_PLL_EN_FOUTVCOEN_SHIFT		(3U)
+#define PCIE_CLK_PLL_EN_DACEN_SHIFT		(2U)
+#define PCIE_CLK_PLL_EN_FOUTPOSTDIVEN_SHIFT		(1U)
+#define PCIE_CLK_PLL_EN_SHIFT		(0U)
+#define PCIE_CLK_PLL_EN_PLL_CLOCK_GEN_MASK		((u32)0x1U  << PCIE_CLK_PLL_EN_PLL_CLOCK_GEN_SHIFT)
+#define PCIE_CLK_PLL_EN_DSMEN_MASK		((u32)0x1U  << PCIE_CLK_PLL_EN_DSMEN_SHIFT)
+#define PCIE_CLK_PLL_EN_FOUTVCOEN_MASK		((u32)0x1U  << PCIE_CLK_PLL_EN_FOUTVCOEN_SHIFT)
+#define PCIE_CLK_PLL_EN_DACEN_MASK		((u32)0x1U  << PCIE_CLK_PLL_EN_DACEN_SHIFT)
+#define PCIE_CLK_PLL_EN_FOUTPOSTDIVEN_MASK		((u32)0x1U  << PCIE_CLK_PLL_EN_FOUTPOSTDIVEN_SHIFT)
+#define PCIE_CLK_PLL_EN_MASK		((u32)0x1U  << PCIE_CLK_PLL_EN_SHIFT)
+
+#define PCIE_CLK_PLL_DIV_POSTDIV_SHIFT		(20U)
+#define PCIE_CLK_PLL_DIV_REFDIV_SHIFT		(12U)
+#define PCIE_CLK_PLL_DIV_FBDIV_SHIFT		(0U)
+#define PCIE_CLK_PLL_DIV_POSTDIV_MASK		((u32)0x7FU << PCIE_CLK_PLL_DIV_POSTDIV_SHIFT)
+#define PCIE_CLK_PLL_DIV_REFDIV_MASK		((u32)0x3FU << PCIE_CLK_PLL_DIV_REFDIV_SHIFT)
+#define PCIE_CLK_PLL_DIV_FBDIV_MASK		((u32)0xFFFU << PCIE_CLK_PLL_DIV_FBDIV_SHIFT)
+
+#define PCIE_CLK_PLLLOCK_ST_PLL_LOCK_P_SHIFT		(1U)
+#define PCIE_CLK_PLLLOCK_ST_PLL_LOCK_F_SHIFT		(0U)
+#define PCIE_CLK_PLLLOCK_ST_PLL_LOCK_P_MASK		((u32)0x1U << PCIE_CLK_PLLLOCK_ST_PLL_LOCK_P_SHIFT)
+#define PCIE_CLK_PLLLOCK_ST_PLL_LOCK_F_MASK		((u32)0x1U << PCIE_CLK_PLLLOCK_ST_PLL_LOCK_F_SHIFT)
+
+#define PCIE_FBUS_CLK_RATE		(500000000UL)
+#define PCIE_PHY_CLK_RATE		(25000000UL)
 
 struct ss14lpp_x2ns_pcie_phy {
 	struct device *dev;
@@ -88,7 +86,8 @@ struct ss14lpp_x2ns_pcie_phy {
 	struct clk *phy_clk;
 	struct clk *fbus_clk;
 	u32 bifurcation;
-	u32 mode;
+	bool enable;
+	bool enable_ext_refclk;
 };
 
 static inline u32 ss14lpp_x2ns_pcie_phy_readl(const void __iomem *base, u32 offset)
@@ -115,9 +114,11 @@ static s32 ss14lpp_x2ns_pcie_phy_enable_clk(const struct ss14lpp_x2ns_pcie_phy *
 	s32 err = 0;
 
 	if (priv != NULL) {
-		err = clk_prepare_enable(priv->fbus_clk);
-		if (err == 0) {
-			err = clk_prepare_enable(priv->phy_clk);
+		if (priv->enable) {
+			err = clk_prepare_enable(priv->fbus_clk);
+			if (err == 0) {
+				err = clk_prepare_enable(priv->phy_clk);
+			}
 		}
 	} else {
 		err = -EINVAL;
@@ -131,8 +132,10 @@ static s32 ss14lpp_x2ns_pcie_phy_disable_clk(const struct ss14lpp_x2ns_pcie_phy
 	s32 err = 0;
 
 	if (priv != NULL) {
-		clk_disable_unprepare(priv->phy_clk);
-		clk_disable_unprepare(priv->fbus_clk);
+		if (priv->enable) {
+			clk_disable_unprepare(priv->phy_clk);
+			clk_disable_unprepare(priv->fbus_clk);
+		}
 	} else {
 		err = -EINVAL;
 	}
@@ -140,7 +143,7 @@ static s32 ss14lpp_x2ns_pcie_phy_disable_clk(const struct ss14lpp_x2ns_pcie_phy
 	return err;
 }
 
-static s32 ss14lpp_x2ns_pcie_phy_set_clk(const struct ss14lpp_x2ns_pcie_phy *priv, u32 mode)
+static s32 ss14lpp_x2ns_pcie_phy_set_clk(const struct ss14lpp_x2ns_pcie_phy *priv)
 {
 	s32 err = 0;
 
@@ -154,50 +157,46 @@ static s32 ss14lpp_x2ns_pcie_phy_set_clk(const struct ss14lpp_x2ns_pcie_phy *pri
 			ss14lpp_x2ns_pcie_phy_writel(priv->phy_base, PCIE_PHY_REG58, val, mask);
 
 			mask = PCIE_PHY_REG_REF_USE_PAD_MASK;
-			val = ((mode & 0xF0U) != 0x0U) ? mask : 0x0U;
+			val = priv->enable_ext_refclk ? mask : 0x0U;
 			ss14lpp_x2ns_pcie_phy_writel(priv->phy_base, PCIE_PHY_REG08, val, mask);
 
-			if ((mode & 0xF0U) == 0x0U) {
-				mask = PCIE_CLK_CFG_PLL_LOCK_CNT_MASK;
-				val = (u32)7U << PCIE_CLK_CFG_PLL_LOCK_CNT_SHIFT;
-				ss14lpp_x2ns_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG06, val, mask);
-
-				mask = PCIE_CLK_CFG_OFFSETCALCNT_MASK;
-				val = (u32)0x2U << PCIE_CLK_CFG_OFFSETCALCNT_SHIFT;
-				ss14lpp_x2ns_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG07, val, mask);
-
-				mask = PCIE_CLK_CFG_DESKEW_MASK | PCIE_CLK_CFG_FBDIV_MASK;
-				val = (u32)200U << PCIE_CLK_CFG_FBDIV_SHIFT;
-				ss14lpp_x2ns_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG00, val, mask);
-
-				mask = PCIE_CLK_CFG_REFDIV_MASK | PCIE_CLK_CFG_REFDIVEN_MASK;
-				val = (u32)0x3U << PCIE_CLK_CFG_REFDIV_SHIFT;
-				val |= (u32)0x1U << PCIE_CLK_CFG_REFDIVEN_SHIFT;
-				ss14lpp_x2ns_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG02, val, mask);
-
-				mask = PCIE_CLK_CFG_PLLEN_MASK | PCIE_CLK_CFG_POSTDIV0_MASK | PCIE_CLK_CFG_POSTDIVEN_MASK;
-				val = (u32)0x4U << PCIE_CLK_CFG_POSTDIV0_SHIFT;
-				val |= (u32)0x1U << PCIE_CLK_CFG_POSTDIVEN_SHIFT;
-				ss14lpp_x2ns_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG01, val, mask);
-
-				val |= (u32)0x1U << PCIE_CLK_CFG_PLLEN_SHIFT;
-				ss14lpp_x2ns_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG01, val, mask);
-
-				mask = PCIE_CLK_CFG_PLL_STS_LOCK_MASK;
+			if (!priv->enable_ext_refclk) {
+				mask = PCIE_CLK_PLL_DIV_POSTDIV_MASK |
+					PCIE_CLK_PLL_DIV_REFDIV_MASK |
+					PCIE_CLK_PLL_DIV_FBDIV_MASK;
+				val = ss14lpp_x2ns_pcie_phy_readl(priv->clk_base, PCIE_CLK_PLL_DIV) & ~mask;
+				val |= (u32)0xC8U << PCIE_CLK_PLL_DIV_FBDIV_SHIFT;
+				val |= (u32)0x2FU << PCIE_CLK_PLL_DIV_POSTDIV_SHIFT;
+				val |= (u32)0x1U << PCIE_CLK_PLL_DIV_REFDIV_SHIFT;
+				ss14lpp_x2ns_pcie_phy_writel(priv->clk_base, PCIE_CLK_PLL_DIV, val, mask);
+
+				mask = PCIE_CLK_PLL_EN_PLL_CLOCK_GEN_MASK |
+					PCIE_CLK_PLL_EN_DSMEN_MASK |
+					PCIE_CLK_PLL_EN_FOUTVCOEN_MASK |
+					PCIE_CLK_PLL_EN_DACEN_MASK |
+					PCIE_CLK_PLL_EN_FOUTPOSTDIVEN_MASK |
+					PCIE_CLK_PLL_EN_MASK;
+				val = PCIE_CLK_PLL_EN_PLL_CLOCK_GEN_MASK |
+					PCIE_CLK_PLL_EN_FOUTPOSTDIVEN_MASK |
+					PCIE_CLK_PLL_EN_MASK;
+				ss14lpp_x2ns_pcie_phy_writel(priv->clk_base, PCIE_CLK_PLL_EN, val, mask);
+
+				mask = PCIE_CLK_PLLLOCK_ST_PLL_LOCK_P_MASK |
+					PCIE_CLK_PLLLOCK_ST_PLL_LOCK_F_MASK;
 				do {
-					val = ss14lpp_x2ns_pcie_phy_readl(priv->clk_base, PCIE_CLK_CFG02);
-				} while((val & mask) == 0x0U);
+					val = ss14lpp_x2ns_pcie_phy_readl(priv->clk_base, PCIE_CLK_PLLLOCK_ST);
+				} while ((val & mask) == 0x0U);
 
-				mask = PCIE_CLK_CFG_PLL_CLK_EN_MASK;
-				val |= mask;
-				ss14lpp_x2ns_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG02, val, mask);
+				mask = PCIE_CLK_REF_BUFF_CFG_ERIO_EN_MASK;
+				val = mask;
+				ss14lpp_x2ns_pcie_phy_writel(priv->clk_base, PCIE_CLK_REF_BUFF_CFG, val, mask);
 
-				mask = PCIE_CLK_CFG_ERIO_ISO_ENB_MASK |
-					PCIE_CLK_CFG_ERIO_PLL_LOCK_DONE_MASK |
-					PCIE_CLK_CFG_ERIO_USE_LOCK_DONE_MASK |
-					PCIE_CLK_CFG_ERIO_EN_MASK;
+				mask = PCIE_CLK_REF_BUFF_CFG_ERIO_PLL_LOCK_DONE_MASK |
+					PCIE_CLK_REF_BUFF_CFG_ERIO_USE_LOCK_DONE_MASK |
+					PCIE_CLK_REF_BUFF_CFG_ERIO_FB_CLK_EN_MASK |
+					PCIE_CLK_REF_BUFF_CFG_ERIO_ISO_ENB_MASK;
 				val = mask;
-				ss14lpp_x2ns_pcie_phy_writel(priv->clk_base, PCIE_CLK_CFG04, val, mask);
+				ss14lpp_x2ns_pcie_phy_writel(priv->clk_base, PCIE_CLK_REF_BUFF_CFG, val, mask);
 			}
 		}
 	} else {
@@ -228,20 +227,13 @@ static s32 ss14lpp_x2ns_pcie_phy_set_mode(struct phy *phy, enum phy_mode mode, s
 {
 	s32 err = 0;
 
+	(void)submode;
+
 	if (phy != NULL) {
-		struct ss14lpp_x2ns_pcie_phy *priv = (struct ss14lpp_x2ns_pcie_phy *)phy_get_drvdata(phy);
+		const struct ss14lpp_x2ns_pcie_phy *priv = phy_get_drvdata(phy);
 
 		if (mode == PHY_MODE_PCIE) {
-			u8 clk_mode;
-
-			if (!__builtin_add_overflow(submode, 0, &clk_mode)) {
-				err = ss14lpp_x2ns_pcie_phy_set_clk((const struct ss14lpp_x2ns_pcie_phy *)priv, (u32)clk_mode);
-				if (err == 0) {
-					priv->mode = (u32)clk_mode;
-				}
-			} else {
-				err = -EINVAL;
-			}
+			err = ss14lpp_x2ns_pcie_phy_set_clk(priv);
 		} else {
 			err = -EINVAL;
 		}
@@ -257,8 +249,7 @@ static s32 ss14lpp_x2ns_pcie_phy_reset(struct phy *phy)
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct ss14lpp_x2ns_pcie_phy *priv =
-			(const struct ss14lpp_x2ns_pcie_phy *)phy_get_drvdata(phy);
+		const struct ss14lpp_x2ns_pcie_phy *priv = phy_get_drvdata(phy);
 
 		/* TODO */
 		(void)priv;
@@ -274,10 +265,12 @@ static s32 ss14lpp_x2ns_pcie_phy_power_on(struct phy *phy)
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct ss14lpp_x2ns_pcie_phy *priv =
-			(const struct ss14lpp_x2ns_pcie_phy *)phy_get_drvdata(phy);
+		struct ss14lpp_x2ns_pcie_phy *priv = phy_get_drvdata(phy);
 
-		err = ss14lpp_x2ns_pcie_phy_pwr_down(priv, false);
+		err = ss14lpp_x2ns_pcie_phy_pwr_down((const struct ss14lpp_x2ns_pcie_phy *)priv, false);
+		if (err == 0) {
+			priv->enable = true;
+		}
 	} else {
 		err = -ENODEV;
 	}
@@ -290,10 +283,12 @@ static s32 ss14lpp_x2ns_pcie_phy_power_off(struct phy *phy)
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct ss14lpp_x2ns_pcie_phy *priv =
-			(const struct ss14lpp_x2ns_pcie_phy *)phy_get_drvdata(phy);
+		struct ss14lpp_x2ns_pcie_phy *priv = phy_get_drvdata(phy);
 
-		err = ss14lpp_x2ns_pcie_phy_pwr_down(priv, true);
+		err = ss14lpp_x2ns_pcie_phy_pwr_down((const struct ss14lpp_x2ns_pcie_phy *)priv, true);
+		if (err == 0) {
+			priv->enable = false;
+		}
 	} else {
 		err = -ENODEV;
 	}
@@ -306,8 +301,7 @@ static s32 ss14lpp_x2ns_pcie_phy_init(struct phy *phy)
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct ss14lpp_x2ns_pcie_phy *priv =
-			(const struct ss14lpp_x2ns_pcie_phy *)phy_get_drvdata(phy);
+		const struct ss14lpp_x2ns_pcie_phy *priv = phy_get_drvdata(phy);
 
 		/* TODO */
 		(void)priv;
@@ -323,8 +317,7 @@ static s32 ss14lpp_x2ns_pcie_phy_exit(struct phy *phy)
 	s32 err = 0;
 
 	if (phy != NULL) {
-		const struct ss14lpp_x2ns_pcie_phy *priv =
-			(const struct ss14lpp_x2ns_pcie_phy *)phy_get_drvdata(phy);
+		const struct ss14lpp_x2ns_pcie_phy *priv = phy_get_drvdata(phy);
 
 		/* TODO */
 		(void)priv;
@@ -350,8 +343,7 @@ static s32 ss14lpp_x2ns_pcie_phy_suspend_late(struct device *dev)
 	s32 err = 0;
 
 	if (dev != NULL) {
-		const struct ss14lpp_x2ns_pcie_phy *priv =
-			(const struct ss14lpp_x2ns_pcie_phy *)dev_get_drvdata(dev);
+		const struct ss14lpp_x2ns_pcie_phy *priv = dev_get_drvdata(dev);
 
 		err = ss14lpp_x2ns_pcie_phy_disable_clk(priv);
 	} else {
@@ -366,8 +358,7 @@ static s32 ss14lpp_x2ns_pcie_phy_resume_early(struct device *dev)
 	s32 err = 0;
 
 	if (dev != NULL) {
-		const struct ss14lpp_x2ns_pcie_phy *priv =
-			(const struct ss14lpp_x2ns_pcie_phy *)dev_get_drvdata(dev);
+		const struct ss14lpp_x2ns_pcie_phy *priv = dev_get_drvdata(dev);
 
 		err = ss14lpp_x2ns_pcie_phy_enable_clk(priv);
 	} else {
@@ -395,6 +386,7 @@ MODULE_DEVICE_TABLE(of, ss14lpp_x2ns_pcie_phy_id_table);
 static s32 ss14lpp_x2ns_pcie_phy_of_parse_clk(struct platform_device *pdev, struct ss14lpp_x2ns_pcie_phy *priv)
 {
 	s32 err = 0;
+
 	if ((pdev != NULL) && (priv != NULL)) {
 		priv->fbus_clk = devm_clk_get(&pdev->dev, "pcie_fbus");
 		if (IS_ERR(priv->fbus_clk)) {
@@ -411,11 +403,6 @@ static s32 ss14lpp_x2ns_pcie_phy_of_parse_clk(struct platform_device *pdev, stru
 				err = clk_set_rate(priv->phy_clk, PCIE_PHY_CLK_RATE);
 			}
 		}
-
-		if (err == 0) {
-			err = of_property_read_u32(pdev->dev.of_node, "bifurcation",
-					&priv->bifurcation);
-		}
 	} else {
 		err = -EINVAL;
 	}
@@ -465,6 +452,16 @@ static s32 ss14lpp_x2ns_pcie_phy_of_parse_dt(struct platform_device *pdev, struc
 		if (err == 0) {
 			err = ss14lpp_x2ns_pcie_phy_of_parse_clk(pdev, priv);
 		}
+
+		if (err == 0) {
+			err = of_property_read_u32(pdev->dev.of_node, "bifurcation",
+					&priv->bifurcation);
+		}
+
+		if (err == 0) {
+			priv->enable_ext_refclk = of_property_read_bool(pdev->dev.of_node,
+					"telechips,enable-ext-refclk");
+		}
 	} else {
 		err = -EINVAL;
 	}
@@ -502,7 +499,7 @@ static s32 ss14lpp_x2ns_pcie_phy_probe(struct platform_device *pdev)
 	struct ss14lpp_x2ns_pcie_phy *priv;
 	s32 err = 0;
 
-	priv = (struct ss14lpp_x2ns_pcie_phy *)devm_kzalloc(&pdev->dev,
+	priv = devm_kzalloc(&pdev->dev,
 			sizeof(struct ss14lpp_x2ns_pcie_phy), GFP_KERNEL);
 	if (priv != NULL) {
 		err = ss14lpp_x2ns_pcie_phy_of_parse_dt(pdev, priv);
@@ -527,8 +524,7 @@ static s32 ss14lpp_x2ns_pcie_phy_remove(struct platform_device *pdev)
 	s32 err = 0;
 
 	if (pdev != NULL) {
-		const struct ss14lpp_x2ns_pcie_phy *priv =
-			(const struct ss14lpp_x2ns_pcie_phy *)platform_get_drvdata(pdev);
+		const struct ss14lpp_x2ns_pcie_phy *priv = platform_get_drvdata(pdev);
 
 		pm_runtime_disable(priv->dev);
 		err = ss14lpp_x2ns_pcie_phy_disable_clk(priv);
diff --git a/include/soc/telechips/dw_edma.h b/include/soc/telechips/dw_edma.h
deleted file mode 100644
index c58a9f227cc8..000000000000
--- a/include/soc/telechips/dw_edma.h
+++ /dev/null
@@ -1,30 +0,0 @@
-// SPDX-License-Identifier: GPL-2.0-or-later
-/*
- * Copyright (C) Telechips Inc.
- */
-
-#ifndef DW_EDMA_H
-#define DW_EDMA_H
-
-enum edma_data_direction {
-	EDMA_NONE = 0,
-	EDMA_TO_PNR = 1,
-	EDMA_FROM_PNR = 2,
-};
-
-static inline s32 valid_edma_direction(enum edma_data_direction dir)
-{
-	return (dir == EDMA_TO_PNR) || (dir == EDMA_FROM_PNR);
-}
-
-#ifdef CONFIG_PCIE_DW_EP
-s32 dw_edma_async_transfer(void *arg, enum edma_data_direction dir, u64 src, u64 dst, u32 sz, bool pending);
-#else
-static inline s32 dw_edma_async_transfer(void *arg, enum edma_data_direction dir, u64 src, u64 dst, u32 sz, bool pending)
-{
-	return -ENXIO;
-}
-#endif
-
-#endif	/* DW_EDMA_H */
-
-- 
2.25.1

